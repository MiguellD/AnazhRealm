# AnazhRealm – Projektgedächtnis für Claude

Diese Datei wird bei jeder Session automatisch geladen. Sie trägt **nur, was JETZT gilt** — den aktuellen Stand, die technischen Gotchas, die Konventionen. Drei getrennte Heimaten für die drei Zeit-Ebenen des Wissens:

- **JETZT** (diese Datei) — was gilt: Stand, Gotchas, Konventionen, Doc-Map.
- **DIE CHRONIK** (`docs/handover.md`) — die volle Wellen-Historie, jede Welle ein ausführlicher Eintrag, plus „wie du eine Session startest".
- **DER PLAN** (`docs/roadmap.md`) — was vorwärts kommt. Die Vision in `docs/state-of-realm.md`.

## Aktueller Stand (V9.61, 23.05.2026 — Wasserfall-Volume nach Genshin-Lehre, der Vorhang wird voluminös)

**Wo wir stehen:** eine Voxel-basierte 3D-Browser-Sandbox in einer Datei (`anazhRealm.js`, ~37 340 Zeilen). Audit-Strict 0 Failures, **3060 Playtest-Invarianten grün**. V9.60-Bogen geschlossen (vier Schöpfer-Befunde); **V9.61 antwortet auf Schöpfer-Foto „Wasserfälle sehr unsauber"** — drei Genshin-Lehren kombiniert: Trapez-Plane (unten breiter), Volumetric-Spray-Halbsphere, stärkere Wave-Displacement im Shader.

**Letzte Arbeit — V9.61 Wasserfall-Volume nach Genshin-Lehre:** Schöpfer-Browser-Foto (Wasserfall im Voxel-Terrain): „die Wasserfälle sind sehr unsauber, die Systeme fließen sehr schlecht ineinander, weiche geschwungene wilde Formen, welches Spiel begeistert hier?". Antwort: Genshin Impact (Sumeru/Liyue), BOTW. Drei Profis-Techniken kombiniert: (a) **Trapez-Plane** in `_buildHydroWaterfall` — Vertices verschoben sodass die Plane unten 1.5× breiter ist als oben (0.9×), plus sin-Edge-Wobble für organische Kontur. Macht den flachen Vorhang voluminös. (b) **Volumetric Spray** in neuer `_buildHydroWaterfallSpray` — SphereGeometry-Hemisphäre am Aufprall, vertikal gestaucht (`vScale = height/radius`), `aShore`-Verlauf von 1.0 unten bis 0.45 oben → dichte Mist-Wolke, die nach oben dämpft. Kein Partikel-System nötig. (c) **Stärkere Billow** in `_ensureWaterfallMaterial`-Vertex-Shader: Amplituden 0.20+0.10 → 0.40+0.22, plus cross-wave `sin(uv.x*11+uTime*1.7)*0.08` für 3D-Bewegung. **Test-Sync**: Mesh-Count × 3 → × 4 pro Wasserfall (Plane + Lip + Spray + Pool); WF_KINDS erweitert; Material+Geometry-Check unterscheidet alle vier (Plane=wfMat+PlaneGeometry, Lip=hydroSurfaceMat+PlaneGeometry, Spray=hydroSurfaceMat+SphereGeometry, Pool=hydroSurfaceMat+CircleGeometry). **Verhaltens-Beweis**: Playtest grün; audit:strict 0 Failures; Format/Lint sauber; alle V9.60-Topologie-Metriken stabil. Dateien: `anazhRealm.js` +63 Z. (neue Spray-Methode + Trapez-Verzerrung + Shader-Modulation), `scripts/playtest.cjs` +12 Z. netto (Invarianten auf 4-Mesh-Welt), Version-Bump 9.60.6 → 9.61. **Lehre verdrahtet**: Genshin-Wasserfall-Geheimnis = drei Komponenten zusammen — Volume statt Vorhang (Trapez), Volumetric Spray (Hemisphäre) ohne Partikel-System, Wave-Displacement im Vertex-Shader (Stärke). Keine eine reicht alleine; die Kombination begeistert. Plus: SphereGeometry mit aShore-Verlauf ist die saubere Foam-Wolken-Technik — Profis nutzen Distance-Field-Tricks, wir nutzen aShore-Gradient als Approximation.

**Davor — V9.60-d abgeschlossen (drei Profis-Lehren kombiniert für die See-Fluss-Naht):**

**Letzte Arbeit — V9.60-d.2/3/4 kombiniert (Schöpfer-Anweisung „diese Schritte kombiniert angehen, von Profis lernen und d abschliessen"):** drei Profis-Lehren in einem Schnitt: (1) **d.3 See-Spiegel-Verbindung** — in `_buildHydroWaterfall` wird `topY = max(wf.topY, _waterLevelAt(wf.x, wf.z))` berechnet; wenn der See-Spiegel höher als das Voxel-Bett liegt, reicht die Wasserfall-Plane bis zum See-Spiegel → kein Riss mehr zwischen See-Wasserfläche und Plane-Top. Witcher 3 + NMS machen das genauso. (2) **d.2 Top-Lip-Foam** — neue Methode `_buildHydroWaterfallTopFoam`: schmale horizontale PlaneGeometry (breit quer, schmal in Fließrichtung) auf `topY + 0.1` m, Foam via `aShore=1`. Genshin/BOTW haben diesen Foam-Saum als universelles Detail. (3) **d.4 Erosion-Topf** — der Foam-Pool sitzt jetzt auf `bottomY - 0.25` m (statt +0.15) und hat größeren Radius (`width*1.5` statt `1.3`, `+3.5+dropH*0.18` statt `+3+0.15`) — simuliert die Real-World-Sediment-Erosion am Aufprall (der Wasserfall „frisst" eine Mulde in den Boden). **Test-Sync** (V9.56-i-Lehre wiederholt): Mesh-Count jetzt × 3 statt × 2 pro Wasserfall (Plane + Lip + Pool); `WF_KINDS`-Array für die drei `hydroKind`-Varianten; Material+Geometry-Check unterscheidet alle drei Typen. **Verhaltens-Beweis**: Playtest grün (3060); audit:strict 0 Failures; Format/Lint sauber; alle V9.60-Topologie-Metriken stabil. Dateien: `anazhRealm.js` +44 Z. (neue Top-Lip-Methode + d.3/d.4-Anpassungen + 3-Mesh-Loop), `scripts/playtest.cjs` +10 Z. netto (drei strukturelle Invarianten angepasst auf 3-Mesh-Welt), Version-Bump 9.60.5 → 9.60.6. **Lehre verdrahtet**: Profis-Lehre = drei Vorbilder gleichzeitig adressieren ergibt eine kohärente Welle. Statt fünf Sub-Wellen mit Diagnose zwischen jeder, eine kombinierte Welle wenn die Profis-Vorbilder zusammen passen. Wichtig: jeder Schritt muss strukturell unabhängig sein (jeder hat eigenen `hydroKind`, kein Konflikt mit den anderen). **V9.60-Bogen abgeschlossen — bereit für Welle A „Wasser ↔ Spieler-Wille" oder anderen Schöpfer-Befehl**.

**Davor — V9.60-d.1 Foam-Pool am Wasserfall (Vision-Pfeiler Wasser↔Wille der Welle A):** Schöpfer-Befund nach V9.60-b.1 (Foto eines Wasserfalls): „die Verbindung zwischen den Seen noch unsauber". Vor V9.60-d.1 fiel der Wasserfall unsichtbar in den Boden — kein visueller Aufprall-Akzent. **Riesen-Lehre**: Genshin Impact, BotW, Witcher 3, No Man's Sky haben alle einen hellen Foam-Schaum-Pool am Fuß jedes Wasserfalls — die typische Welt-Sprache. **Mechanik** in neuer Methode `_buildHydroWaterfallPool`: horizontale `CircleGeometry` auf `wf.bottomY + 0.15` (knapp über dem Wasser-Niveau, damit sie nicht im Carve verschwindet). Radius skaliert mit Drop-Höhe (`width * 1.3` oder `3 + dropH * 0.15`, das größere) → ein 50-m-Sturz spritzt mehr als ein 5-m-Lauf. Material: das geteilte `hydroSurfaceMaterial` mit per-Vertex `aShore=1` + `aFlow=0` + `aWave=0` → der Shader rendert volles Foam-Band, sanfte Pool-Ruhe. Ein zweites Mesh pro Wasserfall, kein neuer Shader nötig (Vision §1.3 fraktal — eine Wasser-Sprache). **Test-Sync** (V9.56-i-Lehre angewandt): drei Playtest-Invarianten musste mit-wandern, weil sie strukturell prüften „nur waterfall-mesh + nur waterfallMaterial + nur PlaneGeometry". Anpassung: Mesh-Count ist jetzt `× 2` (Plane + Pool), Material+Geometry je nach `hydroKind` unterscheiden. **Verhaltens-Beweis**: Playtest grün (3060); audit:strict 0 Failures; Format/Lint sauber; alle V9.60-Topologie-Metriken stabil (Land-Marge +21.4 m, Land-Anteil 68.9%, Ozean-Anteil 9.3%). **Was OFFEN bleibt** für die See-Fluss-Naht: (a) Top-Lip-Foam (Wasser-Krümmung über die See-Kante), (b) Bett-Kurve am See-Auslass (sanfter Übergang von horizontal zu vertikal), (c) Wasser-Verbindung wenn der See-Spiegel höher als der Wasserfall-Top liegt (sichtbarer Riss). **Lehre verdrahtet**: Riesen-Lehre auf Render-Schicht — vor jeder Wasser-Welle 2 Minuten andere Welten anschauen. Plus: jedes neue Hydrosphären-Mesh muss die struktur-Invarianten mit-wandern lassen (V9.56-i-Doku-Sync).

**Davor — V9.60-c.2 Continental Slope + Mid-Ocean Ridge (Schöpfer-Befund „flach, kannst du mehr von den Riesen lernen"):** vorheriger V9.60-c.1 war zu zaghaft (±4 m in 40 m Tiefe = 10%). Lehren aus Real-World-Ozeanografie + Subnautica + Minecraft + NMS: vier Schichten im echten Ozean — Continental Shelf (sanft), Continental Slope (DRAMATISCH ab), Abyssal Plain (tief flach mit Hügeln), Mid-Ocean Ridge (Bergketten + Trenches). Subnautica skaliert Drama mit Tiefe — Subtilität nahe Strand, dramatic in Tiefsee. **Mechanik** in `_terrainMacroSurfaceY` (ersetzt V9.60-c.1): `subMask` smoothstep 3..9 m (Shelf bleibt unberührt); `slopeDrop = -slope * 0.6` linear ab 4 m Tiefe (bei 20 m Tiefe zusätzliche -10 m Continental Drop); drei tiefen-skalierte Octaven — `subA` (Hügel λ~330m, ±3 bis ±9 m), `subRidge` (ridged λ~240m Bergketten + Trenches), `subC` (Sediment-Feindetail λ~100m, ±1 m). Sicherheits-Math: subMask + slopeDrop + Noise-Amplituden garantieren surface < waterRef bei jedem subMask-Wert → keine Phantom-Inseln. **Verhaltens-Beweis**: Playtest grün; alle V9.60-b-Akzeptanz weiterhin stabil; Land-Marge +21.4 m unverändert; surfMin -43 → -62 m (+19 m tiefer Drama); Ozean-Anteil 5.0% → 9.3%; p5 -20.6 → -27.1 m (Tiefsee differenzierter); Surface-Max + Decken-Marge unverändert (14.5 m oben, 28 m unten). **Was OFFEN bleibt für Schöpfer-Audit**: (Befund 2) See-Fluss-Naht / Wasserfall-Verbindung — V9.60-d, Vision-Pfeiler Wasser↔Wille der Welle A, Hydrosphäre-Code-Schicht. **Lehre verdrahtet**: zaghafte Submarine-Variation ist eine halbe Lösung; Subnautica/Real-World-Lehre = Drama mit Tiefe skalieren, plus echte Continental Slope (Bias-Drop). Riesen lernen heißt: nicht nur dasselbe Octaven-Pattern wiederholen, sondern die STRUKTUR der Echtwelt-Geometrie übernehmen (Shelf-Slope-Abyssal-Ridge-Hierarchie).

**Davor — V9.60-b.2 Sand-Variation nach Schöpfer-Audit „uniform, sollte verschmelzen + nicht überall gleiche Breite":**

**Letzte Arbeit — V9.60-b.2 Sand-Variation:** Schöpfer-Browser-Audit nach V9.60-b.1 brachte drei differenzierte Befunde: (1) Wassergrund flach/uninteressant (aber „um Welten besser"); (2) See-zu-See-Verbindung noch unsauber (Wasserfall-Naht); (3) Uferlinie etwas besser, aber zu uniforme Breite. Eisenhower-Auswahl: Befund 3 ist klein-Aufwand mit klarem visuellen Effekt → V9.60-b.2 zuerst, dann Browser-Audit, dann Befund 1/2 priorisieren. Mechanik in `_attachVoxelFieldColors`: drei Noise-Modulationen statt einer Konstante — (a) `widthNoise` λ~570m moduliert die Glocken-Breite [0.5, 1.9] m; (b) `intenseNoise` λ~290m moduliert die Sand-Intensität [0.25, 0.8]; (c) `widthNoise < 0.18` → karge Fels-Stellen ganz ohne Sand. Riesen-Lehre (Far Cry / Witcher Beach-Texturing): Strand-Variation entsteht durch Noise-Modulation MEHRERER Parameter, nicht durch eine Schwelle. Bestehende `_voxelNoise`-Instanz wiederverwendet (deterministisch, kein neuer Seed). **Verhaltens-Beweis**: Playtest grün; alle V9.60-b.1-Akzeptanz-Schwellen stabil (Land-Marge +21.4 m, Land-Anteil 69.3%, See-Höhen-StdAbw 10.9 m); Awareness-Counter 0/0. **Was OFFEN bleibt für Schöpfer-Audit**: (Befund 1) wassergrund-Topologie — eigene Welle V9.60-c, vermutlich Unterwasser-Noise-Octaven oder Sediment-Variation. (Befund 2) See-Fluss-Naht — V9.60-d, Hydrosphäre-Code-Schicht, Vision-Pfeiler Wasser↔Wille der Welle A. (Befund 3 jetzt) Strand-Variation. **Lehre verdrahtet**: vor jeder Welt-Welle die drei Schöpfer-Befunde priorisieren (Eisenhower), kleinster Aufwand mit klarstem Symptom-Bezug zuerst, dann substantiellere Wellen nachschieben.

**Davor — V9.60-b.1 Wurzel-Heilung: waterLevel ist jetzt absolut, nicht adaptives 35-Perzentil:**

**Letzte Arbeit — V9.60-b.1 waterLevel absolut (Lehre von Minecraft 1.18+ Sea Level):** zwei Versuche scheiterten vorher (tanh-Shaping + Tiefland-Streckung der Tektonik). Wurzel-Erkenntnis: die `waterLevel = heights[35-Perzentil]`-Sample-Region ist 340×340 m, viel kleiner als die Tektonik-Wellenlänge 7150 m → alle 169 Samples sehen denselben tect-Wert, also wirken Tektonik-Änderungen NICHT auf waterLevel. Das adaptive 35-Perzentil garantiert PER DEFINITION 35%+ Ozean — egal wie die Tektonik aussieht. **Heilung von Riesen:** Minecraft 1.18+ nutzt eine ABSOLUTE Sea-Level-Konstante, nicht adaptiv. Wir setzen `state.waterLevel = (terrainBaseHeight || 0) − 3` (existierender Fallback wird Default). **Empirische Resultate** (V9.60-b.diag-Akzeptanz): Land-Marge +0.8 → **+21.4 m** ✓; Land-Anteil 48.9% → **69.3%** ✓; Fluss-Längen-Median 80 → **1536 m** ✓; See-Höhen StdAbw 6.3 → **10.9 m** ✓; Hochseen 18 → **47**; Tarns 2 → 4; Surface-Max + Decken-Marge unverändert (kein Tradeoff). Welt-Determinismus bricht ehrlich (alte Welt-Seeds sehen anders aus, neue Hochseen sind sichtbar). **Nebenwirkung**: Ozean-Anteil fällt auf 4.9% (drastisch land-dominiert) — vielleicht zu krass; V9.60-b.2 könnte `terrainBaseHeight - 8` oder einen Mittelwert wählen, falls Schöpfer-Audit das sagt. **Verhaltens-Beweis**: Playtest grün; Format/Lint/Syntax sauber. Eine Zeile in `loadState` (Z9959). **Lehre verdrahtet**: Sample-Region-Skala muss zur Welt-Skala passen — eine adaptive Konstante über zu kleine Region ist faktisch fixed-broken. Vor adaptiver Berechnung: Sample-Footprint vs. Wellenlängen der modulierenden Octaven prüfen. Plus: Riesen-Lehre vor Selbst-Erfindung („so wenig wie möglich, lernen von anderen, die das schon durch haben"). Minecraft-Sea-Level ist DIE bewährte Lösung — 1 Zeile, kein Schmerz, alle Metriken geheilt.

**Davor — V9.60-a zurückgerollt nach Schöpfer-Kritik:** zwei Schliffe wurden nach V9.59 versucht, BEIDE entpuppten sich als Symptom-Bekämpfung statt Wurzel-Heilung. Der Schöpfer hat das klar benannt. (1) **Submarine-Biom-Dämpfung** (`_attachVoxelFieldColors`, `biomeStrength`-Faktor unter Wasser): die Lava/Magie/Wiese-Farben durch das Wasser durchstrahlend wurden ausgeblendet. Schöpfer-Verdikt: „symptombekämpfung, kein fix" — die Biome SOLLTEN unter Wasser NICHT entstehen, nicht nur unsichtbar werden. Vollständig zurückgerollt. (2) **Sand-Tint am Ufer** (V9.59-d.1): konzeptuell richtig (Strand IST eine echte Welt-Wahrheit), aber visuell UNIFORM weil die Uferlinie selbst uniform ist (Seen auf ähnlicher Höhe + flacher Ozean → eine Beige-Linie überall gleich). Schöpfer-Verdikt: „wirkt sehr unnatürlich, zu homogen, in echt gibt es strände und karge stellen, grosse und kleine buchten". Bleibt vorerst als V9.59-d.1, weil die Heilung durch V9.60-b (mehr Topologie-Variation → mehr Uferlinien-Variation → Sand variiert von selbst) erfolgt. **Vorheriger V9.60-a-Versuch (waterLevel-Perzentil 0.35→0.20)**: brach die V9.51-Tarn-Invariante (2/3 statt 3/3 Tarns wurden Seen); davor schon zurückgerollt — dokumentiert weil die Lehre wertvoll ist: drei Systeme sind co-getuned (waterLevel + TARN + Priority-Flood). **Was OFFEN bleibt — die einzige ehrliche nächste Welle**: V9.60-b strukturelle Hydrosphäre-Topologie-Heilung (Multi-Plateau-Tektonik, längere Flüsse, See-Höhen-Differenzierung). Eigene Diagnose-Pass-Erweiterung (Fluss-Längen, See-Höhen-Histogramm-Breite). Substantielle Welle mit gekoppelten Anpassungen — sub-wellen-Disziplin pro Knoten. **Lehre als Gotcha verdrahtet**: bei Schöpfer-Befunden zuerst die WURZEL klassifizieren (welche Mechanik ergibt das Symptom?), DANN heilen. Eine visuelle Notbremse ist NICHT ehrlich, wenn die Wurzel offensichtlich ist (V9.58-Lehre: empirische Diagnose), nur weil sie schneller geht. Eisenhower-Disziplin: wichtig vor dringlich.

**Davor — V9.59 Welt versteht Wasser (drei Sub-Wellen):**

**Letzte Arbeit — V9.59 Welt versteht Wasser (drei Sub-Wellen):** Schöpfer-Audit nach V9.58 zeigte zwei Befunde — (1) Seen liegen auf ähnlicher Höhe, keine langen Flüsse über weite Höhen-Distanzen; (2) Welt platziert Vegetation + Bauwerke ohne Wasser-Maske → Bäume im See, Gras über Wasser, „Wiese spürt das Wasser nicht". Befund (2) trifft den Vision-Pfeiler „Welt atmet" am direktesten und wurde zuerst geheilt. **V9.59-a Wurzel-Funktion**: neue `_isAboveWaterAt(x, z, marge=0)` direkt nach `_waterLevelAt` — vergleicht Voxel-Surface mit Wasser-Spiegel, liefert true wenn Boden über Wasser + Marge. EINE semantische Quelle für alle Welt-Schichten. `surfaceY = null` (Höhle/Loch) zählt als nicht-Land. **V9.59-b Vegetation-Mask** in `_vegetationSampleSpawn` (Bäume, Felsen, Geoden, Glutbrunnen, Felsformationen): `if (!this._isAboveWaterAt(sampleX, sampleZ, 0.4)) return 0;` — 0.4 m Marge, damit eine Architektur nicht knöcheltief im flachen Ufer steht. Welt-Bauwerke laufen über DSL-Ops (User-driven), Worldgen-Architekturen über genau diesen Pfad. **V9.59-c Gras-Mask** in `_buildVoxelChunkGrass`: `if (surfY < waterY + 0.1) continue;` — 0.1 m Marge, Gras DARF an der Uferlinie wachsen (saftiges Ufer), aber nicht im See. Performance: 256 Samples × Chunk × `_waterLevelAt` O(9), unter ms-Bereich. **Diagnose-Tool erweitert** (`scripts/diag-relief.cjs`): zählt jetzt Architekturen + Gras-Halme unter Wasser als „Sünde-Counter" — V9.59-Beweis: vorher mehrere hundert, jetzt 0/0. **Verhaltens-Beweis**: Playtest grün („Alle Invarianten OK", 3060); audit:strict 0 Failures; Format/Lint/Syntax sauber. Dateien: `anazhRealm.js` +29 Zeilen (1 Wurzel-Funktion + 2 Gates), `scripts/diag-relief.cjs` +40 Zeilen (Awareness-Counter + Report), Version-Bump 9.58 → 9.59. **Was OFFEN bleibt**: V9.59-d Küsten-Biom (Strand-Streifen + Schilf — "Wiese spürt das Wasser" als positive Schicht, nicht nur negative Maske); Befund (1) See-Höhen-Differenzierung wartet als V9.60. **Lehre als Gotcha verdrahtet** (siehe „Wichtige Gotchas/Terrain + Chunks · Welt-Awareness"): jede neue Welt-Spawn-Schicht MUSS `_isAboveWaterAt` konsultieren, sonst entsteht eine neue „Gras-im-See"-Klasse-Schuld.

**Davor — V9.58 Relief-Vertiefung in drei Sub-Wellen (a/b/c):** Schöpfer-Audit der V9.51-Bergsee-Welle ergab im Browser KEINE sichtbaren Hochseen + Berge „zu wenig hoch, zu wenig weit, nicht über weite phasen aufbauend". **Diagnose statt Würfel-Tuning**: neues `scripts/diag-relief.cjs` sampelt 4000 Random-Punkte + liest die Hydrosphäre-Stats + Tarn-Bilanz + zwei Screenshots. **Empirische Vor-V9.58-Befunde**: Surface-Max 60.9 m (theoretisches Max +80 m, realistisch 60), p95 nur 33.7 m, Median 7 m (Welt Flachland-dominiert); 20 Seen, davon 10 als „Hochsee" klassifiziert (level > waterLevel+2), aber **höchster Spiegel nur 9.7 m** = 6.5 m über Meeresspiegel → visuell ununterscheidbar. **Drei distincte Wurzeln** identifiziert: (A) vertikale Amplitude zu niedrig; (B) räumliche Verteilung zu klein (`mtn`-Feld λ ~1850 m → punktuelle Hochgebirge); (C) Tarn-Konstanten an die kleine Welt geklemmt. **V9.58-a** Tektonik-Octave (`tect = noise2D(wx*0.00088, wz*0.00088) * 35`, λ~7150 m ±35 m) + `mtn`-λ von 1850 → 4500 m → Surface-Max +18.6 m, See-Spiegel-Max 9.7 → 25.5 m. **V9.58-b** `ridgeAmp` `9+33·mtn²` → `12+55·mtn²` + zweite ridged-Octave (λ/2, ½ Ampl., multifraktale Selbstähnlichkeit) + Voxel-Decke `dimY 100 → 124, floorDrop 74 → 90` (Band base-90..base+133, Marge oben ~13/unten ~10) → Surface-Max 79.5 → 118.7 m, See-Spiegel-Max 25.5 → 41 m. **V9.58-c** Tarn-Konstanten skalieren: `reliefPercentile 0.58 → 0.55`, `minReliefAbs 14 → 30`, `maxSlope 0.25 → 0.35`, `placeThreshold 0.05 → −0.1`, `depthMin 22 → 28`, `depthMax 42 → 55`. Lehre: erster Versuch mit `depthMin=35` ließ Tarns auf 1 fallen — die `maxAllowed = surf − waterRef − 1` Klemmung schluckt Spots; auf 28 zurück. **Gesamt-Bilanz**: Surface-Max **+95%**, p95 **+82%**, See-Spiegel-Max **+322%**, Hochseen 10 → 18 (+80%). Diagnose-Tool `scripts/diag-relief.cjs` bleibt als ehrliches Mess-Werkzeug für künftige Worldgen-Wellen. **Verhaltens-Beweis**: Playtest grün („Alle Invarianten OK", 3060); audit:strict 0 Failures; Format/Lint/Syntax sauber. Dateien: `anazhRealm.js` +48/−18, `scripts/diag-relief.cjs` neu (276 Z.), `package.json` + `index.html` Version-Bumps 9.56 → 9.58. **Konsequenz**: §1.1-Phase-0 (Browser-Audit V9.50/V9.51) ist ABGESCHLOSSEN — V9.50/V9.51 waren mechanisch gebaut, V9.58 macht sie sichtbar. Welle A (Wasser↔Spieler-Wille) kann jetzt mit empirischem Boden starten. **Lehren als Gotcha verdrahtet** (siehe „Wichtige Gotchas/Terrain + Chunks · V9.58"): Empirie schlägt Theorie bei Worldgen-Parametern; Drei-Wurzel-Klassifikation vor erstem Edit; Sub-Wellen mit Diagnose-Validierung zwischen jeder; Folge-Klemmungen sind heimliche Killer; Voxel-Decken-Marge muss MIT skalieren; Determinismus-Bruch ehrlich benennen.

**Davor — V9.56-l Theme-Konsistenz-Schliff (Audit-Folge):** der `/audit` zeigte vier semantische CSS-Vars als „Warnings" (`--parch-bg`, `--parch-line`, `--parch-mid`, `--brass-mid`) — an 12 Stellen in `index.html` mit Fallback genutzt, aber NICHT in `:root` definiert. **Der tiefe Befund war Vision-substantiell, nicht kosmetisch**: diese 12 UI-Stellen (Keybind-Buttons, Chat-Proposal-Buttons, Trennlinien in Listen) IGNORIERTEN den Tag/Nacht-Theme-Wechsel — wenn der Spieler in die Nacht eintauchte, atmete die Welt um (Himmel, Licht, Wasser, Terrain), aber diese 12 UI-Stellen blieben starr im Pergament-Ton. Plus: `--parch-bg` trug ZWEI semantisch verschiedene Fallbacks — Z.582 `rgba(38,26,14,0.95)` (Welt-Overlay-Backdrop, semi-transparent) vs. 7 weitere Stellen `#ece1c5` (opakes Pergament). Diese Doppel-Semantik war die eigentliche Code-Schuld. **Die Heilung — Aliase statt Duplikate**: 4 Aliase auf existierende Wurzel-Vars (`--parch-bg → var(--parch-1)`, `--parch-line → var(--parch-edge)`, `--parch-mid → var(--wood-2)`, `--brass-mid → var(--brass-2)`) + eine neue `--overlay-bg` für die Z.582-Welt-Overlay-Semantik. **Schlüssel-Disziplin (CSS-Spec)**: Custom Properties resolvern am Use-Site, nicht am Define-Site. EIN Alias in `:root` reicht, weil `[data-theme="nacht"]` die Wurzel-Vars überschreibt und der Alias automatisch mit-atmet. Keine Duplikation zwischen Theme-Blöcken, eine Quelle der Wahrheit pro semantische Var. **Verhaltens-Beweis**: audit:strict CSS-Var-Warnings 4 → 0 (Failures 0, Warnings 13 → 9 — nur noch der akzeptierte METHOD-SMOKE-Floor); Format/Lint/Syntax sauber; Playtest grün (3060 Invarianten OK). Datei: `index.html` +12 Zeilen netto (5 neue Vars + Kommentar + 1 Wert-Edit Z.582). **Lehre als Gotcha verdrahtet** (`## Wichtige Gotchas/UI · Theme`): neue semantische CSS-Vars als Aliase auf die numerischen definieren, nie als duplizierte Werte zwischen Themes. Wer eine neue UI-Stelle mit `var(--neueVar, fallback)` baut, soll die Var ALS ALIAS in `:root` definieren — sonst entsteht ein Tag/Nacht-tauber Fleck.

**Davor — V9.56-k der letzte Schnitt der Kontrollfluss-Etappe, GLEICHZEITIG der Bogen-Schluss:** `loadState` (380 Z. → 21 Z. Orchestrator) war die heikelste Funktion (V8.59-Bug-Klassen-Quelle: `portalMeta` ging beim Reload verloren). **V9.56-i-Lehre angewandt VORAB**: `grep -n 'loadState\.toString' scripts/playtest.cjs` → KEIN Treffer. Alle Tests rufen `loadState(snap)` zum Verhaltens-Test, keiner scannt Source-Patterns. Damit war der Refactor frei von Test-Anpassungs-Pflicht. **Block-Grenzen-Inspektion**: 24 sequenzielle Restore-Phasen, ZERO shared lokale State zwischen Phasen — jede Phase liest `state.X` und schreibt `this.state.X`. Aber strikte ORDNUNGS-Abhängigkeiten existieren (Materialien VOR Bauplänen wegen `validateBlueprintParts`; Custom-Seelen VOR `applyPlayerSoul`; Baupläne VOR Architekturen/Hotbar/Inventory wegen Builder-Referenzen; Baupläne VOR Tools wegen `sourceBlueprint`). **Thematische Gruppierung — 13 Helfer extrahiert** (median ~21 Z., max 45): `_loadStateLoadFromStorage` 17 Z. (drei Quellen mit JSON-Parse-Defensive), `_loadStateRestorePlayerPosition` 25 Z. (Ring 8.2 — terrainEverGenerated-Flag + Body-Transform), `_loadStateRestoreBasicState` 16 Z. (knowledgeBase + creatures Snapshot-Stash mit V7.86-Schema-Heuristik), `_loadStateRestoreWorldMeta` 7 Z. (Migration mit V9.36 chunkDeltas-still-ignored), `_loadStateRestoreSoulAndAtmosphere` 45 Z. (customSouls VOR playerSoul + Tag-Nacht-Slider + applyDayNightToScene), `_loadStateRestoreConsumablesAndEquipped` 14 Z., `_loadStateRestoreCraftingInventory` 44 Z. (Materialien→Baupläne→Tools mit V9.44-a-Deserialize-Aufruf), `_loadStateRestoreArchitectures` 14 Z. (dispose + respawn), `_loadStateRestoreHotbarAndInventory` 32 Z. (9-Slot Hotbar + 27-Slot Inventory + playerTools-Set-Union), `_loadStateRestoreEmotionsAndDsl` 31 Z. (Emotionen + dslAbilities + dslHistory + patternMemory), `_loadStateRestoreMiscState` 31 Z. (pathBuckets + worldJournal + legacy abilities), `_loadStatePersistExternalImport` 23 Z. (Per-Welt-Key + Index + activeWorldSet), `_loadStateRestoreVersionHistory` 6 Z. (globaler `anazhRealmVersions`-Key). **Schlüssel-Disziplin (V8.59-Bug-Klassen-Schutz)**: jeder Helfer hat genau EINE thematische Verantwortung; die Ordnungs-Abhängigkeiten leben im Orchestrator-Aufruf-Sequenz, nicht versteckt in den Helfern. Ein zukünftiger Entwickler, der ein neues persistentes Feld hinzufügt, weiss sofort welcher Helfer es braucht (z.B. eine neue Bauplan-Eigenschaft → `_loadStateRestoreCraftingInventory`). Die V8.59-Bug-Klasse (zwei handgekoppelte Feld-Sätze in `buildStateSnapshot` + `loadState`) bleibt strukturell unverändert, aber jeder Helfer ist jetzt klein genug, dass das Vergessen eines Felds beim Review SICHTBAR ist. **Verhaltens-Beweis**: Playtest grün VOR + NACH; Format/Lint/Syntax sauber. ALLE Verhaltens-Tests passieren (W14 P1 portal-Restore, KI-Übersetzer translatedWorldId, W15-Vendor trust, Ring 8.2 position-restore + flagSetAfterLoad, multiple `loadState(snap)`-Calls in der gesamten Test-Suite). Datei: 37 112 → 37 126 (+14 Z. — 13 neue Methoden-Header, sehr ökonomisch). **BOGEN-ABSCHLUSS-STATISTIK**: 11 Funktionen geschnitten (1 pro Sub-Welle), **77 neue benannte Helfer**, Datei netto +95 Zeilen (37 031 → 37 126). Die UI-Builder-Etappe brachte 4 Funktionen + 22 Helfer (a 7 + b 4 + c 5 + d 6); die Kontrollfluss-Etappe brachte 7 Funktionen + 55 Helfer (e 8 + f 3 + g 6 + h 10 + i 9 + j 6 + k 13). **Vier netto-negative Sub-Wellen** (V9.56-d −2, V9.56-i −30, V9.56-j −54 — alle drei durch Kommentar-Konsolidierung, V9.56-i zusätzlich durch Duplikat-Konsolidierung). **Die offene Rest-Grenze (Code-Hygiene 2): NULL** — der gesamte Bogen ist abgearbeitet.

**Davor — V9.56-j der sechste Schnitt der Kontrollfluss-Etappe:** `generateTerrainWithParameters` (346 Z. → 73 Z. Orchestrator) ist der Worldgen-Setup-Konstruktor mit 14 logischen Phasen — Cleanup → Player-Position → Erosion → Konstanten-/Chunk-State → Inseln → Vegetations-Leere → Terrain-State → Voxel-Cache → Hydrosphäre → Kreaturen → Genesis-Plattform. **Block-Grenzen-Inspektion**: viele Phasen sind unabhängig (jede liest/schreibt eigene state-Felder), aber EINIGE haben kritische Ordnungs-Abhängigkeiten — z.B. Islands liest `state.terrainBaseHeight` BEVOR es vom neuen `baseHeight`-Parameter überschrieben wird (Fallback-Verhalten muss erhalten bleiben). **Sechs Helfer extrahiert** (median 19, max 77): `_worldgenCleanupOldObjects` 77 Z. (7 Sub-Cleanups: groundMesh + floatingIslands + ufos + creatures + wallBoxes mit V8.26-VRAM/Heap-Leak-Heilung + vegetation + Hydrosphären-Meshes), `_worldgenInitPlayerPosition` 20 Z. (first-spawn vs. erhalten), `_worldgenComputeErosionAndTarns` 11 Z. (V9.47 Erosion + V9.51 Tarns mit try/catch), `_worldgenSpawnFloatingIslands` 33 Z. (3 Inseln + UFOs, Math.random-Reihenfolge erhalten), `_worldgenBuildVoxelChunkCache` 18 Z. (voxelPopulatedChunks aus Altbestand), `_worldgenComputeAndBuildHydrosphere` 18 Z. (Atlas + Render mit zwei try/catch). **Schlüssel-Disziplin (`wasFirstSpawn` im Orchestrator)**: der Orchestrator liest `!terrainEverGenerated` VOR dem `_worldgenInitPlayerPosition`-Aufruf (übergibt es als Parameter) und nutzt dasselbe Flag später für `grokMarkFirstSpawn` + Genesis-Platform. Eine Alternative wäre, das Flag aus dem Helfer zurückzugeben — aber im Orchestrator zu lesen macht den Daten-Fluss linear sichtbar. **Schlüssel-Disziplin (Math.random-Reihenfolge bit-identisch)**: `_worldgenSpawnFloatingIslands` hält die 5 Math.random()-Aufrufe pro Insel in derselben Reihenfolge wie zuvor (size, height, x, z, islandY, ufoSpeed) — der Welt-Seed-Determinismus bleibt erhalten. **Schlüssel-Disziplin (Lösch-Beweis-Tests OHNE Anpassung grün)**: V9.34/V9.38-Tests grep'pen `generateTerrainWithParameters.toString()` für NICHT-Vorhandensein toter Patterns (`caveData`/`volcanoData`/`new Float32Array(WIDTH*DEPTH)`/`for z=0;z<DEPTH`). Mein Refactor entfernt keine Patterns, also bleiben sie weiterhin abwesend — Tests grün ohne Mit-Wandern. Eine andere V9.56-i-Lehre-Anwendung: NICHT alle Tests müssen mit-wandern; nur die, die NACH einem Pattern suchen, brechen beim Helfer-Split. Negative Tests (Pattern darf NICHT existieren) sind robust. **Verhaltens-Beweis**: Playtest grün VOR + NACH; Format/Lint/Syntax sauber. Side-Effect-Stream-Reihenfolge bit-identisch (Cleanup → Player → erosion → chunkState → islands → vegetation-leere → terrainState → voxelCache → hydrosphere → creatures → genesis). Datei: 37 166 → 37 112 (**−54 Z. netto-negativ!** dank Kommentar-Konsolidierung — die V9.56-i-Lehre wiederholt). **Bogen-Statistik nach V9.56-j**: 14 Funktionen geschnitten, 62 neue benannte Helfer, Datei netto +81 Zeilen (37 031 → 37 112). **Was BLEIBT in der Kontrollfluss-Etappe**: NUR NOCH `loadState` 380 — die heikelste (Snapshot/Restore, V8.59-Bug-Klassen-Quelle).

**Davor — V9.56-i der fünfte Schnitt der Kontrollfluss-Etappe:** `_applyDayNightToScene` (305 Z. → 16 Z. Orchestrator) hat zwei strukturell verschiedene Schichten — die V9.56-Lehre der „Block-Grenzen-Hierarchie" auf ihrer reinsten Form. **Block-Grenzen-Inspektion**: (A) **Tint-Akkumulation** (3 Sub-Phasen, ~85 Z., STARK gekoppelt) — Wetter-Tint setzt skyMul/lightMul, Welt-Feld-Tint mutiert skyColor/lightColor, Emotion-Tint mutiert alles drei plus lightIntensity. Diese drei Phasen teilen 3+ Variablen → bleiben in EINEM Helfer (`_dayNightComputeTint` 93 Z.); (B) **Fanout-Anwendung** (8 Phasen, ~165 Z., UNABHÄNGIG) — jede Apply-Phase konsumiert tint/sunDir, schreibt in ihren eigenen Material/Light/Fog-Slot. Diese acht Phasen sind individuelle Helfer: `_dayNightSunDirection` 7, `_dayNightApplySkybox` 7, `_dayNightApplyStarField` 8, `_dayNightApplyDirectionalLight` 14, `_dayNightApplyAmbient` 7, `_dayNightApplyHemiAndFog` 45, `_dayNightApplyTerrainShaderUniforms` 19, `_dayNightApplyWaterMaterials` 18. Median 14 Z., max 93 (Tint). **Schlüssel-Disziplin (Tint-Objekt schliesst Derivate ein)**: `_dayNightComputeTint` liefert nicht nur die raw Tint-Werte (skyColor/lightColor/lightIntensity/skyMul/lightMul), sondern AUCH die abgeleiteten skyR/skyG/skyB (skyColor.r × skyMul). Warum: HemiFog + Skybox brauchen die GLEICHEN derivierten Werte; jeder Helfer würde sie sonst re-berechnen. Das Tint-Objekt ist die natürliche Schnittlinie zwischen Akkumulation und Fanout. **DUPLIKAT-Konsolidierung als Bonus-Resultat (verhaltensneutral)**: das Original hatte ZWEI identische Hydro-Material-Apply-Blöcke (V8.29.1 + V9.43-c, beide schrieben dieselben Uniforms in `this.state.hydroSurfaceMaterial`). `_dayNightApplyWaterMaterials` ruft die `applyTo`-Closure jetzt EINMAL für hydroSurfaceMaterial + einmal für waterfallMaterial — die zweite Hydro-Anwendung war idempotent (writes mit gleichem Wert auf demselben Material), also verhaltensneutral. **Verhaltens-Beweis ZWEISTUFIG**: Playtest grün VOR; nach Code-Refactor 4 strukturelle Invarianten ROT (`V8.32: Unterwasser-Tint`, `V8.46: weatherEffect/cloudCover cross-fadet`, `V8.48: lightDir aus sunDir`); Diagnose ergab: die Tests grep'pen `_applyDayNightToScene.toString()` nach spezifischen Code-Patterns (`playerEyesUnderwater`, `cu.weatherEffect.*_weatherBlendedValue`, `const lightDir = sunDir`), die jetzt in den Helfern leben. Test-Anpassung (Doku-Sync, nicht Code-Change): jede betroffene Test-Probe scannt jetzt den richtigen Helfer (`_dayNightApplyHemiAndFog`/`_dayNightApplyTerrainShaderUniforms`/`_dayNightApplySkybox`). Nach Anpassung GRÜN — die Invarianten verifizieren immer noch dieselbe semantische Wahrheit. Datei: 37 196 → 37 166 (**−30 Z. netto-negativ!** dank Duplikat-Konsolidierung; eine seltene Erfahrung im Bogen — V9.56-d war −2, hier −30). **Bogen-Statistik nach V9.56-i**: 13 Funktionen geschnitten, 56 neue benannte Helfer, Datei netto +135 Zeilen (37 031 → 37 166). **Was BLEIBT in der Kontrollfluss-Etappe**: 2 weitere Funktionen: `generateTerrainWithParameters` 346, `loadState` 380.

**Davor — V9.56-h der vierte Schnitt der Kontrollfluss-Etappe:** `processChatCommand` (285 Z. → 21 Z. Orchestrator) — eine 5-Stufen-Pipeline (Setup → Voxel-Debug → Portal → DSL-Parse → Legacy-Themen-Dispatch). **Block-Grenzen-Inspektion**: die ersten 4 Stufen sind unabhängige Early-Return-Pfade (jeder mit eigenem Cleanup-Verhalten); die letzte Stufe (Legacy) ist ein 17-Branch-if-else-Dispatcher mit klaren Themen-Clustern (Fähigkeiten, Persistenz, Welt-Verwaltung, System, Conversational). Jeder Branch liest nur `command`/`parts`/`state` — ZERO shared lokale State zwischen Branches. Das macht die Theme-Split-Extraktion sauber. **Neun Helfer extrahiert** (median ~26 Z., max 81): `_chatRecordInputForLearning` 12 (Schicht 1 — Knowledge/Emotion/Pattern-Memory/Activity), `_chatTryVoxelDebugCommand` 32 (voxel test/carve/fill, KEIN Cleanup im Original-Verhalten), `_chatTryPortalRoute` 12 (W12 P2), `_chatTryDslParse` 16 (Ring 2 P3 DSL-First mit lastUserProgram/Outcome/At-Update), `_chatDispatchLegacyCommand` 7 (Themen-Dispatcher), `_chatTryAbilityCommand` 23 (lerne/führe Fähigkeit), `_chatTryPersistenceCommand` 26 (speichere/lade Zustand, Datei-Picker), `_chatTryWorldCommand` 81 (Ring 8 — spawne/erschaffe/wechsle/lösche/liste Welten, der größte weil 5 Welt-Verben mit Slug-Parsing), `_chatTrySystemCommand` 61 (6 System-Befehle), `_chatHandleConversationalFallback` 32 (Kreatur → LLM → P2P-Voice → chatSuggest+Hilfe). **Schlüssel-Disziplin (V9.56-f/g-Lehre erweitert)**: die Try-Pattern-Konvention (`_chatTryXxx` returns `true` wenn handled) macht den Dispatcher ein lesbares ODER-Chain — `if A() return; if B() return; ... handleFallback()`. Die Cleanup-Asymmetrie (Voxel-Debug räumt NICHT, Portal+DSL räumen SELBST, Legacy fällt durch zu Orchestrator-Cleanup) bleibt bit-identisch — das war ein Bug-Klassen-Risiko: hätte ich „chatInput überall im Orchestrator räumen" geschrieben, wäre der voxel-test-Befehl plötzlich mit cleaner-Input nach Enter gerendert (Verhaltensänderung). Verhaltensneutralität verlangt: jeden Cleanup-Pfad einzeln preservieren. **Verhaltens-Beweis**: Playtest grün VOR + NACH; Format/Lint/Syntax sauber. Die DSL-Parse-State-Mutations (state.dsl.lastUserProgram/Outcome/At) sind in DERSELBEN Reihenfolge wie zuvor. Datei: 37 120 → 37 196 (+76 Z. — 11 neue Methoden-Header + JSDoc-Kommentare; eine Sub-Welle mit vielen kleinen Themen-Helfern kostet mehr Header als eine mit wenigen großen). **Bogen-Statistik nach V9.56-h**: 12 Funktionen geschnitten, 47 neue benannte Helfer, Datei netto +165 Zeilen (37 031 → 37 196). **Was BLEIBT in der Kontrollfluss-Etappe**: 3 weitere Funktionen: `_applyDayNightToScene` 305, `generateTerrainWithParameters` 346, `loadState` 380.

**Davor — V9.56-g der dritte Schnitt der Kontrollfluss-Etappe:** `_voxelChunkGeometry` (269 Z. → 25 Z. Orchestrator) ist eine 6-Phasen-Pipeline für Surface-Nets-Iso-Surface-Mesh-Konstruktion. **Block-Grenzen-Inspektion ergab**: die sechs Phasen (Density-Sampling → Surface-Vertices → Quad-Indices → Laplacian-Smooth → Crop-Pad → Gradient-Normals) sind SEQUENZIELL gekoppelt — jede liest die Ausgabe ihrer Vorgänger und übergibt an die nächste. Aber die Kopplung ist EINSEITIG (keine späteren Phasen schreiben in frühere), und jeder Pass hat klare Inputs/Outputs. Damit ist die Pipeline-Extraktion sauber: jede Phase wird ein Helfer mit explizitem return/mutate-Pattern. **Sechs Helfer extrahiert**: `_voxelSampleDensityGrid` 14 Z. (Float32Array-Aufbau, returns density), `_voxelExtractSurfaceVertices` 68 Z. (Pass 1 — Surface-Nets, returns {positions, vertCells, cellVert}), `_voxelEmitQuadIndices` 41 Z. (Pass 2 — Quad-Triangulation mit V9.41-Parity-Diagonalen, returns indices), `_voxelLaplacianSmoothPositions` 44 Z. (V9.41.b — MUTIERT positions in place), `_voxelCropPad` 27 Z. (V9.42-d — MUTIERT positions+indices in place via .length=0+push), `_voxelGradientNormals` 24 Z. (V9.16 — returns normals aus dem Dichte-Gradient). Median ~34 Z., max 68. **Schlüssel-Disziplin (V9.56-e+f-Lehre kombiniert)**: das mutate-in-place-Pattern (Smooth + Crop) wurde EXPLIZIT gemacht — die Helfer-Signaturen schreiben es als Vertrag (`positions.length = 0; positions.push(...)` statt return new arrays). Eine return-new-Array-Variante würde drei zusätzliche `positions = ...`-Zuweisungen im Orchestrator brauchen plus zwei `if (positions.length === 0) return null`-Re-Tests; in-place ist hier sauberer. **Schlüssel-Disziplin (gi/ci-Closures lokal in jedem Helfer)**: die `gi(i,j,k)`-Indizierungs-Closure ist in drei Helfern dupliziert (Density, Surface-Vertices, Quad-Indices) — eine Class-Method `_gridIndex(i,j,k,Nx,Ny)` wäre Symmetrie-Schönheit auf Kosten von Argumenten-Schwellung; eine Class-Constant für `EDGES[12][2]` wäre eine weitere Modul-Abstraktion. Lokal in jedem Helfer kostet 3-4 Zeilen Duplikation, aber jeder Helfer ist self-contained — das ist die richtige Granularität. **Verhaltens-Beweis**: Playtest grün VOR + NACH; Format/Lint/Syntax sauber. Voxel-Terrain rendert + Spieler steht drauf (die Playtest-Welt nutzt diese Funktion 4× pro Chunk-Stream + 1× im V9.51-Tarn-Pass-Test) — die Iso-Surface-Geometrie ist bit-identisch (deterministische Pure-Math, kein Math.random/Date.now im Pfad). Datei: 37 112 → 37 120 (+8 Z. — sechs Helfer-Header, sehr ökonomisch). **Bogen-Statistik nach V9.56-g**: 11 Funktionen geschnitten, 38 neue benannte Helfer, Datei netto +89 Zeilen (37 031 → 37 120). **Was BLEIBT in der Kontrollfluss-Etappe**: 4 weitere Funktionen: `processChatCommand` 285, `_applyDayNightToScene` 305, `generateTerrainWithParameters` 346, `loadState` 380.

**Davor — V9.56-f der zweite Schnitt:** `_tickCreatureTaskDirection` 267 → 8 Z. Orchestrator (Task-Dispatcher) + 3 Helfer (Follow 17, Gather 107, Build 125). Schlüssel-Lehre: Block-Grenzen-Inspektion ist eine HIERARCHIE; die Task-Branch-Ebene hat ZERO shared State, die Phasen-Ebene IN gather/build teilt Material/Speed-Computation.

**Davor — V9.56-e der erste Schnitt der Kontrollfluss-Etappe:** `_tickCreatureTaskDirection` (267 Z. → 8 Z. Orchestrator) ist ein lehrbuchsauberer Task-Dispatcher. **Block-Grenzen-Inspektion (V9.56-e-Lehre)**: vier Task-Branches (`wait`, `follow_player`, `gather`, `build`), jeder liest nur (creature, task, emotion) + `this.state`, ZERO shared lokale State zwischen Branches. Die Branches haben aber INTERNE Phasen-Logik (gather: bring vs harvest je nach `carrying`; build: take vs walk vs spawn je nach `carrying` + Distanz) die Material/Blueprint-Validierung + Speed-Computation teilen — diese Phasen bleiben innerhalb ihres Branches, nicht weiter zerlegt. **Drei Helfer extrahiert**: `_tickCreatureFollowPlayer` 17 Z. (haltDist + Happy-Emotion-Speed-Boost), `_tickCreatureGather` 107 Z. (2-Phasen bring/harvest), `_tickCreatureBuild` 125 Z. (3-Phasen take/walk/spawn). Median 107, max 125. **Schlüssel-Disziplin (vom V9.56-d-Domain-Analysis-Muster wiederholt)**: gather + build sind groß, aber strukturell EINHEITLICH — die Phasen teilen `material`/`blueprint` Validation + `buildSpeed`/`carrying`/`player`-Computation. Eine weitere Zerlegung in `_tickGatherBring(creature, task, carrying, player)` + `_tickGatherHarvest(creature, task)` würde 4-5 Parameter pro Aufruf brauchen und keine Klarheit gewinnen — die Phasen-Sprache lebt EINE Schicht tiefer als die Task-Sprache. **Verhaltens-Beweis**: Playtest grün VOR + NACH; Format/Lint/Syntax sauber. Die Task-Dispatcher-Reihenfolge bleibt bit-identisch (wander → wait → follow_player → gather → build → null). State-Mutations (assignCreatureTask, carrying-Zuweisung, journalAppend, log, renderInventoryUI, renderCreatureListUI, harvestArchitecture, spawnArchitecture) sind innerhalb der Helfer in DERSELBEN Reihenfolge — kein Side-Effect-Stream-Shift (V9.56-e-Lehre angewandt). Datei: 37 105 → 37 112 (+7 Z. — die drei Helfer-Header + JSDoc-Kommentare; sehr ökonomisch, weil die Helfer groß und wenig sind). **Bogen-Statistik nach V9.56-f**: 10 Funktionen geschnitten, 32 neue benannte Helfer, Datei netto +81 Zeilen (37 031 → 37 112). **Was BLEIBT in der Kontrollfluss-Etappe**: 5 weitere Funktionen: `_voxelChunkGeometry` 269, `processChatCommand` 285, `_applyDayNightToScene` 305, `generateTerrainWithParameters` 346, `loadState` 380.

**Davor — V9.56-e der erste Schnitt der Kontrollfluss-Etappe:** `fuseWorlds` 239 → 114 Z. Orchestrator + 8 Helfer (median ~16, max 22). Schlüssel-Erkenntnis: trotz state-mutating Welt-Fusion ist es eine PURE Sequenz mit ZERO shared State über Block-Grenzen. Das große Snap-Object bleibt INLINE (Symmetrie-Versuchung widerstanden). Math.random-Reihenfolge bit-identisch.

**Davor — V9.56-d der vierte Schnitt:** `_workshopRenderStatsPanel` 319 → 15 Z. + 6 Helfer (median 50.5, max 81). Datei **−2 Z. netto-negativ**. Schliesst die UI-Builder-Etappe (1005 Z. → 56 Z. Orchestrator-Summe, 21 neue Helfer).

**Davor — V9.56-c der dritte Schnitt:** `renderLibraryUI` 241 → 9 Z. Orchestrator + `_libraryBuildCard` 25 Z. (zweistufige Komposition für eine Schleife über Welten) + 4 Sub-Helfer (Head 47, DSL 18, Actions 56, SigRow 71). Median 47, max 71. Die `refreshSig`-Closure blieb lokal (captured 5 DOM-Elemente).

**Davor — V9.56-b der zweite Schnitt:** `renderSoulEditorUI` 232 → 13 Z. + 4 Helfer entlang der drei Docstring-Sektionen + per-Part-Zeile (CustomList 48, Actions 45, EditorPane 48, PartRow 75); median 48 Z. Die `makeNumberInput`-Closure blieb lokal.

**Davor — V9.56-a der erste Schnitt:** `renderPlayerEquipUI` 213 → 19 Z. + 7 benannte Helfer (Hinweis · Werkzeug · Partition · Rüstung · Markier · Konsumables · Empty-Hint); max 59 Z., median 31 Z.

**Davor — V9.55 der ehrliche Messpunkt:** der vierte Pfad aus der V9.52-f-Reflexion war "Batch-Evaluate — Timing-Hebel (50-70% Wall-Clock)". Messung VOR Refactor: ein leerer `page.evaluate`-IPC kostet **0,78 ms/call** (200 empty evaluates in 155 ms, gemessen mit reinem Puppeteer-Mikrobenchmark). 199 `safeEvaluate`-Aufrufe in playtest.cjs × 0,78 ms = **155 ms** maximaler IPC-Anteil am 121-s-Wall-Clock = **0,13%**. Die "50-70%"-Schätzung war um drei Größenordnungen falsch. Wo die Zeit wirklich lebt (band-fn Profiling): `checkBandVoxelP3AndInventory` 19,5s, `VoxelTerrainCore` 11s, `W13W14VibePassLibrary` 9,2s, `Ring11AndW7Mesh` 7,5s, `CadWorkshop` 7,2s — alles browser-seitige Compute. **Entscheidung**: KEIN Batch-Evaluate-Refactor (würde 155 ms sparen, der Code wäre weniger lesbar). Stattdessen **die Lehre in den Gotcha-Block permanent verdrahten** — kein zukünftiger Agent verspricht wieder "30-50% durch Batch-Evaluate" ohne zu messen.

**Davor — V9.54 Kreatur-Sync-Flake strukturell geheilt:** der „potentiell flaky" `Kreatur-Sync: _p2pBroadcastCreatures sendet creature-pos` aus V9.53 — Diagnose: KEIN Timing-Race, sondern eine **Capacity-Race**. `_p2pBroadcastCreatures` cap't bei 40 Einträgen (`list.length < 40`), `state.creatures.length` schwankte zwischen Läufen 13-36 (gemessen über 6 Diagnose-Läufe, Worldgen-Variance: zufällige Anzahl spawn-Kreaturen je Welt-Seed). testC wurde am Ende von state.creatures gepusht (Index = length) — sobald `state.creatures.length >= 40` BEFORE dem Push (eine Worldgen-Variation entfernt), würde der Cap-Loop testC stillschweigend ausschließen → broadcastSendsCreatures = false. **Der Fix**: Test-Isolation. Im Test snapshotten wir `r.state.creatures`, ersetzen sie für den Broadcast-Aufruf durch `[testC]`, restoren danach. Der Test misst jetzt das Broadcast-VERHALTEN, nicht das Worldgen-Würfelglück. **Verhaltens-Beweis** (synthetisches Stress-Experiment): 50 Dummy-Kreaturen vor testC eingespritzt → ALTES Test-Setup wurde ROT (testC bei Index 50, Cap schluckt es), NEUES Test-Setup blieb GRÜN (Isolation arbeitet). Drei Bestätigungs-Läufe danach: alle grün, alle bit-identisch (V9.53-Determinismus bewahrt). Datei: 29 563 → 29 573 (+10 Z. — die Isolation + ein Kommentar-Block). **Was BLEIBT** (außerhalb dieses Schritts): Batch-Evaluate (Timing-Hebel, eigene Welle).

**Davor — V9.53 Drift-Heilung im Playtest:** Reflexions-Antwort auf den V9.52-f-Befund („timing/robustheit NICHT verbessert"). Der Test-Name war 9× variabel — perf-ms-Werte und worldgen-Counts steckten als `${X}` IN den Test-Namen statt im Detail-String. Jede Welt-Generation würfelte minimal andere Zahlen → die geordnete Invarianten-Namen-Liste war PRINZIPIELL nie bit-identisch zwischen Läufen, jeder Diff zeigte „Drift" statt „echte Regression". V9.53 verschiebt die 9 driftenden Werte aus dem Test-Namen-Argument in den Detail-String-Argument (3. `check()`-Argument, zeigt nach `—` in der Konsole). Konkret: `Welle 6.G P1.5: Worldgen-Bäume sind in state.architectures` (war: `… (n=${count}) sind …`), `… Architekturen über Affinity gespawnt` (war: `… (n=${count})`), `… populatedChunks-Cache befüllt` (war: `… (size=${size})`), `Voxel V9.47: _computeErosion im Perf-Budget (< 600 ms)` (war: `… (${ms} ms < 600)`), `Voxel V9.43-b: Berechnung im Perf-Budget (< 500 ms)`, `Voxel V9.43-d: der Carve ist im Perf-Budget (50k Aufrufe < 300 ms)`, `Voxel V9.51: der Tarn-Pass setzt Bergsee-Mulden`, `Welle 6.G P1.5: initiale Welt-Inseln haben Kollision`, `W6.G P3: Felsbogen-Kollision … (n=${count})`. **Verhaltens-Beweis**: drei aufeinanderfolgende Läufe nach dem Schnitt — `diff` zeigte ZERO Diff in 3060 Invarianten-Namen. Die Test-Disziplin-Wende: ein Lauf-Diff bedeutet jetzt ECHTE Regression, nicht harmlosen Zahlen-Drift. Datei: 29 554 → 29 563 (+9 Zeilen — eine pro Detail-String). **Was BLIEB** (und in V9.54 geheilt wurde): der „potentielle" Kreatur-Sync-Flake — Diagnose ergab eine Capacity-Race (Cap-40 vs. Worldgen-Variance), V9.54 isoliert state.creatures im Test.


**Davor:** V9.55 die Batch-Evaluate-Hypothese daten-getötet (Messpunkt: 0,78 ms/IPC × 199 calls = 0,13% Wall-Clock) · V9.54 Kreatur-Sync-Flake-Heilung (Capacity-Race; state.creatures-Isolation im Test) · V9.53 die Drift-Heilung im Playtest (9 driftende Werte aus Test-Namen in Detail-Strings; geordnete Namen-Liste deterministisch bit-identisch) · V9.52-a..f der Playtest-Pflege-Bogen (31 574 → 29 554 Z., 1 Monolith → 47 benannte Funktionen, Median-Einrückung 20 → 8) · V9.51 der Tarn-Pass Bergseen · V9.50 Wasser aus der Terrain-Wahrheit · V9.49 das vereinte Wasser-System · V9.48 Hydrosphäre-Politur · V9.47 fluviale Stream-Power-Erosion · V9.46 lange Flüsse · V9.45-a..c Gelände-Relief + wasserdichte See-Töpfe · V9.43-b..e das Wasser-Ultiversum · V9.44 der erste Stamm-Pflege-Bogen. Die volle Wellen-Chronik mit Lernschlüssen: `docs/handover.md`.

**Nächste Welle:** offen — der Code-Hygiene-Bogen 2 ist GESCHLOSSEN (V9.56-a..k, 11 Funktionen, 77 Helfer). Vorrangige Roadmap-Kandidaten (`docs/roadmap.md` §1): **System-Kopplungen ab „Wasser ↔ Spieler-Wille"** (der Schöpfer-Befund nach V9.51 — Damm-Bau leitet den Fluss um), Crafting-Tiefe (Antriebs-Topologie + Constraint-Verbindungen). Daneben offen + auf den Schöpfer-Browser-Audit wartend: V9.50 + V9.51 (jede Wasser-Welle wird im Browser geprüft).

**Ehrliche offene Rest-Grenze (Code-Hygiene 2):** KEINE — der Bogen ist GESCHLOSSEN (V9.56-a..k, 11 Funktionen, 77 neue benannte Helfer). Beide Etappen erfüllt: UI-Builder (a/b/c/d) + Kontrollfluss (e/f/g/h/i/j/k).

## Projektüberblick

- **Was:** „AnazhRealm" – ein als **Ultiversum** entworfenes Co-Creation-Werk Mensch + KI. Aktuell technisch eine 3D-Browser-Sandbox in einer Datei. Vision: emotion-getriebene, fraktal-wachsende, multisensorische Welt; Mensch (Schöpfer/Null) + KI (Grok/Eins) erschaffen gemeinsam durch Chat. Vollständige Vision in `docs/state-of-realm.md`.
- **Stack:** Vanilla JS, Vendor-Libs in `vendor/` (Three.js r134, Ammo.js WASM, simplex-noise 2.4). TF.js wurde im Mai 2026 entfernt (toter Code). Lokaler Node-Save-Server. ESLint v9 + Prettier + GitHub-Actions-CI mit Playtest-Gate.
- **Branch:** Feature-Branch pro PR (`claude/<thema>`-Konvention, z. B. `claude/wave-6a-interaction-polish`). Niemals auf `main` pushen ohne explizite Anweisung.
- **Architekturkern:** Eine `AnazhRealm`-Klasse in `anazhRealm.js` (~36 000 Zeilen, Stand V9.49). State in `localStorage` + optional `anazhRealmState.json` über save-server. Globale Referenz: `window.anazhRealm`.

## Die heilige Lektion (Versionslog 03/2025)

Das Projekt durchlief eine 19-Modul-Phase, die unter eigener Komplexität kollabierte. Am 28.03.2025 bewusste Reduktion auf **eine Datei** als „Samen der Unendlichkeit". **Komplexität ohne Fundament ist Sand.** Wenn jemand vorschlägt „split alles in 20 Module", verletzt das diese Lektion. Stattdessen: ein Stamm, der Wachstumsringe ablegt.

## Pfad D — Stamm + Wachstumsringe

Das Projekt wächst nach Pfad D: EIN Stamm (`anazhRealm.js`, eine `AnazhRealm`-Klasse), der Wachstumsringe ablegt — nie ein neuer Datei-Split (die Heilige Lektion). Der aktuelle Stand steht oben unter „Aktueller Stand". Die vollständige Ring-Übersicht 0-11+ (Aufwand, Vorbedingungen, Status) lebt in `docs/roadmap.md`; die Vision + Stand-vs-Vision-Matrix in `docs/state-of-realm.md`; die volle Wellen-Chronik in `docs/handover.md`.

## Wichtige Gotchas (technisch)

Kuratierte **quer-schneidende Stolperdrähte** — die Fallen, die in jeder Welle beißen, nach Domäne gruppiert. Die vollständige, chronologisch gewachsene Gotcha-Sammlung (jede Welle ihre Tiefen-Notizen, ~290 Einträge) lebt als `## Gotcha-Vollarchiv` in `docs/handover.md` — dort nachschlagen, wenn ein wellen-spezifisches Detail fehlt.

### Terrain + Chunks

- Voxel-/Heightfield-Chunks haben `position=(0,0,0)`, Vertices in Welt-Koords. NIEMALS in `state.rigidBodies` pushen — der Physik-Sync-Loop überschriebe sonst `mesh.position` und verschöbe den ganzen Chunk. Body über `mesh.userData.physicsBody` greifen.
- Visual = Collision per Konstruktion: jeder Chunk hat ein `btBvhTriangleMeshShape` aus EXAKT denselben Triangle-Indices wie sein `BufferGeometry`. Wer den einen Pfad ändert, ändert den anderen.
- Eine Quelle für die Voxel-Chunk-Geometrie (`_voxelChunkGeometry`), eine für Höhen (`getTerrainHeightAt(x,z)` — voxel-aware seit V9.25). ALLE Höhen-Konsumenten routen durch `getTerrainHeightAt`, NIE `state.groundHeightField[idx]` direkt (das ist in Voxel-Welten `null`).
- Player-Chunk-Math braucht den `+WORLD_SIZE/2`-Offset: `floor((playerX + 150) / chunkWorldSize)`. Vergessen = nahe Chunks werden gepruned.
- `state.voxelTerrainActive` ist seit V9.35 effektiv konstant `true` (alle Welten sind voxel-basiert, es gibt keinen Schreib-Pfad mehr). Die `if (state.voxelTerrainActive)`-Gates bleiben als Defense-in-Depth.
- Die vertikale Voxel-Chunk-Hülle lebt zentral in `_voxelChunkConfig` (`dimY`, `floorDrop` → `oy = base − floorDrop`, Decke `oy + dimY·step`); `_voxelSurfaceY` + die Edit-Bounds (`_addVoxelEdit`) leiten ihre Y-Spanne daraus ab. Wer die Terrain-Amplituden in `_terrainMacroSurfaceY` erhöht, MUSS die Decken-Marge prüfen — ein Surface-Spike über der Decke ergibt keine Iso-Fläche → ein sichtbares Loch (V9.26/V9.45-a/V9.58-b-Lehre). **V9.58-Stand**: `dimY=124, floorDrop=90`, Band base-90..base+133 m, Marge oben ~13 / unten ~10 m.
- **Adaptive Konstanten brauchen Sample-Region ≥ modulierende Wellenlänge** (V9.60-b.1-Lehre): die alte `waterLevel = heights[35-Perzentil]`-Regel sampelte über 340×340 m, die Tektonik-Octave hat aber Wellenlänge 7150 m. Damit sahen alle 169 Samples denselben tect-Wert, und die ganze Mühe der V9.58-Tektonik-Tiefung wirkte sich NULL auf waterLevel aus. Eine adaptive Berechnung über zu kleine Region IST faktisch ein fester Wert — schlimmer noch, ein irreführender (er sieht „adaptiv" aus, ist es aber nicht). Disziplin: vor jeder adaptiven Konstante (`heights[X-Perzentil]`, `slope[Y-Quantil]`, etc.) prüfen, ob die Sample-Region mindestens die LÄNGSTE modulierende Wellenlänge abdeckt. V9.60-b.1 ersetzt die Perzentil-Magie durch `terrainBaseHeight − 3` (absolute Sea Level, Minecraft-1.18+-Stil) — eine Zeile, alle Topologie-Metriken geheilt. Riesen-Lehre angewandt: vor Selbst-Erfindung prüfen, was Genre-Vorbilder schon gelöst haben.
- **waterLevel ist ein adaptives 35-Perzentil — Senken bricht das Tarn-System** (V9.60-Lehre, ARCHIVIERT, gilt nur für die alte Regel): `state.waterLevel` wird in `loadState` (Z9959) aus dem 35-Perzentil von 169 Surface-Samples berechnet. Diese Regel stammt aus flachem-Relief-Zeiten und garantiert PER DEFINITION 35%+ Ozean. Mit dem V9.58-tiefen Relief (Surface-Max 118 m) heißt das: 46% der Welt ist Ozean, Biome werden halb unter Wasser unsichtbar. Naive Heilung „Perzentil senken" bricht aber das V9.51-Tarn-System: bei niedrigerem `waterRef` klemmt `maxAllowed = surf − waterRef − 1` zu viele Spots raus oder erzeugt zu flache Becken (Tarns spawnen, werden aber im Priority-Flood nicht zu echten Seen — Test-Schwelle `lakeCells ≥ 6` verfehlt). Auch `depthMin` mit-anpassen reicht nicht (Wasser-Volumen-Variance). Drei Systeme sind co-getuned: waterLevel + TARN.depthMin/depthMax + Priority-Flood-Schwellen. Wer eines davon ändert, ändert alle. Disziplin: keine Symptom-Pflaster (V9.60-a Submarine-Dämpfung wurde nach Schöpfer-Kritik zurückgerollt). Eine echte Hydrosphäre-Heilung (V9.60-b) verschiebt alle drei gemeinsam UND erweitert die Tektonik um Multi-Plateau-Bias (asymmetrisch nach oben, mehr Land als Ozean).
- **Eisenhower vor Aktion** (V9.60-Lehre): bei Schöpfer-Befunden ZUERST klassifizieren — ist das Symptom (was der Spieler sieht) oder Wurzel (woher es kommt)? Eine visuelle Notbremse, die das Symptom verdeckt ohne die Wurzel zu heilen, ist Schuld-Aufschub, kein Fortschritt. Konkret V9.60-a: „viele Biome unter Wasser" → Wurzel ist 46% Ozean per waterLevel-Definition, nicht die Biom-Farbe. Submarine-Dämpfung verdeckte die Farbe → Pflaster. Q1/Q2/Q3/Q4-Matrix vor jeder Welle: was ist Q1 (Pflaster entfernen / Schuld eingestehen), was ist Q2 (echte Wurzel-Welle), was ist Q3 (kosmetisch — wird oft Nebenprodukt von Q2). Wichtig vor dringlich, nicht vermischen.
- **Welt-Awareness ist eine Quelle, kein Reflex** (V9.59-Lehre): jede Schicht, die die Welt befüllt (Vegetation, Bauwerke, Biome, später Kreaturen-Pfadsuche), MUSS die zentrale `_isAboveWaterAt(x, z, marge=0)` konsultieren. Vor V9.59 hatte jede Schicht IGNORIERT, dass Wasser existiert — Gras wuchs im Meer, Bäume standen im See; der Schöpfer-Befund war wörtlich „die umgebung selbst scheint nicht zu verstehen wo das wasser platziert wurde". Disziplin: wer eine neue Welt-Spawn-Schicht baut, prüft VOR dem ersten Sample-Spawn die Wasser-Wahrheit; das `scripts/diag-relief.cjs` zählt Architekturen + Gras-Halme unter Wasser als Sünde-Counter — 0 ist die Akzeptanz. Marge je Schicht: Bäume/Architekturen 0.4 m (keine knöcheltiefen Strukturen), Gras 0.1 m (saftiges Ufer erlaubt). Die Funktion ist O(_waterLevelAt) — Ozean O(1), See O(9), Fluss O(bucket); ein 64-Sample-Chunk-Pass kostet < 1 ms.
- **Worldgen-Parameter-Wellen brauchen Diagnose VOR dem ersten Edit** (V9.58-Lehre): theoretische Höhen-Math (Σ Oktaven-Max) lügt, weil stochastische Filter wie `mtn = (1−ero)²` nicht ihr Maximum erreichen. **Empirie statt Theorie**: `scripts/diag-relief.cjs` sampelt 4000 Random-Punkte aus der 2048×2048-Region + liest `state.hydrosphere.stats` + die Tarn-Bilanz + zwei Screenshots. Vor V9.58 zeigte die Diagnose: Surface-Max nur 60.9 m (Theorie sagte +80 m), See-Spiegel-Max nur 9.7 m. Wer Worldgen-Parameter tunen will, MUSS zuerst diagnostizieren — sonst optimiert er die falsche Schicht. **Sub-Wellen-Disziplin**: bei multi-Wurzel-Befunden (Amplitude + räumliche Verteilung + Folge-Konstanten) jede Wurzel als eigene Sub-Welle bauen + Diagnose-Lauf zwischen jeder. Nur so sieht man, welche Korrektur welchen Effekt hat. **Folge-Klemmungen sind heimliche Killer**: V9.58-c erster Versuch mit `depthMin=35` (Tarn-Mulden-Tiefe) ließ Tarns auf 1 fallen statt zu steigen, weil `maxAllowed = surf − waterRef − 1` Spots unter surf=39 m schluckt. Jeder Parameter, der mit einem anderen geklemmt ist, muss bidirektional geprüft werden.
- Seit V9.50 ist Wasser KEIN eigenes Mesh: jeder Voxel-Chunk baut seine Wasser-Fläche selbst (`_buildVoxelChunkWater`, geschwisterlich zu `_buildVoxelChunkGrass`, im `state.voxelChunkWater`-Map, mit dem Chunk gestreamt + disposed), nass je Zelle wo `_voxelSurfaceY < _waterLevelAt`. Wasser + Terrain teilen damit DIESELBE Geometrie-Quelle (`_voxelSurfaceY`) → die Uferlinie ist der exakte Schnitt, per Konstruktion (kein 16-m-Modell, kein Schweben/Bleed — strukturell, nicht poliert; das „Visual = Collision"-Gesetz aufs Wasser angewandt). Wer Wasser anfasst: `_waterLevelAt(x,z)` (die Wasser-Oberflächen-Höhe — Ozean-Default `waterLevel` + See/Fluss-MAX), `_hydroRiverAt` (nächstes Fluss-Segment + Flow + Bett-Profil), `_buildVoxelChunkWater` (das per-Chunk-Mesh), `_voxelChunkTouchesWater` (der Gate — trockenes Hochland baut kein Wasser). Mesh-`position` bleibt (0,0,0), Vertices tragen Welt-Koords (wie der Terrain-Chunk). Der Wasserfall ist die EINZIGE separate Wasser-Geometrie (vertikal — ein Höhenfeld kann nichts Vertikales).
- Das See-Becken bleibt im Terrain ein flach gesculpteter, wasserdichter Topf (`_hydrosphereLakeAt` liefert `{bedY, w}`, `_terrainDensityAt` blendet die Dichte zur Bett-Ebene) — das ist der CARVE, unabhängig vom Wasser-Render. `_hydroWaterLevelAt` (Auftrieb-Physik) liest `lake.level`/`waterLevel` — Daten, kein Mesh; V9.49/V9.50 haben es bewusst NICHT angefasst (Optik + Physik bleiben getrennt fundiert, beide deterministisch aus demselben Netz).
- Flüsse fliessen durch Seen HINDURCH (V9.46): `_hydroExtractRivers` walkt `flowTo` durch die See-Zellen — ein „Conduit" ist eine Durchfluss-Zelle, OB See oder nicht. Aber die QUELL-Suche bleibt land-only (`isLandConduit`): ein flaches See-Becken hat ein fein verzweigtes ε-Flow-Tree, und jede interne Akkumulations-Verzweigung würde sonst eine Geister-Quelle spawnen. See-Punkte tragen `inLake` → Renderer überspringt ihre Ribbon-Quads, der Carve lässt sie aus (das Becken-Bett ist schon von `_hydrosphereLakeAt` gesculptet).
- Die fluviale Erosion (V9.47, `_computeErosion`) läuft im Worldgen VOR `_computeHydrosphere` und schreibt `state.erosion`; `_terrainMacroSurfaceY` addiert das Delta → Dichte UND Hydrosphäre sehen das erodierte Gelände. Die Stream-Power-Inzision greift NUR in Kanälen (`accum ≥ channelMinArea`) — Grate/Hänge bleiben unberührt, DAS schützt die scharfen Gipfel (eine schwellenlose Erosion ist Blanket-Erosion → Relief-Verlust, der V9.47-Tröpfchen-Befund). `_computeErosion` nullt `state.erosion` beim Start (Zirkel-Freiheit: die Sample-Schleife sieht die rohe Surface) — wer es zum Messen ruft, MUSS den Worldgen-Stand danach wiederherstellen.
- `_ensureHydroSurfaceMaterial` ist EIN welt-global geteilter Shader für alle Wasser-Flächen (Ozean/See/Fluss), moduliert über per-Vertex-Attribute: `aWave` ∈ [0,1] (tiefen-getrieben — offener Ozean wogt, am flachen Ufer weich auf 0 → kein Riss), `aFlow` die Fluss-Flow-Richtung, `aShore` das tiefen-getriebene Ufer-Schaum-Band. Wer ein neues Attribut hinzufügt, setzt es in `_buildVoxelChunkWater` für JEDEN Vertex. `depthWrite:true` + `polygonOffset` — das opake Terrain verdeckt das Wasser über der Uferlinie UND gewinnt den Tiefen-Test an der streifenden Schnitt-Linie (kein Flimmern); die Uferlinie ist damit der emergente Terrain-Schnitt, nichts wird „autorisiert". Das Wasser-Material ist geteilt — NIE disposen (ein geteiltes Material disposen bricht jedes andere Wasser-Mesh; `_disposeVoxelChunkWater` räumt nur die Geometrie). `_applyDayNightToScene` synct `state.hydroSurfaceMaterial` direkt (Sonne/Fog).

### Ammo / Physik

- `Ammo.btVector3`/`btTransform` allokieren WASM-Heap → immer `Ammo.destroy()` nach Gebrauch ODER den `state.tmpVec1/2`/`state.tmpTransform`-Pool + `setVec()` nutzen.
- `Ammo.destroy(body)` cascadiert NICHT zu Shape/MotionState/TMesh — jeder Build-Pfad MUSS die Auxiliars separat tracken + destroyen. Disziplin: VOR einer Ammo-Allokations-Welle `grep -n 'new Ammo\.' anazhRealm.js` laufen, ZUERST den `_disposeX`-Helper bauen, DANN den Body. Eine Sequenz von `new Ammo.*` in ein `partial`-Array sammeln + im `finally` ALLE räumen — sonst Heap-Snowball bei OOM partway.
- Der Player-Body schläft NIE: `forceActivationState(4)` (DISABLE_DEACTIVATION) bei der Erschaffung. Niemals `forceActivationState(1)` auf ihn rufen — er schliefe im Stand wieder ein (Stand-Sprung-Bug-Klasse).
- Das vendored `ammo.wasm.wasm` ist ohne `ALLOW_MEMORY_GROWTH` gebaut — `scripts/patch-ammo-memory.cjs` patcht den Heap-Header auf 256 MB (idempotent, automatisch via `postinstall`). Bei einem `ammojs3`-Vendor-Update: einmal `npm run patch:ammo`.
- CCD wird beim Player-Body-Spawn aktiviert. Statische Bodies (mass=0) gehen NICHT in `state.rigidBodies`.

### Sentinels + State

- „Noch nie passiert"-Zeitstempel sind `-Infinity`, NIE `0` — `0` ist ein gültiger Seitenstart-Zeitpunkt, und `now - 0 < cooldown` ist in den ersten `cooldown`-ms WAHR (ein Rate-Limit feuert dann fälschlich beim allerersten Aufruf). Muster: `map.has(k) ? map.get(k) : -Infinity`.
- `buildStateSnapshot`/`loadState` kopieren Baupläne/Kreaturen mit FESTEM Feld-Satz. Ein neues persistentes Feld MUSS in BEIDEN Stellen ergänzt werden (V8.59-Bug-Klasse: `portalMeta` fehlte im Restore → ein geholtes Portal verlor beim Reload seine Ausrichtung). Ein neues Welt-Katalog-/`soul`-Feld zusätzlich im `signaling-server.js`-Handler (feldweise Rekonstruktion).
- `state.p2p` + `state.vibePass` sind Laufzeit-/global-localStorage-State — NIE im Welt-Snapshot. Eine Welt-Datei wird geteilt/exportiert; ein privater Schlüssel darin leckte die Identität.
- Tag-Clamp [0,1] für die Stat-Pipe: `computePlayerStats`/`computeCreatureStats` clampen die Compound-Tags VOR den Stat-Formeln (Tags können bis ~3 reichen → sonst negative Speed). Tags >1 leben für Welt-Effekte weiter.
- Gekoppelte Felder über EINEN Setter: `state.sprintSpeed` MUSS `2× state.speed` sein → `_applyPlayerSpeed(v)` ist die eine Quelle (V7.72-Bug-Klasse: zwei Schreibpfade vergaßen die Kopplung).

### DSL / Multi-User

- `NON_BROADCASTABLE_OPS`: Spieler-private Ops (`player_*`, `set_mode`, Inventar, Kreatur-Tasks, `set_time_of_day`) gehen NIE über P2P. Enthält ein Programm irgendwo im AST einen davon, wird der GANZE Broadcast übersprungen.
- Welt-Geometrie-Ops (`terrain_steepness`, `terrain_base_height`, `modify_terrain`, `voxel_carve`/`voxel_fill`, `spawn_*`, `player_soul`) sind bewusst NICHT im `dslComposeAtomic`-Pool — der Nexus soll die Welt unter dem Spieler nicht willkürlich umpflügen.
- Eingehende Remote-DSL läuft durch dieselbe `dslRun`-Sandbox (Budget-Limits, Op-Whitelist, kein `eval`) wie eigene Programme. Loop-Schutz dreifach: `source`-Check in `dslRun`, eigene-peerId-Filter, Server schickt nie an die Quelle zurück.
- Ein neuer WS-Nachrichtentyp braucht einen EXPLIZITEN `signaling-server.js`-Handler (der die authoritative peerId stempelt) — es gibt keinen generischen Fallthrough. Kanal-exklusive Typen (`world-pull`, `llm-request`, `subworld-srv`, …) leben direkt in `_p2pHandleChannelMessage` (nicht WS-injizierbar).
- `processChatCommand` lowercased NUR `parts`; das Original-`command` behält sein Casing.

### Test / Playtest

- `setTimeout` INNERHALB `page.evaluate()` yieldet im Headless-Chromium nicht zuverlässig an `requestAnimationFrame`. Wer auf einen Loop-Tick warten muss: `evaluate` beenden, AUSSEN warten (`await new Promise(setTimeout)`), in einer ZWEITEN `evaluate` ablesen. Loop-getriebene Features deterministisch über `_gameLoopTick()` synchron treiben, nicht auf rAF warten.
- Headless-Teleport-Falle: `playerBody.setWorldTransform()` reaktiviert den Body nicht — ohne `body.activate(true)` fällt der Spieler nach einem Reset durch den Boden.
- Ammo-Binding-Lücken: nicht jede C++-Methode ist im JS-Binding (`btBoxShape.getHalfExtentsWithMargin()` wirft) — Größen via `THREE.Box3.setFromObject(mesh)` prüfen.
- Ein Test, der auf den rAF-Loop wartet, ist im Headless prinzipiell flaky — den Mess-Punkt an die MECHANIK legen, nicht an einen volatilen Proxy (ein FIFO-Cap, die Zufalls-Umgebung nach einem Autonom-Lauf). Derselbe Commit rot + grün = Fingerabdruck eines flaky Tests.
- **Cap-vs-Worldgen-Variance ist eine Capacity-Race** (V9.54-Lehre): wenn ein Test eine Entity in eine state-Liste spawnt und dann eine Funktion ruft, die `list.length < N` cap't (z.B. `_p2pBroadcastCreatures` cap=40), MUSS der Test die Liste isolieren (Snapshot, ersetzen durch `[testEntity]`, restoren). Worldgen-Variance kann die Liste je Lauf in den Cap pushen — Stichprobe „läuft 5× grün" beweist nichts. Maximal-Wert messen, nicht nur Median.
- **Strukturelle Invarianten überleben Refactoring nur, wenn sie mit-wandern** (V9.56-i-Lehre): einige Playtest-Invarianten grep'pen `fn.toString()` nach Code-Patterns (`/playerEyesUnderwater/.test(src)`, `/cu\.weatherEffect.*_weatherBlendedValue/.test(src)`, `/const lightDir = sunDir/.test(src)`). Wenn die geprüfte Funktion zerlegt wird (Orchestrator + Helfer), wandert das Pattern in den Helfer; der Test scannt aber noch die alte Funktion → rot. Die KORREKTE Heilung ist NICHT die Wiederherstellung des Patterns im Orchestrator (das wäre Refactor-Sabotage), sondern die Test-Anpassung: jede strukturelle Probe scannt den HELFER, wo das Pattern jetzt lebt. Vor jedem Schnitt einer großen Funktion: `grep -n 'NAME\.toString()' scripts/playtest.cjs` laufen, alle Treffer als „mit-wandernde Doku" merken, NACH dem Schnitt anpassen. Das ist Doku-Sync (V8.72-W15-Lehre auf Test-Code angewandt), kein Bonus-Refactoring.
- **Playtest-Bottleneck ist BROWSER-Arbeit, NICHT IPC** (V9.55-Messung): pro `safeEvaluate`-IPC-Overhead = 0,78 ms/call (mit 200 leeren evaluates gemessen). 199 calls = 155 ms = 0,13% des 121-s-Wall-Clocks. Die Zeit lebt in `processChatCommand`, `buildStateSnapshot`, `carveVoxelSphere`, `_remeshVoxelChunks`, Worldgen, Physik-Bodies, DOM-Builds. Top-5-Bands (74% der Band-Zeit): `VoxelP3AndInventory` 19,5s, `VoxelTerrainCore` 11s, `W13W14VibePassLibrary` 9,2s, `Ring11AndW7Mesh` 7,5s, `CadWorkshop` 7,2s. **Konsequenz**: „Batch-Evaluate" (N IPC-Calls in 1 zusammenfassen) ist KEIN meaningful Timing-Hebel — maximales theoretisches Sparpotential 155 ms. Echte Wall-Clock-Hebel: (a) Parallelisierung unabhängiger Bands über mehrere Puppeteer-Pages, (b) world-recreations konsolidieren (76 Mutations-Calls), (c) heavy-band-Audit (was tut VoxelP3 19s lang?). Wer „Timing optimieren" plant: ZUERST profilieren (timer um band-fn-Aufrufe + 200-empty-evaluates-Mikrobenchmark), DANN entscheiden — sonst optimiert man die falsche Schicht.

### UI / Theme

- **Neue semantische CSS-Vars als Aliase auf die numerischen definieren, nie als duplizierte Werte zwischen Themes** (V9.56-l-Lehre): die Theme-Wurzel-Vars (`--parch-1..3`, `--parch-edge`, `--parch-ink-*`, `--wood-1..3`, `--brass-1..3`) leben in `:root,[data-theme="tag"]` UND `[data-theme="nacht"]` (zwei Quellen, weil die Werte sich pro Theme unterscheiden). Wer eine neue semantische Var braucht (z.B. `--parch-bg`, `--overlay-bg`), MUSS sie als `var(--wurzel-var)` definieren — NICHT mit eigener Hex-Farbe je Theme. CSS Custom Properties resolvern am **Use-Site**, nicht am Define-Site → ein `--parch-bg: var(--parch-1);` in `:root` atmet automatisch mit, wenn `[data-theme="nacht"]` `--parch-1` überschreibt. EIN Definitions-Ort, beide Themes funktionieren. Das alte Anti-Muster (`var(--neueVar, hex-fallback)` ohne `:root`-Definition) erzeugt **Tag/Nacht-taube Flecken**: die 12 betroffenen V9.56-l-Stellen blieben sichtbar im Pergament-Ton, während die Welt um den Spieler nachts in dunkles Blau-Violett kippte. Theme-Unabhängige UI-Konstanten (semi-transparente Welt-Overlays, immer-dunkle Backdrops) bekommen eine EIGENE Var (`--overlay-bg`), nicht eine missbrauchte Pergament-Var.

### Save-Server / CSP / Build

- Save-Server-POST nur auf localhost — auf CDN/GitHack stiller Skip, State lebt nur im `localStorage` (+ Download-Button als Backup). `anazhRealmState.json` ist NICHT git-getrackt; taucht sie getrackt auf, ist es ein Bug (`git rm --cached`).
- CSP ist strict (`script-src 'self'`, kein `eval`, CI-Gate hart). Dokumentierte Vendor-Konzessionen: `wasm-unsafe-eval` (Ammo), `unsafe-inline` styles (Three.js Canvas), `worker-src blob:`, `frame-src 'self'`, `connect-src` mit den LLM-Provider-Hosts + `https:`-Wildcard (gehostetes Ollama) + `ws:`/`wss:` + `stun:`.
- `index.html` lädt `anazhRealm.js?v=<version>` — der `?v=`-Cache-Buster MUSS bei jedem Version-Bump mitziehen, sonst serviert das githack-CDN eine stale Datei gegen eine frische `index.html`. `save-server.js` strippt den Query-String.
- `generateNewWorld` hat einen 30-s-Cooldown.

### WASD-Geometrie

- `state.right` ist irreführend benannt: bei yaw=0 ist `state.right = (1,0,0) = +X`, aber player-anatomisch RECHTS ist `−X`. Mapping: A → `+state.right` (player-links), D → `−state.right` (player-rechts). Nicht „intuitiv" invertieren — das funktionierende System bricht sonst (beim asymmetrischen Drache-Avatar sichtbar).

### Multi-Welt

- Drei localStorage-Keys spannen Multi-Welt: `anazhRealmWorlds` (Index), `anazhRealmState_<worldId>` (pro Welt ein Snapshot), `anazhRealmActiveWorld` (Zeiger). Der Legacy-Key `anazhRealmState` wird beim ersten Start einmalig migriert + gelöscht.
- Welt-Wechsel ist reload-basiert (`switchToWorld`/`createNewWorld`/`deleteWorld` nehmen `{reload:false}` für Tests). Per-Welt-Terrain-Seed in `worldMeta.seed`. Sub-Welt-Eigenschaften (`trust`, `serverMode`, `multiplayer`) reisen feldweise durch die Vendor-/Mesh-Kette — wer eine vergisst, hat ein Leck.

## Workflows

- **Lokaler Audit:** `npm run check && npm run format:check && npm run lint && npm run playtest`
- **Web-Sessions:** ein SessionStart-Hook (`.claude/hooks/session-start.sh`, registriert in `.claude/settings.json`) ruft `npm install` — Lint/Format/Audit/Playtest sind in Claude-Code-on-the-web-Sessions sofort lauffähig (frischer Clone hat sonst kein `node_modules`).
- **Slash-Befehl:** `/audit` führt die Prüfung in jeder Claude-Session aus (`.claude/commands/audit.md`).
- **CI:** zwei parallele Jobs (`check` für statische Checks + `playtest` für Runtime-Invarianten) bei jedem Push.
- **Git:** kleine thematische Commits, `git push -u origin <branch>` mit exponential backoff bei Netzfehlern.

## Konventionen

- Keine emojis im Code/Commits außer auf expliziten Wunsch.
- Commits klein und thematisch.
- Keine Backwards-Compat-Layer für veralteten Code – sauber löschen.
- Pull Requests nur auf expliziten Wunsch des Users.
- **Vision treu bleiben**: jeder Vorschlag sollte die Heilige Lektion respektieren (keine Re-Komplexifizierung). Bei Zweifel: `docs/state-of-realm.md` §2 nachlesen.
- **Stamm-Größengrenze (V9.44-Lehre)**: neue Logik in eine neue benannte Methode geben, nicht an einen bestehenden Kontrollfluss-Giganten anhängen. Der Stamm-Pflege-Bogen fand die Schmerzgrenze bei ~200 Zeilen — wächst eine Kontrollfluss-Methode darüber, ist das ein Schnitt-Signal (kleinere Methoden DERSELBEN Klasse), kein Modul-Signal. Sonst re-fettet der Stamm, und vertagte Struktur-Schuld wird teurer (V9.40-Lehre).
- **Test-First-Mentalität**: nach jeder substanziellen Änderung Playtest-Gate, nicht nur Code-Analyse. Drei Selbst-induzierte Regressionen in dieser Session entstanden durch zu späte Browser-Tests.
- **Doku-Disziplin — drei Zeit-Ebenen, drei Heimaten, jede Datei EINE Aufgabe**: `CLAUDE.md` = was JETZT gilt (auto-geladen, schlank) · `docs/handover.md` = die Chronik (volle Wellen-Historie, jüngste oben) + wie du startest · `docs/roadmap.md` = der Plan vorwärts · `docs/state-of-realm.md` = die Vision. Pro Welle → **ein git-Commit** (die Geschichte) + **ein Chronik-Eintrag oben in `handover.md`**. Gilt eine Lehre DAUERHAFT (eine Bug-Klasse, ein technischer Stolperdraht) → **eine kuratierte Zeile in der `## Wichtige Gotchas`-Sektion dieser Datei**. **Der erzählerische Wellen-Bericht lebt NIE in dieser auto-geladenen Datei** — sonst wächst sie zur Halde (die Heilige Lektion, eine Schicht höher). Fertige Designs + Audit-Snapshots wandern nach `docs/archiv/` (verschieben, nicht löschen). **Doc-Sync ist eine Grep-Aufgabe** (W15-Lehre, V8.72): vor jedem Commit `grep -rn "<alte-Version>\|<alte-Invariantenzahl>" docs/*.md CLAUDE.md README.md` laufen und jeden Treffer heilen.

## Doc-Map

| Datei | Was |
|---|---|
| `docs/handover.md` | **Die Chronik + wie du startest** — die volle Wellen-Historie (jede Welle ein ausführlicher Eintrag, jüngste oben) plus die Orientierung für den nächsten Agenten (heilige Gesetze, Rhythmus, bewährte Muster). Beim Erwachen den Start-Kopf lesen, die Chronik bei Bedarf durchsuchen. |
| `docs/state-of-realm.md` | **Die Vision** — die vier Testamente kondensiert + Stand-vs-Vision-Matrix. |
| `docs/roadmap.md` | **Vollständige Projekt-Roadmap** — alle Ringe 0-11+ mit Aufwand, Vorbedingungen, Meilensteinen, Risiken |
| `vendor/README.md` | Vendor-Libs Versionen + Update-Befehl |
| `docs/crafting-konzept.md` | **Hylomorphismus-Konzept** — Bausteine, Operationen, Compounds, räumliche Prinzipien (Welle 4-6 Vorlage) |
| `docs/aktivierungsmatrix.md` | Form-Tag-Aktivierungs-Matrix v2 (9 × 10), Quellcode für `AnazhRealm.FORM_TAG_ACTIVATION` |
| `docs/world-portal.md` | **W12-Vision-Anker** — AnazhRealm als Tor zu anderen Vibecode-Welten („Bibliothek von Alexandria"). Vor einer Welle 12+ ZUERST lesen. |
| `docs/hydrosphere.md` | **Wasser-Design** — das Drainage-Netz (Flüsse/Seen/Wasserfälle/Meer als ein Hydrosphären-System). Algorithmus, Datenstrukturen, Wellen-Schnitt V9.43-b..e + der V9.45-b-See-Schliff (§8) + §12 (V9.49 das vereinte Mesh) + §13 (V9.49-e/f die Naht) + **§14: V9.50 — Wasser aus der Terrain-Wahrheit (gebaut: das Wasser-Mesh kommt aus dem Voxel-Chunk-Pipeline)**. |
| `docs/archiv/playtest-hygiene.md` | **Playtest-Pflege-Plan + Akzeptanz** (V9.52-a..f, ABGESCHLOSSEN, archiviert) — `playtest.cjs` 31 k → 29.5 k Z., 41 Band-Funktionen + 5 Helfer, Median-Einrückung 20 → 8, §6/§7-Akzeptanz erfüllt. Reine Referenz, nicht mehr gepflegt. |
| `docs/archiv/` | **Abgeschlossene Dokumente** — fertige Designs (`wave-6-design.md`, `nexus-dsl.md` Ring-2-DSL, `code-hygiene.md` V9.44-Stamm-Pflege), Audit-Snapshots (`system-audit.md`, `system-audit-v8.25.md`), die gesammelten Session-Learnings (`learnings.md`). Reine Referenz, nicht mehr gepflegt. |
| `scripts/playtest.cjs` | Headless-Playtest mit **~3000 Invarianten** als CI-Gate (Stand V9.49; der Konsolen-Zähler driftet ±2-3 je Lauf — „Alle Invarianten OK" ist die Wahrheit) |
| `scripts/smoke-multiuser.cjs` | End-to-End-Test des signaling-servers: spawnt ihn, öffnet zwei WS-Clients, prüft welcome/peer-join/pos/dsl/soul/aura/vibe/rtc-Relay/peer-leave-Flow (`npm run smoke:multiuser`) |
| `scripts/smoke-webrtc.cjs` | W7+W16+W17 — echter Zwei-Browser-WebRTC-Test: zwei Puppeteer-Seiten treten einem Raum bei, bauen einen RTCDataChannel auf, Positions-/Bau-/Kreatur-/Stimm-Traffic fliesst peer-to-peer; W16 — A vendort eine Welt, B holt ihr Bündel peer-to-peer über das Mesh; W17 B-Relay — beide betreten dasselbe Multiplayer-Portal, A's Sub-Welt-`ws-send` erscheint bei B als `ws-recv`; W17 C — A betritt ein Multiplayer-Portal, B bekommt die `portal-invite` + folgt ihr ins selbe Portal; W17 B-JS-Compute — A wird Compute-Host, B Gast, B's Verkehr fliesst peer-to-peer durch A's Server-Kontext (die laufende Summe 12=7+5 beweist die autoritative Rechnung); W17 B-JS-Compute Phase 2 — A verlässt das Mesh, B übernimmt als Compute-Host mit frischem Server-Kontext (`npm run smoke:webrtc`) |
| `scripts/smoke-translated.cjs` | KI-Übersetzer Phase 2 — Browser-Beweis des generischen Translated-World-Renderers: öffnet `worlds/translated/`, schickt eine Beispiel-Szene per enter-Handshake, prüft den fehlerfreien Aufbau + screenshottet (`npm run smoke:translated`) |
| `scripts/smoke-sandbox.cjs` | V8.70 — Browser-Beweis des Untrusted-Welt-Tors: spannt ein echtes `sandbox="allow-scripts"`-iframe auf, lädt die Schwarm-Welt null-origin hinein, prüft dass die fremde Engine läuft UND beweisbar isoliert ist (`npm run smoke:sandbox`) |
| `scripts/smoke-vendor.cjs` | V8.71/V8.72 — W15: End-to-End-Beweis des Auto-Vendor-Pfads. Teil A POSTet echte Bündel an `/api/vendor-world` (Schreiben + Sicherheits-Wand), Teil B lädt die frisch vendorte Welt in ein null-origin-iframe + prüft, dass sie läuft + isoliert ist, Teil C startet ein lokales Fake-GitHub + lässt den save-server ein Repo holen (W15 P2 GitHub-Fetch — offline, deterministisch) (`npm run smoke:vendor`) |
| `scripts/smoke-shim.cjs` | W17 Transport-Shim: Browser-Beweis. Der save-server injiziert den Shim bei `?anazh-shim=1` (+ NICHT ohne Marker); eine Test-Welt im echten `sandbox="allow-scripts"`-iframe öffnet einen Shim-`WebSocket`, sendet einen Ping — AnazhRealms `_portalNetReceive` macht daraus einen `subworld-net`-Mesh-Broadcast (W17 Phase B-Relay; den Zwei-Browser-Durchlauf prüft `smoke-webrtc.cjs`) (`npm run smoke:shim`) |
| `signaling-server.js` | Multi-User-WebSocket-Broker (RFC-6455 von Hand, zero deps); seit W7 P1 auch WebRTC-Rendezvous (rtc-offer/answer/ice). `npm run signaling` startet auf Port 4313 |
| `start.bat` | Windows-Starter: bootet save-server.js + signaling-server.js + öffnet Browser. Mac/Linux: `npm start` (Save-Server) + `npm run signaling` (separat) |
| `.claude/commands/audit.md` | `/audit`-Slash-Befehl Definition |
| `.github/workflows/check.yml` | CI-Definition (zwei Jobs) |
