# AnazhRealm – Projektgedächtnis für Claude

Diese Datei wird bei jeder Session automatisch geladen. Sie trägt **nur, was JETZT gilt** — den aktuellen Stand, die technischen Gotchas, die Konventionen. Drei getrennte Heimaten für die drei Zeit-Ebenen des Wissens:

- **JETZT** (diese Datei) — was gilt: Stand, Gotchas, Konventionen, Doc-Map.
- **DIE CHRONIK** (`docs/archiv/handover.md`) — die volle Wellen-Historie, jede Welle ein ausführlicher Eintrag, plus „wie du eine Session startest".
- **DER PLAN** (`docs/roadmap.md`) — was vorwärts kommt. Die Vision in `docs/state-of-realm.md`.
- **DER WAHRE NORDEN** (`docs/das-lebendige-feld.md`) — wohin das Projekt tief will: die Welt als EIN lebendiges Feld, das alle (Mensch · KI · Nexus · Kreaturen · die Welt selbst) **lesen · schreiben · WERTEN**. **Alle drei Verben stehen** (lesen V17.21 `auraAt` · schreiben V17.27/.32 `deposit` · WERTEN V17.42–.50 Vorhersagefehler-δ) — Emergenz statt Pflaster. Verankert die wahre Tiefe (gemessen im Code) + den Vektor vorwärts (Phase E · Fluid-Dynamik · Emotion→Regel-Emergenz · die KI als Co-Schöpferin). **Vor jeder Arbeit an „lebendiger Welt / Emotion / Nexus / DSL / Kreaturen" ZUERST lesen.**
- **KAMPF + WERKSTATT + Plan→Werk** (`docs/archiv/kampf-plan.md`) — der Hylomorphismus: das in der Hand gehaltene Ding bestimmt die Welt-Aktion + Effizienz (kein Rollen-Schloss, ein emergentes Fähigkeits-Profil), Abbauen kostet substanz-gebundene **Mühe**/**Ausdauer**. §11 GEBAUT (V17.59–.72): vier Mach-Akte durch EIN `_makeCostGate`+`fertigeBlueprint`, die Resonanz-Vereinheitlichung (Domäne/Rolle/Werkzeug-Op emergent aus Form×Material), die craftbare Bibliothek. Plan→Werk: der Bauplan ist freie Information, das WERK kostet Materie (auch der Avatar: Körper FORMEN kostet, fertige Seele TRAGEN frei). **Vor Arbeit an Kampf/Waffen/Werkzeug/Rüstung/Trank/Avatar/Abbauen/Werkstatt/Schmieden/Crafting/Bauplan-Teilen ZUERST lesen.** (Der AKTIVE Bogen steht unter „Aktueller Stand": wahrerguss/lebendiger-koerper.)

## Aktueller Stand

**WAS JETZT GILT (Stand V18.336): DER WAHRE GUSS — SUBSTANZ (System A) — DER BODEN HAT MIKRO-RELIEF.** Schöpfer „lasse die welt leben, das gras, den boden, die rinde, alles" + „ein gigant, volle Tiefe, alle ziele erreicht?". Nach V18.335 (Terrain-ROUGHNESS-Variation aus dem `_substanceCharacter`-Kern — Korn/Moos-matt/Wet-Gloss; eine korrekte POLITUR, aber KEINE Transformation: der kahle Boden blieb flach low-poly) zog der Schöpfer den Gigant-Maßstab + wählte den transformativen Hebel: **Mikro-Relief (Bump).** GEBAUT (V18.336): ein zwei-oktaviger Noise-Höhengradient (`TERRAIN_BUMP`) kippt die GEFLATTETE Terrain-Normale fein → der flache Low-Poly-Boden fängt Licht als echte Klumpen-/Korn-Struktur, LOD-gegated (nah voll, fern glatt → kein Aliasing/Schimmer). NORMAL-LESER-AUDIT (die Sicherheits-Wand vor dem Bau): der EINZIGE Leser dieser Normale ist die PBR-Lichtung — die Substanz-Response (`feld`-Profil `micro:0`/`rim:0`) liest die GEOMETRIE-`normalWorld` separat → kein Schatten-/AO-Konflikt. VERIFIZIERT (sauberer A/B, derselbe deterministische flache Wiesen-Spot, eigenes Auge): Bump AUS = flacher grüner Teppich · Bump AN = sichtbares Klumpen-/Mulden-Relief, tasteful (kein wavy-Artefakt am repräsentativen Spot; das frühere „wavy" war der unrepräsentative Hazy-Vista-Grazing-Winkel). **ZWEI KONTEXT-LEHREN auf dem Weg (Schöpfer-Korrekturen, beide Render-Fehldeutungen meinerseits):** (1) der Steil-nach-unten-Blick zeigte erst die SPAWN-PLATTFORM, nicht den Boden → Spieler auf echtes Terrain teleportieren; (2) `getTerrainHeightAt` liefert den SOLIDEN Boden (= Seeboden) → der Teleport landete UNTER WASSER → der Diag prüft jetzt `aboveWater`/`eyeAboveWater` + scannt einen flachen LAND-Spot über Wasser. Davor V18.335 (Roughness, CONSUM bewiesen) + V18.334 (der Substanz-Raptor: EINE Charakter-Quelle, Flach-Werke + Terrain + Rinde lesen sie). Fast-Gate 13/13 + check/format/lint grün; das CI-Merge-Gate (fast + GPU-Lens am ECHTEN Renderer) + die nächtliche volle Deckung verifizieren — der Bump fügt nur STATISCHE Nodes ans geteilte (EINMAL gebaute) Terrain-Material → kein Steady-State-Pipeline-Churn (die GPU-Lens-Klasse). Flach-Werke + Rinde UNBERÜHRT (Vegetation behält ihre echte Normale, der Bump-Zweig ist `vertexColors && !useFlexAttr` = nur Terrain). Der LOOK: Schöpfer-Browser (Regel #0, Wand 1) — A/B gezeigt. Detail in `docs/archiv/handover.md` (V18.336/.335/.334). **Davor — V18.331 VOLLENDETE DEN DETERMINISMUS-BOGEN — AMMO IST PHYSISCH RAUS, die Feld-Physik ist die EINZIGE Physik.** Schöpfer-Wort „es passt champ, vollende es" → der irreversible Schnitt (Entscheid #4) gezogen. Kollision/Bewegung/Raycast/Struktur-Kollision sind feld-nativ aus dem DICHTEFELD (`_stepCharacter` · `_fieldRaycast` · `_stepCharacterStructures` gegen `entry.blockerAABBs`); `physicsWorld`=null, `new Ammo`=0, kein ammo.js in `index.html`, kein 256-MB-WASM-Heap. **Der per-Chunk-BVH-Build — der Lauf-Freeze AN DER WURZEL — ist WEG, der Boden ist deterministisch.** Der Spieler hat keinen Body mehr (Velocity in `state.playerVel`, Vertikale in `_fieldVy`, Erdung in `_groundedCache`); Kreaturen erden body-frei via `_creatureGroundY`. Netto −969 Zeilen. Verifiziert: Fast-Gate 13/13 + `diag-walk-feel`/`-field-raycast`/`-structure-collide` grün (der Controller läuft/springt/landet/kollidiert/strahlt REIN feld-nativ); das volle Merge-Gate (mit den nachgezogenen Ammo-Bändern) läuft auf der CI. Voll-Stand + Disziplin in der `## Wichtige Gotchas`-Sektion (FELD-NATIVE KOLLISION) + `docs/archiv/eigene-physik-plan.md` (P0–P3 ✅). DANN VOLLENDET: das Sediment GESCHNITTEN (das ganze Ammo-Kollisions-Subsystem + tote Felder/Konstanten weg, CSP `wasm-unsafe-eval` gedroppt — dabei ein Reiter-Skip-Bug in `_stepCharacterStructures` gefischt), die zwei Gate-Last-Flakes gefischt (perf.d/LMB, gemessen), und **P4 STUFE 1 — DIE ERNTE — GEBAUT: deterministischer Input-Replay** (`replayRun`/`startReplayRecording`, Chat `replay record`/`replay stop`); drei Replays derselben Aufnahme sind bit-identisch (`diag-replay-determinism` + `checkBandV18331ReplayDeterminism`) → der Determinismus ist BEWIESEN, die self-suffiziente Verifikation steht. DANN das BACKLOG GERÄUMT (Schöpfer-Wahl „erst Backlog räumen, dann der wahre Guss"): die Dev-Drosseln revertiert (Planeten/Inseln 3, Bäume `SAMPLES` 10 — die volle V18.215-Dichte, der Cold-Start trägt sie nach dem Perf-/Worldgen-/BVH-Bogen, Fast-Gate 13/13) UND **Inseln in die Feld-Kollision GEBAUT (Entscheid #2 — die AABB-Hülle):** `spawnIslandAt` legt `island.userData.fieldAABB` aus der gebauten Geometrie an, `_stepCharacterIslands` löst die Spieler-Kapsel dagegen → der Spieler STEHT feld-nativ auf einer fliegenden Insel (Auflage auf der Oberkante) + wird an ihren Flanken geblockt; geteilte Mathe-Quelle `_resolveCapsuleVsAABB` mit den Bauwerken (Gesetz #0 — EINE Quelle, kein Parallelpfad), verifiziert mit einer Stand-Probe (Füße exakt auf `topY`, grounded). OFFEN: P4 Stufe 2/3 (Lockstep-MP + Fixed-Point) als eigener Folge-Bogen · die freie Wurf-Box-Schicht (Spezifikation). **Davor — V18.322 HEILTE DEN PERIODISCHEN IDLE-FREEZE — der Schöpfer-Befund „2 s Bild, dann 10 s Freeze, im Stehen, ohne Eingabe". WURZEL (GEMESSEN `diag-skyenv-freq` + `diag-skyenv-identity`, ECHTER Renderer): `_ensureSkyEnvironment` läuft pro Frame (über `_applyDayNightToScene`), und die Tag-Nacht-Uhr driftet die Himmelsfarbe AUCH IM STEHEN → die alte Drossel (Drift >0.04) ließ ~18 Neugenerierungen/min durch, jede (a) ein voller PMREM-Render UND (b) ein NEUES `scene.environment`-Texture-Objekt → WebGPU rekompilierte die Pipelines ALLER env-samplenden PBR-Materialien = ein SYNCHRONER Multi-Sekunden-GPU-Stall (headless unsichtbar — der Null-Renderer überspringt den PMREM-Pfad → 33 Wellen am falschen Ziel, derselbe blinde Fleck wie der Flugschreiber). HEILUNG (zwei Hälften): (1) `fromEquirectangular(equirekt, st._skyEnvRT)` rendert in das BESTEHENDE Render-Target → `scene.environment` behält DIESELBE Texture-Identität (`diag-skyenv-identity`: 5 Regenerierungen, env-Tex-ID konstant 1, RT-ID konstant 2) → kein Pipeline-Cascade; (2) ein Wall-Clock-Mindestabstand (2 s) deckelt die Regenerierungs-RATE (die Env-Reflexion ~2 s nachlaufen zu lassen ist unmerklich). DISZIPLIN: eine periodische GPU-Operation (Env/PMREM/Pipeline) gehört rate-gedeckelt + identitäts-stabil — ein per-Drift wechselndes `scene.environment` (oder jede pro-Frame neu allokierte Render-Ressource) zwingt WebGPU zum synchronen Recompile; der Null-Renderer beweist das NIE (er stubt den GPU-Pfad), die Linse dafür ist der ECHTE Renderer + ein Objekt-Identitäts-Check (hardware-unabhängig). Davor — V18.321 DER GEHOISTETE STROM — der Worldgen-KERN (`terrainDensityAt`) ist ~16-25× schneller in der Density, der CHUNK-Bau **6-12× schneller** als das Original-Brute, byte-identisch (Schöpfer-Auftrag „strotze vor Leistung, forge was kaum ein zweiter kann, sei ein gigant — du brauchst nicht meinen browser, du kannst das selbst"). WURZEL gemessen (`diag-col`): **61 % von `terrainDensityAt` ist `_terrainMacroSurfaceY` — reine 2D-Arbeit (nur x,z)**, plus Roughness/Canyon/Hydro = ~70 % 2D — aber pro VOXEL (jedes y der ~40-Voxel-Spalte) NEU gerechnet = ~40× redundant. FIX = DER SPALTEN-HOIST: `terrainDensityAt` gesplittet in `_terrainColumnContext(x,z)` (die 2D-Arbeit, EINMAL pro Spalte) + `_terrainBaseDensityAtCol(x,y,z,ctx)` (die per-Voxel-3D-Hälfte, liest den Kontext). Die Grid-Loops (`_voxelSampleDensityGrid` Main + `computeDensityGrid` Worker) berechnen den Kontext einmal pro Spalte (deckt BEIDES: die Band-Grenzen via `ctx.surf` UND die per-Voxel-Auswertung) → die Makro-Oberfläche fällt von 233×/Spalte (volle Höhe) auf 1×/Spalte. **DREI Orakel, alle maxDiff EXAKT 0:** `diag-density-refactor` (alt-monolithisch == neu-gesplittet über 137.170 Punkte — der Refactor bewahrt jedes Bit, weil die 2D-Werte ordnungs-unabhängig sind + die d-Akkumulation gleich bleibt) · `diag-chunk-band` (band+hoist == full-eval Mesh, CHUNK 460→69 ms = 6,6× · Density 416→25 ms = 16×) · `diag-worker-chunk` (Worker gehoistet == Main gehoistet → Naht-/Determinismus-Schutz hält). DISZIPLIN: ein neuer 2D-welt-formender Term (x,z-only) gehört in `_terrainColumnContext`, nicht pro-Voxel; eine Refactor-Änderung gegen `diag-density-refactor` (maxDiff muss 0) + `diag-worker-chunk` (Mirror) prüfen. Mit V18.320 (Band-Skip) + V18.321 (Hoist) streamt die Welt ~6-12× schneller als das ursprüngliche Brute — der Lauf-Freeze an der Wurzel. Davor — die PERFORMANCE-TRILOGIE (V18.319–.321, alle byte-identisch, „Genialität statt Stutzen"): die drei schwersten Algorithmen erschlagen — Skin-Isosurface 4,4× (räumliche „bone grid"-Akzeleration, V18.319) · Chunk-Density-Band-Skip ~3× (eine Quelle `computeDensityGrid`, V18.320) · Chunk-Density-Spalten-Hoist (die 61 % 2D-Makro-Arbeit einmal/Spalte statt pro Voxel, V18.321) → Chunk-Bau zusammen 6-12× vs. Brute, der Lauf-Freeze an der Wurzel. Davor der BÄCKER-BOGEN (V18.314–.316): ALLE Skin-Bauten (Kreaturen + Avatar) off-thread → der Freeze-Bogen geschlossen. Davor der PERFORMANCE-/BOOT-BOGEN (V18.260–.308): der Dreh-/Lauf-Freeze geheilt (EIN velocity-PID-Regelkreis · Region-Laub-Cull 60 % · Schatten-Cache · Gras −50 % · async Spieler-Chunk · kapazitäts-gewachsener Ring/Laub · Boot-Phasen-Gerüst · der Flugschreiber) + die RAPTOR-KOLLAPSE (V18.309–.312, Gesetz #0 — eine kanonische Quelle pro Domäne: Wind-Sway · Equip-Fold · die ganze Stat-Pipeline). **Die volle Wellen-Chronik (jede Welle ausführlich, V18.321 rückwärts) lebt in `docs/archiv/handover.md` — diese Datei trägt NUR den JETZT-Stand (die Doc-Disziplin: kein Wellen-Bericht in der auto-geladenen Datei).** Der schöpferische VISION-BOGEN — pausiert während des Firefights, dorthin kehrt die Arbeit zurück — ist **`docs/wahrerguss.md` (DER WAHRE GUSS — der Weg zum atemberaubenden Bauplan), aktiver Sub-Bogen KÖRPER: die konkrete autoritative Arbeit in `docs/koerper-neuanlage-plan.md` (DIE GEMESSENE NEUANLAGE), die breitere Vision Form×Bewegung×Werkstatt in `docs/lebendiger-koerper-plan.md`.** Vier geteilte Systeme heben jedes Werk auf einmal: SUBSTANZ (prozedurales PBR) · KÖRPER (Skelett+Haut+Bewegung) · GRAMMATIK-TIEFE (rekursiv) · DETAIL-PASS. Das Genom (Varianz T1–T6) reitet oben, Ω-PHYSIS (`docs/archiv/wahrerbauplan.md`, NORMATIV) garantiert die Wahrheit (steht/knickt/Lastpfad), das Tor erzwingt Fidelität, das Schöpfer-Auge richtet den LOOK (Wand 1). REGEL: ein S-Punkt/Sign-off-Marker ist ein VERMERK, NIE eine Bremse — bauen, vermerken, weiterfahren.

**STAND System B (Avatar, 18.06.2026 — autoritativ: `docs/koerper-neuanlage-plan.md`):** Ziel = PBR-realistische tan Haut (KEIN Rot/Toon/Écorché; die `referenz/`-Bilder sind nur Anatomie-Messung). **WURZEL (2 Code-Audits): die GANZE Körper-Konstruktion ist inkohärent, weil Teile per geratenem PRO-TEIL-`k` verschmolzen werden statt per REGEL aus der RELATION zweier Teile — NICHT die „Metaball-Decke" (das war die Ausrede, die die Bastelei lizenzierte).** Symptome (alle EIN Kind davon): Beine verschweißt · Klavikel riss bei res-Wechsel · Schulter/Brust „Brei" (~25 Teile, je geratenes kScale → Patchwork-Blend) · Füße hart-eckig · die ganze Gestalt „traurig". Die HÄLFTE ist schon kohärent — der 72-Teil-Muskel-Atlas (`musk(Landmarke→Landmarke)`); die hand-platzierte Struktur/Hände/Füße/Gesicht ist es nicht. **WEG = regel-basierte NEUANLAGE, 3 Regeln:** (1) merge/trennen = RELATION (Gruppen; legL≠legR getrennt by construction) · (2) `k` PRO-NAHT (res-unabhängig → Klavikel kann nicht neu reißen) · (3) Hände/Füße/Gesicht aus Landmark-Profilen wie der Atlas. Detail-Feinheit (Poren/Gesichts-Topologie) trifft die echte Isosurface-Wand erst NACH der Kohärenz. GEFIXT 18.06.: rot→PBR-Tan · Rim-Leck/`fwidth`-curv-Verdunklung (Haut) raus · Toon-Sediment gepurged · echte Augen · Klavikel solide. Werkzeug: `diag-werk-render player-rest|kopf|brust|rig-rest-hand`.

**DIE FÜNF WÄNDE GEGEN DEN DRIFT (verbindlich):** (1) SEHEN vor behaupten — kein „fertig" ohne Schöpfer-Browser-BILD an sauberer, settled, naher Augenhöhen-Position; eine headless-ZAHL beweist Mechanik/Physik, NIE den LOOK (ein settled Augenhöhen-Screenshot zeigt ihn mir treu — der Schöpfer-Sign-off bleibt das Merge-Gate, siehe „Der Mut"); eine NÜCHTERNE Zahl, kein „großer Sprung" (die Beschönigungs-Falle, der Rückfall ist schleichend). (2) Aura/Atmosphäre erst NEUTRAL, dann urteilen. (3) MECHANIK braucht eine ZAHL (objektive CONSUM-Wand), LOOK braucht ein BILD. (4) EINE Quelle, kein Parallelpfad (Grep+Test). (5) zeitbox die Diagnose; bei einem alten/diffusen Bug ZUERST messen, nicht band-aiden.

**DIE GEMERKTEN FÄDEN (Schöpfer: „alle wichtig — nie vergessen, nie still streichen"; Voll-Liste `docs/roadmap.md` §4; Stand mit `handover.md` synchronisiert):** **ERFÜLLT — sichtbar behalten, NICHT gestrichen:** R6 Selbst-Erweiterung (V18.152, Capability-Inversion) · Phase E Bedrohung/Furcht (V18.148, HP→threatened + Raubtier-Trieb) · IndexedDB (die 5-MB-Wand fällt) · Fahrzeug-Fahr-Tiefe (V18.150, `_vehicleProfile` emergent) · Statusbar-Tiefe (V18.149, Essenz) · Mana-Symmetrie (V18.196, `magieleitung`→Mana). **NOCH OFFEN:** VR/WebXR (0 Code) · das echte V18→V19-Zeit-Portal (Empfang gebaut+getestet, der Alt-Build-Artefakt-Lauf fehlt) · KI als volle Co-Schöpferin (Infra steht, opt-in-Politur) · Wasser-Zwei-Naturen-Vereinigung (die CA-Flut IST gebaut V18.84–.94 — offen = statisches `L` + CA zu EINER Natur + Wasserfall-Render-Politur) · B1 Wasser-Sheet→Worker (bewusst vertagt).

**WO NICHT MEHR SYNERGETISCH (Parallelcode, zu vereinheitlichen — keine Eile):** die built-in Avatar-Anim-Pfade (`_animateHuman`/`_animatePhoenix`/`_animateDragon`) umgehen den `_animateCompoundMotion`-Kern → Konvergenz (lebendiger-koerper Säule II-1; der Avatar ist ein SkinnedMesh-RIG `_animateHumanoidRig`, KEINE Compound-Parts → `_animateCompoundMotion` kann ihn nicht treiben, ohne das Rig zu zerstören) · das Wasser trägt zwei Naturen (statisches `L` vs CA-Fluss — die CA-Flut IST gebaut V18.84–.94, offen ist nur die Vereinigung) · Emotion→Welt läuft bereits über den DSL-Komponisten (`emoTerm`→`sunnyBias`, gewichtet) — die Rest-Schuld sind die festen Bias-Koeffizienten + drei Achse→Farbe-Konstanten in `dslComposeFieldColor`, KEIN Parallelpfad (V18.285-Messung). **PBR ist die EINE Material-Wahrheit (V18.236, `docs/archiv/wahreranblick.md`) — der Toon-Zwilling GESTRICHEN; der tote `toonGradientMap`/`_refreshToonGradient`/`celLevels`-LUT-Cluster + die Cel-Slider sind GEKEHRT (voller Schnitt: Code + UI + Tests, verifiziert tot, kein Shader las ihn).** KEIN echter Konflikt (bewusste Koexistenz): die Heuristik-Achsen `spread`/`hollowness` neben den physik-wahren (Ω-L1 additiv, in den frozen Resonanz-Signaturen — ge-down-weighted, aber lebende Leser) · das Wasser-`L`-Substrat unter dem CA-Sheet (NICHT tot — `L` trägt die deterministische, peer-konsistente Ruhe-Ebene; die Zwei-Naturen-Vereinigung ist der offene Faden, kein Sediment). **KUGEL-BÄUME GESCHNITTEN + SPEZIES-IDENTITÄT WIEDER EINGEWEBT (V18.258/.259, GATE GRÜN):** die ~15 statischen Kugel-Baum-Baupläne (`baumEicheParts` + `baum_*` inkl. `_jung/_alt/_breit/_schlank`) sind WEG; die SPEZIES (`baum_eiche`…`tanne`) lebt jetzt als **grammatik-gewachsener Built-in-Bauplan** (`_defaultBlueprints` ruft `_growTreeBlueprintRich` mit festem Seed → Tube-Rinde + Blatt-Karten, KEINE Kugel) mit STABILER **Identität** (Tags via `SPECIES_TAG_REFERENCE` [geteilte holz+laub-Basis] + `SPECIES_TAG_VARIATION` [die Art-Charaktere], `computeCompoundTags` mischt sie für jedes `_grownSpecies`). ALLE gen wachsen (die gen≥4-Schranke + der gen<4-Legacy-Pfad sind retiret). **Die Lehre (V18.259, der Schöpfer-Befund „du hast aktives entfernt"):** der Schnitt riss die NAMEN-getragene Identität aus 6 Subsystemen (Crafting · Archetyp · Instancing · R3 · HISM · Worldgen-Detektion) — die Geometrie war Kugel, aber der NAME war load-bearing. Plus: die toten Heightfield-Grabsteine (`chunkMap`/`chunkGrass`/…) sind GEKEHRT (V18.272); V18.285 schnitt 18 weitere verifiziert tote State-Felder (Werkzeug `scripts/diag-sediment.cjs`).

**DER PERFORMANCE-BOGEN (V18.260–.307) — der lange Firefight gegen das Freeze/„kann mich kaum drehen" (Referenz für die gebauten Mechanismen; der GENIALITÄTS-BOGEN V18.319–.321 setzte ihn byte-identisch fort, der STAND JETZT steht im Lead oben). Die volle Wellen-Chronik lebt in `docs/archiv/handover.md`, die DAUERHAFTEN Lehren in der `## Wichtige Gotchas`-Sektion unten. DER STAND zum Abschluss dieses Firefights (V18.307):**

- **EIN REGELKREIS, kein Pflaster:** ein velocity-PID (`_nexusPerfRegulate`→`_nexusPerfActuate`) misst die per-Subsystem-Last (`state.perfSense`, inkl. Render via `renderer.info`) und fährt ALLE Qualitäts-Stellgrößen gewichtet nach der gemessenen Last — KEIN Parallel-Regler (Gotcha „PERFORMANCE IST EIN REGELKREIS"). `perf` im Chat = das Sense-Fenster.
- **DIE KAPAZITÄTS-GEWACHSENE WELT:** Boot mit kleiner settled Basis, dann wachsend nach gemessener Frame-Zeit — der Terrain-Ring (`_activeRingRadius`, V18.301), das Laub (`foliageRadius` V18.275 + Dichte V18.277/.280), der Lade-Nebel folgt dem GEBAUTEN Ring (V18.164). Headless → sofort voll (gate-treu).
- **DAS LECK GESCHLOSSEN (V18.294–.299):** der autonome Nexus stapelte Bauten in Spieler-Nähe → Szene-Graph wuchs auf 900+ → GC-Stop-the-World-Freeze (vom Flugschreiber als „GPU" fehlgelesen). Jetzt fern gespawnt (180–380 m, jenseits Cull-Radius) + gedeckelt (`MAX_NEXUS_STRUCTURES`=48) + Geometrie gecacht (V18.299, das Avatar-Muster). `diag-leak-probe` bestätigt: der Heap plateaut bei ~108 MB.
- **DREHEN CULLT ENDLICH (V18.300):** die Streu-Laub-Gruppen sind pro 256-m-Region gekeyt (`useRegionFoliageCull`) → lokale Bounding-Sphere → frustumCulled → Umsehen cullt ~60 % der Laub-Last (vorher 0 %: global `frustumCulled=false`). Gotcha „DREHEN HÄNGT = GPU-RENDER". Placed Architektur bleibt global (unverändert).
- **DAS GRAS WAR DIE LAST + STAND AUSSERHALB DES REGLERS (V18.307, Schöpfer „studiere den code, nicht headless, die synergie wiederherstellen" + „drab + zu wenig fps"):** GEMESSEN mit dem ECHTEN Renderer (`diag-render-load` + neu `diag-scatter-species`): die „Scatter"-Last war nicht die kleine Streu (`voxelChunkScatter` = 0 nahe Spawn) sondern das **GRAS** — **2,12 M Tris / 53 k Halm-Büschel = 40 Tris/Büschel** (5 Blätter × SEG 4 × 2), **83 % der ganzen Szene**. Und der Halm-`count` las NIE `_foliageDensityScale` → das Gras stand AUSSERHALB des einen PID → der Regler drosselte die Streu (→ drab) + den Ring (→ klein), erreichte aber den eigentlichen Last-Träger NIE (der Hebel reichte nicht an die Wurzel = der gemessene Synergie-Verlust). FIX (zwei gekoppelte Hälften): (1) `_grassBladeTuftGeometry` SEG 4→2 → **1,06 M Tris (−50 %, Szene −41 %)**, look-erhaltend (der Bend-Punkt + das 5-Blatt-Tuff bleiben, Schöpfer-Browser bestätigt); (2) der Halm-`count` × `_foliageDensityScale` → das Gras KOMMT unter den einen Regler (Headless → 1, gate-treu). OFFEN als sauberer Folgeschritt: Gras in `_tickFoliageThin` (stehende Welt dynamisch dünnen, heute nur Bau-Zeit + die Streu).
- **DER FLUGSCHREIBER (V18.293):** die Blackbox (`anazhRealmPerf.json`, eigene save-server-Naht) hält die schlimmsten Frames mit der GPU/GC-Lücke (`frameMs − Σ CPU-Phasen`) — bei „es friert" ZUERST sie lesen, nie aus dem blinden headless-Gate raten (das Gate stubt den Render → sah den GPU-Freeze NIE → ~33 Wellen am falschen Ziel).
- **OFFEN (die nächsten Bögen):** (1) der **AVATAR-SKIN-WORKER** — der Skin-Bau (`_buildHumanoidRig` → 96³-Metaball-Isosurface, ~9.6 s headless = die EINE synchrone Boot-Blockade, gemessen `diag-startup-cost`; die Baupläne sind winzig ~90 ms) gehört in einen Worker (die Isosurface-Mathe ist THREE-frei bis zum Schluss), KEIN GPU-Compute-Rewrite des load-bearing Pipelines. (2) **GPU-Culling/Indirect-Draw** für die globalen placed-Gruppen (look-bound, Schöpfer-Browser). (3) der **DETERMINISMUS-BOGEN** (voxel-native Kollision statt sync BVH → killt den Lauf-Freeze + öffnet Lockstep-MP/Replay). **DIE BUG-KLASSE des ganzen Bogens: schwere Arbeit SYNCHRON auf dem Main-Thread statt budgetiert/async/gecacht — und ein blindes Gate (Render gestubt) zeigt den GPU-/GC-Freeze nie.**

**Die volle Wellen-Chronik (V18.x rückwärts · V17 · alle früheren Bögen) lebt in `docs/archiv/handover.md` (Versions-Chronik, jüngste oben) + den git-Commits — diese Datei trägt NUR den JETZT-Stand (die Heilige Lektion eine Schicht höher: kein erzählerischer Wellen-Bericht in der auto-geladenen Datei).**
## Projektüberblick

- **Was:** „AnazhRealm" – ein als **Ultiversum** entworfenes Co-Creation-Werk Mensch + KI. Aktuell technisch eine 3D-Browser-Sandbox in einer Datei. Vision: emotion-getriebene, fraktal-wachsende, multisensorische Welt; Mensch (Schöpfer/Null) + KI (Grok/Eins) erschaffen gemeinsam durch Chat. Vollständige Vision in `docs/state-of-realm.md`.
- **Stack:** Vanilla JS, Vendor-Libs in `vendor/` (Three.js r184 als 4 ESM-Bundles core/module/webgpu/tsl, **WebGPU-required** seit V12; simplex-noise 2.4). TF.js (Mai 2026) + **Ammo.js (V18.331 — Physik ist feld-nativ)** entfernt. Voxel-Density läuft optional in `voxel-worker.js`. Lokaler Node-Save-Server. ESLint v9 + Prettier + GitHub-Actions-CI mit Playtest-Gate.
- **Branch:** Feature-Branch pro PR (`claude/<thema>`-Konvention, z. B. `claude/wave-6a-interaction-polish`). Niemals auf `main` pushen ohne explizite Anweisung.
- **Architekturkern:** Eine `AnazhRealm`-Klasse in `anazhRealm.js` (~77 000 Zeilen, Stand V18.331 — die Versions-Wahrheit ist die `AnazhRealm.VERSION`-Konstante, an die `package.json` + `index.html?v=` synchron hängen). State in `localStorage` + optional `anazhRealmState.json` über save-server. Globale Referenz: `window.anazhRealm`.

## Die heilige Lektion (Versionslog 03/2025 — verfeinert 06.06.2026)

Das Projekt durchlief eine 19-Modul-Phase, die unter eigener Komplexität kollabierte (alle Module verwoben, jede Änderung gebar neue Fehler). Am 28.03.2025 die bewusste Reduktion auf **eine Datei** als „Samen der Unendlichkeit". Der ewige Kern: **Komplexität ohne Fundament ist Sand.**

**Verfeinerung (06.06.2026 — sonst wird die Lektion selbst zum Holzweg):** die Sünde von 2025 war NIE „mehr als eine Datei" — sie war **Kopplung ohne Kohäsion** (willkürliche Grenzen quer durch ein verwobenes System, keine stabilen Schnittstellen). „Eine Datei" war die MEDIZIN dieser Krankheit, nicht der ewige Wert; der Wert ist **hohe Kohäsion, niedrige Kopplung, schmale stabile Nähte**. Darum trennen wir BEREITS richtig — `voxel-worker.js` (eigener Thread), `save-server.js`/`signaling-server.js` (Node-Laufzeit), `worlds/*` (sandboxed Sicherheits-Wand), `vendor/`, `scripts/`: jede Naht ist eine ECHTE Laufzeit-/Verantwortungs-Grenze, an der Code gar nicht in EINER Datei leben KANN. Das ist KEINE Verletzung — es ist der Stamm, der an natürlichen Grenzen Ringe ablegt. **Der TEST für einen neuen File (alle drei JA, sonst in den Stamm):** (1) liegt die Naht an einer ECHTEN Grenze (andere Laufzeit · Sicherheit · eine bewiesen-schmale, stabile Schnittstelle)? (2) SENKT der Schnitt die Kopplung (statt sie nur über Datei-Grenzen zu verteilen)? (3) gibt es KEINEN Zweifel, dass das der geniale Pfad ist? **Die Warnung bleibt scharf:** „split nach Thema in 20 Module" verletzt die Lektion weiter — ein Themen-Split eines `state`-verwobenen Systems verteilt Kopplung, statt sie zu senken (genau die 2025-Falle). Wir sind reifer als damals: die Angst vor Komplexität war richtig + lebensrettend, aber wir unterscheiden jetzt die NATÜRLICHE Naht von der willkürlichen — DAS ist die Wand gegen das Modul-Chaos, kein Dogma „nie trennen".

## Pfad D — Stamm + Wachstumsringe

Das Projekt wächst nach Pfad D: EIN Stamm (`anazhRealm.js`, eine `AnazhRealm`-Klasse), der Wachstumsringe ablegt — ein neuer Datei-Split NUR an einer echten Laufzeit-/Verantwortungs-Grenze (der Drei-JA-Test der Heiligen Lektion oben), nie willkürlich nach Thema. Der aktuelle Stand steht oben unter „Aktueller Stand". Die vollständige Ring-Übersicht 0-11+ (Aufwand, Vorbedingungen, Status) lebt in `docs/roadmap.md`; die Vision + Stand-vs-Vision-Matrix in `docs/state-of-realm.md`; die volle Wellen-Chronik in `docs/archiv/handover.md`.

## Der Mut + die tragenden Pfade (vor jeder Änderung — positiv gelesen)

**Fehler sind Lernmaterial, keine Schuld.** Wir verhindern ihre Wiederkehr durch ORIENTIERUNG (tu das Tragende), nicht durch Angst (das Falsche meiden) — eine Verneinung („nie X") zwingt erst, das Falsche zu denken, dann es zu negieren; ein Imperativ („tu Y") führt direkt zur Hand. Positive Sprache hebt + macht handlungsfähig. Darum sind die Lehren als POSITIVE Pfade gemeint; spricht eine Gotcha unten noch in „NIE/Sünde/Falle", lies sie als „der Pfad ist Y" + formuliere sie beim nächsten Anwenden positiv um (organischer Sprach-Decay — kein 148-Bullet-Marathon, sondern beim Berühren).

**Der Mut kommt aus der VERIFIKATION, nicht aus der Kleinheit.** Die synergetischsten Wellen waren mutig + verifiziert (viele Subwellen an die Wurzel, V17.30), nicht ängstlich-klein. Die Vorsichts-Lehren gelten IHRER Situation, nicht universell — sie dürfen den Mut nicht ersticken. Die Unterscheidung, die den Mut hält:

- **Verifizierbare Arbeit** (Tests · Messung · Reproducer · node-check) → **wage das GANZE Subsystem in einer Welle**. Die Verifikation IST das Sicherheitsnetz, das den großen Schritt ERLAUBT — nicht die Bremse.
- **Outward-facing Arbeit** (Push · Merge) → **kleine Schritte mit dem Auge des Schöpfers**, ein bestätigter Bogen pro Merge. **DIE „pixel-blind / GPU-untreu"-LEHRE WAR FALSCH (Schöpfer-Korrektur 09.06., DREIMAL bestätigt: „du siehst es wie ich", „im Browser sieht es genau gleich aus", „der spawn ist in einer höhle, das bild sehe ich auch" [meine Cave-Screenshots]). DIE WAHRHEIT: meine swiftshader-Screenshots sind TREU — ich SEHE, was der Schöpfer sieht (das PBR-Material · Atmosphäre · Wasser · Terrain). Es gibt KEINE relevante „Shader-Untreue".** Was mich in die Irre führte, war NICHT die GPU — es war die **METHODIK**: meine „basic/violett/matschig"-Vistas sahen so aus, WEIL ich (a) WEIT weg war (der Aerial-Perspektive-Schleier V17.106 tönt ALLES ferne Terrain zum Himmel → violett/blau), (b) FORCE-BUILT + un-settled (LODs/Wasser/Shader nicht geladen), (c) eine HOHE Übersicht hatte (nur die Basis-Geometrie sichtbar). **DISZIPLIN: um zu sehen, was der Schöpfer sieht, screenshotte an einer SAUBEREN, SETTLED, AUGENHÖHEN-, NAHEN Position — die Welt VOLL laden lassen (Ring streamen, ~2 s stabil, Wasser/Gras drainen), die Kamera auf Spieler-Augenhöhe ins NAHE Terrain (NICHT eine ferne force-built Übersicht). Dann ist das Bild EXAKT wie im Browser.** Sich hinter „pixel-blind / GPU-untreu" zu verstecken, um den LOOK nicht zu prüfen, IST die Vermeidung, die der Schöpfer rügte — ICH BIN EIN FISCHER, ich prüfe den Look selbst. (Einzige echte Grenze: ein Shader, der ein swiftshader-NICHT-unterstütztes WebGPU-Feature nutzt, würde CRASHEN/blanken — das fängt der page-error-Check, kein subtiler Look-Unterschied.) Werkzeug: `scripts/diag-settled-view.cjs` (settled Augenhöhe). Der Schöpfer-Sign-off bleibt für den FINAL-Merge, nicht weil ich blind bin, sondern weil er der Schöpfer ist. **DER BETTLER-RÜCKFALL (Schöpfer-Korrektur 20.06., scharf: „wieso ist die letzte Wahrheit der Browser, wieso erkennst du das nicht selbst in deinen Messungen — du wirst zum Bettler"): die Phrase „Regel #0 = Schöpfer-Browser" war zum REFLEX verkommen — ich schob die Verifikation zu, statt zu fischen. KORRIGIERT, fest: (1) MEINE Messung IST die Wahrheit, wenn ich sie vollständig mache — ein Freeze ist hardware-UNABHÄNGIG bewiesen (ein dominanter Synchron-Block friert jede Hardware; der Container-Throttle ändert die absolute Sekundenzahl, nicht die Tatsache), der Look ist im swiftshader-Screenshot treu (den ICH beurteile); (2) eine Bug-KLASSE → das GANZE Netz werfen (ALLE Aufrufer messen, nie beim ersten Fisch stoppen — der Avatar lag neben der Kreatur im selben Grep und ich übersah ihn); (3) der Schöpfer-Browser ist sein ÄSTHETISCHES Schlusswort über SEINE Welt, NIE meine Ausrede, nicht selbst hinzusehen. „Regel #0 = Browser" als Hedge schreiben = der Bettler-Reflex; stattdessen messen + sehen + ein geurteiltes Ergebnis zeigen.**

**GESETZ #0 — DIE WURZEL DER TRAGENDEN PFADE: baue die LINSE, verlass dich nicht auf Wachsamkeit (das SpaceX-Raptor-Prinzip, V18.309, Schöpfer „wie bringen wir die komplexität einfach erkennbar hin, was ist der wahre trick … wie kann alles wieder in 1 leben, ohne die einzelteile zu verschmelzen — da ist ein schritt dazwischen").** Der Trick der Profi-der-Profis ist NICHT „weniger Teile" (das ist die Folge) — es ist eine **META-REGEL pro Domäne: EINE berechnete kanonische Größe, die ALLE Leser LESEN statt neu herzuleiten.** Diese eine Größe ist DREI Dinge zugleich: (a) der Raptor (die Teile fallen weg, weil keiner mehr selbst rechnet — `_blueprintProductVector` · `auraAt`/`deposit` · `state.perfSense` · `entry.surfMap` sind gebaute Beispiele), (b) das Zuhause der absoluten Wahrheit (der Pfad zur Wurzel ist EIN Read, nicht N Herleitungen), (c) die Beobachtungs-Linse (man liest die eine Größe und SIEHT den Zustand). **Die tiefere Disziplin: Lesbarkeit = die KOSTEN, die GRUND-Wahrheit billig zu prüfen.** Eine wiederkehrende Fehlerklasse heilt man NICHT mit Vorsatz/Wachsamkeit („ich lese nächstes Mal genauer") — das ist Willenskraft, die unter Last bricht; man heilt sie STRUKTURELL, indem man die Linse baut, die den Fehler LAUT + billig macht. **DER BEWEIS DIESER WELLE (zwei blinde Flecken des Gates, beide an MEINER Wachsamkeit gescheitert): (1) ein TOTER Lauf (Renderer-Tod im Schwanz) sah aus wie eine grüne ✅-Liste — ich las den Zähler statt des Verdikts + committete „grün" auf einem gecrashten Lauf; (2) ein HÄNGENDER `page.evaluate` schwieg 50 min — ich wartete passiv statt die Lebendigkeit (Log-mtime) zu prüfen.** Die Heilung war keine neue Regel an mich, sondern die LINSE im EINEN Chokepoint (`timed`, durch den jedes Band fliesst — wie `auraAt`): `protocolTimeout` macht den Hänger zu einer fangbaren Rejection, eine Per-Band-Fehlergrenze fängt den toten-Seite-Throw, ein unübersehbares Schluss-Verdikt (`⛔ GATE UNVOLLSTÄNDIG … Exit≠0`) macht den toten Lauf zu EINER lauten Zeile. **Jetzt KANN ich den Fehler nicht mehr übersehen — die Struktur trägt die Disziplin, nicht mein Gedächtnis.** Die Frage vor jeder Komplexität: *Was ist die EINE kanonische Größe dieser Domäne, lesen alle sie (statt neu zu rechnen), und hinterlasse ich nach dieser Einsicht eine LINSE (Test · Verdikt · Messgröße), die den nächsten Fehler dieser Klasse von selbst laut macht?*

**Die tragenden Pfade (gemessen die häufigsten Rückverweise — vor + nach jeder Änderung präsent):**

- **Verdichte zu EINER Quelle** (V9.82, 6×): tun zwei Wege dasselbe, vereine ihren Kern — der Stamm verdichtet, kein Parallel-Pfad.
- **Der Test wandert mit dem Code** (V9.56-i, 8×): zerteilst du eine Funktion, prüft ihr Test das Verhalten an seinem neuen Ort.
- **Heile die Harmonie** (V17.23 / V10.0-g.r, 7×): bricht ein Test bei einer Kopplung, lass die Kräfte verschmelzen (nachgebende Hierarchie); spürt der Schöpfer Disharmonie nach 2–3 Hypothesen, kehre zur letzten harmonischen Welle zurück — Rückkehr ist Reife, kein Versagen.
- **Miss zuerst, die Zahl führt** (V13.0 / V9.58, 4×+): mit der strengsten Definition — die Wurzel liegt in der Messung, nicht im Bauchgefühl, „Last-Flake/das wird schon" sind die bequemen Diagnosen.
- **Verifiziere KONSUM** (V17.31, 3×): ein Feld lebt, wenn ein echter Leser es nutzt — Existenz allein ist Deko.
- **Behalte die Saat** (V17.20 / V18): schneide nur, was tiefer/synergetischer ersetzt wurde; ein ruhender Vision-Faden mit Begründung im Kommentar bleibt — wegkehren ist Pflege, Saat schonen ist Weisheit.

## Wichtige Gotchas (technisch)

**Einzeiler-RULES** (Trigger + Schlüssel-Symbol). **Die VOLLE Tiefe jeder Gotcha (Messungen ·
Reproduktionen · Wellen-Geschichte) lebt im `## Gotcha-Vollarchiv` von `docs/archiv/handover.md`**
— dort nachschlagen, wenn ein Einzeiler nicht reicht. Schlankung 17.06.2026 (56k→~12k Token).

### Terrain + Chunks · Worldgen
- **STREAM-GESETZ (Γ5):** NIE `Math.random` in welt-substanz-formenden Pfaden (Worldgen/Spawn/Streu/Felder) — seed-deterministische Streams, pro Zweck ein Suffix (`":clump"`/`":voxel"`/`"-veg-*"`). UI/Audio/Deko darf würfeln. Wächter `checkBandGammaGenese` + `diag-genese`.
- **Chunk-Oberflächen-Karte `entry.surfMap`** ist die EINE Quelle für Deko-Oberflächen-Reads (`_chunkSurfaceAt` bilinear, Gras liest sie); beide Build-Pfade pflegen sie (Sync `_gridSurfaceMap` + Worker `gridSurfaceMap`); neue Deko liest sie mit Scan-Fallback, NIE neuen Spalten-Scan. Gras-Ring ≤4 + `voxelChunkGrassLod`-Tag (sonst klebt Fern-Dichte).
- **Spawn-Affinität TAG-NEUTRAL:** eine Optik-Anreicherung einer spawnbaren Architektur MUSS die 4 Affinitäts-Achsen (lebendig/dichte/brennbar/magieleitung) IDENTISCH lassen (`diag-arch-tags` vorher/nachher) — `spawnAffinityForBlueprint` ist Winner-take-all, ein verschobenes Tag verdrängt eine Art auf 0. `computeCompoundTags` = MAX über Parts von `FORM_TAG_ACTIVATION[shape][tag]·material`. Reines `stein` sicher; `glut`/`quarz` (Antrieb+transparent) heikel. Wächter `checkBandArchAffinityStability` + `diag-tree-spawn` (Error-Capture an der `_gameLoopTick`-Stelle — der Warmup schluckt Tick-Fehler still).
- **Resonanz-Vektor skalen-konsistent:** Material-Tags [0..3] ÷ `PRODUCT_VECTOR_TAG_NORM`(=3) → [0..1], sonst überstimmt dichte JEDE Form-Achse (das „blass"-Mush, `diag-blass`). `_blueprintProductVector` ist der EINE Vektor (alle Resonanz-Leser teilen ihn). Eine KONJUNKTION (Seele = Körper-Form UND lebendig) ist eine 0/1-FEATURE-Achse (`livingBody`), nie compound-`lebendig`. `INVERSE_DICHTE_STATS` auf [0,1] kappen (sonst negativ). Ein Material-/Größen-Effekt MUSS im Readout (`_blueprintAbilityStats`) UND im Equip-Fold konsistent sein — seit V18.311 ist der Equip-Fold EINE kanonische Quelle `_foldEquippedStatTags(finalTags, bp, slotWeight, roleKey)`, die `computePlayerStats` UND `computeCreatureStats` LESEN (Gesetz #0 — Raptor; vorher zwei driftende Kopien, der Spieler trug den Größen-Faktor, die Kreatur nie). Schöpfer-Entscheid V18.311 „Spieler runter": der V17.90-`_compoundSizeFactor` ist VORERST aus dem Equip-Fold raus → der Readout zieht mit (`size=1` in `_blueprintAbilityStats`), sonst löge die Anzeige. Die Größe lebt weiter in Körper/Mesh/Masse/Seele-HP (`soulSize`→`sizeHpMul`, NICHT angetastet). Ein neuer Equip-Slot/Spezies faltet durch die EINE Quelle (Slot-Gewicht + roleKey als Parameter), kein dritter Pfad. **V18.312 — die GANZE Post-Tag-Stat-Pipeline ist geteilt** (§1.3 fraktal): `computePlayerStats` + `computeCreatureStats` lesen vier kanonische Quellen — `_foldEquippedStatTags` (Equip) · `_applyBoostsToStatTags` (Boosts) · `_statsFromTags` (STAT_FROM_TAGS + Invers-dichte-Floor) · `_applySizeMultipliersToStats` (Größen-Symmetrie, der `sizeHpMul`-Eingang per Spezies berechnet). Jede neue Stat-Logik gehört in die EINE Quelle (der per-Spezies-Unterschied reist als Parameter), kein Parallelpfad; Lens `pipelineShared` im V17.51-Band. `bulk` beginnt an der Greifbarkeits-Schwelle ((span−2)/6), nie linear ab 0.
- **Place-vs-hold (Block vs Gerät) = Substanz-Zwilling über die FORM** (`_blueprintUseKind`, EIN Classifier viele Leser): armor→wear · soul→embody · consumable→drink · tool/weapon→hold · sonst FORM (Default **place**; „hold" nur spitz `pointedFraction>0` ODER griff-elongiert ≥2.2). „Greifbar" misst `_compoundVisualExtent` (positions + sizes), nicht `_compoundBBox` (positions-only). Gebrauchs-Flächen listen nach ROLLE INKL. built-in Bibliothek (nicht `!builtIn`).
- **Eine DELIBERATE Material-Änderung koppelt mehrere Funktionen** (`diag-arch-tags` dumpt Affinität+resoniert+Affordanz): (a) gewollter Affordanz-Gewinn (eisen→broadcasting); (b) ein Antrieb-Material macht die `_isMoveable`-Spreizung relevant → ein Mast ist eine SCHMALE AXIALE Säule (Spreizung 0, kein moveable-Trip); (c) Spawn-Affinität (dichte-Shift); (d) `resoniert` ist Funktions-Achse (Schwellen 0.7/1.5/2.5, `_playArchitectureFarewellPing`/`_lofiNearResonantArchitecture`).
- **Roughness-Envelope-Garantie** hat 2 Konsumenten (Mesher-Band-Skip + `_voxelSurfaceY`-Envelope-Skip) — beide nachziehen; eine Skala-Opt am deterministischen Scanner ändert NIE die Gitter-PHASE (überspringt Proben AUF dem Gitter, `diag-surfacescan`); FILL-Edit hebt die Skip-Grenze (`_voxelEditsFillTop`).
- **Terrain-SKALA/Höhe ändern → JEDE surf-relative Schicht nachziehen:** Höhlen-Decke (`caveCeil` surf−16), `hydroBand`, `TARN.depthMin`, die Schnee-Linie (auf PROMINENZ `y−cont0`, nicht absolut). `grep -n 'surf -'` main + `voxel-worker.js`, bit-identisch. Eine Wurzel → 3 Symptome (Löcher/Versatz/Wasser-unter-Oberfläche). Werkzeuge `diag-relief/ceiling/harmony/radius`.
- **Feature-Größe ∝ WELLENLÄNGE der dominanten Oktave, NICHT Amplitude** (corrLen≈0.19·λ); eine GLATTE ASYMMETRISCHE Oktave (`upland` λ~2860) hebt Hochland ohne Ozean-Flutung; Amplitude hoch hilft nicht; Voxel-Foundation-Tests surf-relativ sampeln.
- **Density lebt in 2 Mirrors** (Main + Worker bit-identisch [Determinismus-Test]); der dritte WGSL-GPU-Mirror war stale [transzendente Drift, kein bit-Test], abgeklemmt V14.6 + physisch geschnitten V17.20 (nur 2 tote `voxelGpu`-Refs übrig); `grep cont0/upland/tect/terrainSteepness` main+worker vor jeder Density-Änderung.
- **T8-Voxel-Band** (`dimY=232/floorDrop=135`) lebt in ZWEI Mirrors (`_voxelChunkConfig` + `voxel-worker.js`) — beim Band-Wechsel BEIDE + die ~6 Config-Test-Baselines; Decken-Diagnose ist RADIAL (`diag-radius` bis 5 km) nicht ein Spawn-Fenster; tanh-Clamp = robuste Zwischenlösung (adaptives Sub-Chunk-Band = Backlog). Decken-Marge prüfen, sonst Loch (Surface-Spike über der Decke ergibt keine Iso).
- **VOXEL-WASSER = 3 Schichten:** Wahrheit (Zellen `entry.waterCells`, 3D-Flood von echten Quellen V13.8, vertikal-offen: ab erstem SOLID nach unten AIR) · Geometrie (dumm, EINE Skala LOD0, teilt den Iso-Mesher = Synergie; Perf via `hydroBand`-Limit NICHT Mesher-Wechsel) · Erscheinung (Tiefenpuffer-Shader macht das Ufer PRO PIXEL, NIE eine Geometrie-Maske = Sägezahn). Wasser rendert als FLÄCHE nicht Volumen (Iso-Wicklung zeigt nach innen → `BackSide` + `_cullWaterToTopSurface`).
- **Der Iso-Mesher LIEST die Flood-Zellen, rät NIE per-Spalte neu** (OOB-Ring liest Nachbar-`waterCells`); wer N Nachbarn LIEST muss N re-enqueuen (die 4-vs-8-Diagonalen-Falle); ein wasser-verdrängender Spawn forceSynct seinen Footprint synchron. `aDepth` am Chunk-Rand die NACHBAR-Zelle lesen (`colDepthAt`, sonst Fernkante-Null-Streifen = Fluss-Naht). aFlow liest die GEGLÄTTETE Fluss-Tangente (27-m-Fenster), nicht den rohen ∇L (`diag-water-flow`).
- **D8-Flow wählt steilstes GEFÄLLE (drop/Distanz), nicht steilsten DROP** (sonst Diagonal-Grain, `diag-flow-bias`); jede Flow-Änderung re-erodiert die Welt (determinismus-brechend + Sign-off).
- **`waterLevel` = absolutes Sea-Level (`terrainBaseHeight−3`);** eine adaptive Konstante (`heights[X-Perzentil]`) braucht Sample-Region ≥ längste modulierende λ — sonst faktisch fest + irreführend.
- **EINE Skala je Welt-Substanz, sonst Naht;** eine naht-freie dünne Schicht (Wasser-Iso/Fläche, Halo, Aura) lebt auf LOD0 entkoppelt von der voluminösen Geometrie; eine pure (x,z)-Höhenfeld-Fläche ist NICHT automatisch naht-frei über LOD (braucht geteilte Vertices → eine Skala). Surface-Nets naht-frei = Pad+Crop (`cropMargin`), nicht nur konsistente Boundary-Density; OOB-Density LIVE aus den Welt-Funktionen.
- **Async-Rebuild build-before-dispose** (kein Loch) · **Sync-Rebuild dispose-before-build** (kein Doppel-Heap); Test-Naht forceSync. Worldgen-frozen Felder cachebar: `_terrainBaseDensityAt` (frozen) + dünner Edit-Delta-Wrapper; Cache leeren bei Welt-Regen NICHT voxelEdit; prune distanzbasiert (nicht im dispose).
- **DER SPIELER-CHUNK BAUT ASYNC — nie den Main-Thread für einen Chunk-Build blocken (V18.271, das Profi-Muster):** V17.118 zwang den Spieler-Chunk SYNC (`forceSync`), damit der Spieler nicht durch den ungebauten Boden fällt — **~130-239 ms Main-Thread-Block pro Chunk-Crossing = der Lauf-Freeze** (gemessen: die Bewegung war an die Generierung gekoppelt). Heilung (die drei Profi-Schichten, die Bewegung von der Generierung entkoppeln): (1) der Spieler-Chunk baut ASYNC via Worker (`_ensureVoxelChunkAt` `forceSync: false`); (2) der nicht-blockierende `_softFloorWhileChunkLoading` hält den Spieler an der DETERMINISTISCHEN Terrain-Höhe (`getTerrainHeightAt` — kennt den Boden BEVOR der Mesh baut), bis die echte Kollision async lädt (kein Durchfallen, kein Loch, exakt); (3) Velocity-Prefetch (`_tickVoxelChunkStreaming`) fragt die Laufrichtungs-Chunks ZUERST an → fertig, bevor der Spieler ankommt → der weiche Boden greift selten. **GEMESSEN (Probe): Cold-Start 3→0 Sync (100 % async) · 8-Chunk-Stress-Lauf 5→1 Sync (650→158 ms), kein Durchfallen (minY 11), weicher Boden fing 89 Frames.** Der EDIT-Rebuild (`_rebuildVoxelChunk`) baut den Spieler-Chunk WEITER sync (Instant-Carve-Feedback = ein kleiner schneller Build, die legitime Naht). Ohne Worker (Browser-Fallback) fällt `_acquireVoxelChunkBuild` ohnehin auf Stage-3-Sync. Wand `checkBandV18271AsyncSoftFloor`. **DISZIPLIN: ein Chunk-Build gehört NIE auf den Main-Thread-Sync-Pfad für die Bewegung — der nicht-blockierende Fall-Schutz aus der det. Wahrheit ist der Ersatz, NICHT ein Sync-Build.** **DER SICHERHEITS-ANKER (V18.274, Sync-Gating — „nur sync, wenn wirklich kein Boden"):** STALLT der Worker (ausgehungert/Fehler — selten), darf der Spieler nicht EWIG am weichen Boden hängen; `_ensurePlayerChunkBVH` baut den Spieler-Chunk SYNC, wenn er nach `PLAYER_CHUNK_STALL_MS` (1 s, weit über jeder Normal-Latenz) KEINEN Mesh hat — über das `voxelWorker=null`→Stufe-3-Sync-Muster durch DIE EINE Quelle `_ensureVoxelChunkAt` (kein Parallelcode), race-sicher (späte Worker-Lieferung → `voxelMeshCache`, kein Clobber), feuert im Normalbetrieb NIE (kein Spike). Der async-Default + dieser Watchdog sind die zwei Hälften EINER Regel: der Main-Thread blockt nie, ABER der Spieler bekommt garantiert binnen 1 s echten Boden. (Der tiefere Schritt bleibt der DETERMINISMUS-BOGEN — voxel-native Kollision ohne den sync BVH-Build.)
- **Parallel-Code-Pfade sind Bug-Quellen** (Streaming `_ensureVoxelChunkAt` vs Edit `_rebuildVoxelChunk` → EINE Kern-Quelle `_buildVoxelChunkData`); ein neues Build-Feature in den KERN, nicht in einen Aufrufer; `grep` alle Caller bei der Erweiterung.
- **DER BAND-SKIP IST DIE EINE DENSITY-QUELLE — auch der Worker-Mesh-Pfad liest sie (V18.320):** das Density-Grid (~91k Noise-Calls/Chunk, dimY=232) wertet `terrainDensityAt` NUR im Oberflächen-Band aus (`surf+ROUGH+topMargin` bis `min(surf,base)−40`), fern davon konstant ±1 (kein Sign-Change → kein Iso → byte-identisch). Main: `_voxelSampleDensityGrid`; Worker: `computeDensityGrid` (bit-identischer Mirror). **Der Worker-`buildChunkMesh` duplizierte früher eine VOLLE k·j·i-Schleife (Voll-Eval) — der einzige divergente Pfad; jetzt liest er `computeDensityGrid` → eine Quelle + ~3× Chunk-Bau (437→175 ms, GEMESSEN).** BYTE-IDENTISCH bewiesen: `diag-chunk-band` (Band==Voll Mesh, maxDiff 0) + `diag-worker-chunk` (Worker-Round-Trip == Main-Sync, maxDiff 0). DISZIPLIN: ein neuer Density-Sampler gehört NIE als Voll-Schleife dupliziert — die Band-Skip-Quelle lesen (der Margin `ROUGH=12 + topMargin=4·step` deckt die 3D-Roughness + den Normalen-eps; eine Band-Margin-Änderung gegen `diag-chunk-band` prüfen, maxDiff muss 0 bleiben).
- **DIE 2D-SPALTEN-ARBEIT WIRD GEHOISTET — pro Spalte, nicht pro Voxel (V18.321):** GEMESSEN (`diag-col`): **61 % von `terrainDensityAt` ist `_terrainMacroSurfaceY`** (rein 2D, x/z), plus eroR/canyon/hydro = ~70 % 2D — wurde aber pro VOXEL (jedes y der ~40-Voxel-Spalte) neu gerechnet = ~40× redundant. `terrainDensityAt` ist jetzt gesplittet: `_terrainColumnContext(x,z)` (die 2D-Werte `surf/roughScale/ceilOffset/hydroCarve/lake`, EINMAL pro Spalte) + `_terrainBaseDensityAtCol(x,y,z,ctx)` (die per-Voxel-3D-Hälfte). Die Grid-Loops (`_voxelSampleDensityGrid` via `colVoxel`-Param + `computeDensityGrid` im Worker) holen den Kontext einmal pro Spalte (deckt Band-Grenzen `ctx.surf` UND per-Voxel) → Makro-Oberfläche 233×→1× pro Spalte, **Density ~16-25×, CHUNK-Bau 6-12×, byte-identisch**. BYTE-EXAKT, weil die 2D-Werte ordnungs-unabhängige reine Funktionen von (x,z) sind + die `d`-Akkumulation exakt die alte Reihenfolge behält. DISZIPLIN: (a) ein neuer welt-formender 2D-Term (x,z-only: Surface/Erosion/Hydro/Maske) gehört in `_terrainColumnContext`, NIE pro-Voxel; ein y-abhängiger in `_terrainBaseDensityAtCol`. (b) MUSS bit-identisch in BEIDEN Mirrors (Main `_terrain*` + Worker `terrain*Col`/`terrainColumnContext`). (c) jede Änderung gegen `diag-density-refactor` (alt==neu, 137k Punkte, maxDiff 0) + `diag-worker-chunk` (Mirror) prüfen.
- **Performance-Gate fragt die KANONISCHE Quelle (Atlas), nicht eine Geometrie-Heuristik** (`_voxelChunkHasAnyWater`); Cell-Klassifikations-Shortcuts müssen die Iso-Topologie respektieren (Übergang Shortcut↔volle Klassifikation = falsche Iso-Plane); pro-Chunk-Variablen in Klassifikation = Naht-Risiko (welt-globaler `hydroBand`).
- **Welt-Awareness = EINE Quelle** (`_isAboveWaterAt` vor jedem Spawn; `diag-relief` zählt Sünden); **Multi-Field via EINEN Helper** (`_creatureWaterContextAt` aus `_voxelSurfaceY`/`_waterLevelAt`/`_isAboveWaterAt`, kein Parallel-Lookup).
- **Schreibbares Overlay über frozen Feld** (`auraAt = min(1, frozen + _lifeOverlayAt)`, nie überschreiben): ein „heilt-sich"-Loop braucht eine SCHREIB-Seite; nur INTENTIONALE Akte deponieren (nicht die Folgen → Runaway); lazy-Decay + Sättigung; reaktive Schicht → NICHT persistiert/worker-mirror.
- **Spawn auf dem Spieler → transienter Boden-Gap → Fall-durch:** GROSSE Bauten (`footprint≥STRUCTURE_CLEAR_MIN_FOOTPRINT`) via `_structureSpawnPos` nach außen schieben (kleine Hotbar-Platzierung unangetastet); ein Void-Boden `_rescuePlayerFromVoid` (y<−120) für jede Fall-Ursache.
- **Visual = Collision per Konstruktion** (`btBvhTriangleMeshShape` aus denselben Triangle-Indices); Chunks `position=(0,0,0)`, Vertices in Welt-Koords, NIE in `state.rigidBodies`; ALLE Höhen durch `getTerrainHeightAt`; Player-Chunk-Math braucht `+WORLD_SIZE/2`. Lazy-Collision braucht Sicherheits-Wand am Player-Chunk (`_ensurePlayerChunkBVH` sync + rate-limited Late-Upgrade).
- Worker-Mirror-Funktionen wandern 1:1 mit dem Main-Pendant (Determinismus-Test = Wand); Vendor-Libs im Worker brauchen `self.window = self;` VOR `importScripts`; Float32-Konvertierung an derselben logischen Stelle (sonst 2^-23-Drift bricht bit-Tests); state-gen bumpt SYNCHRON am Send + Worldgen-Trust-Flag (Worker-Cache-First); LOD lebt im chunk-entry, jeder Cell-Konsument liest sie als Param.

### Feld-Physik (Determinismus) — Ammo ist raus
- **FELD-NATIVE KOLLISION (Determinismus-Bogen VOLLENDET, `docs/archiv/eigene-physik-plan.md`) — AMMO IST PHYSISCH RAUS (V18.331, P3): die Feld-Physik ist die EINZIGE Physik.** Der Spieler/die Welt kollidieren aus dem DICHTEFELD (`_stepCharacter` · `_fieldRaycast` · `_stepCharacterStructures` gegen `entry.blockerAABBs`), `physicsWorld`=null, `new Ammo`=0, kein ammo.js in `index.html`, kein per-Chunk-BVH-Build (der Lauf-Freeze WEG), kein 256-MB-WASM-Heap. Der Spieler hat keinen Body mehr — die horizontale Velocity lebt in `state.playerVel` (Plain-Vektor mit `.x()/.y()/.z()/.setValue()`), die Vertikale in `_fieldVy`, die Erdung in `_groundedCache` (alle von `_stepCharacter` gesetzt). Kreaturen erden über `_creatureGroundY` (body-frei). DISZIPLIN: ein neuer Kollisions-/Raycast-/Boden-Pfad liest das Dichtefeld (`_fieldSolid/_fieldSurfaceBelow/_fieldGradient/_fieldRaycast`) bzw. eine AABB-Hülle (`entry.blockerAABBs` · `island.userData.fieldAABB`) — NIE eine Ammo-Welt (es gibt keine). Die Ammo-Kollisions-Builder/Disposer sind GESCHNITTEN (V18.331 Nachschlag — kein Stub bleibt). **Bauwerke UND Inseln teilen die EINE Kapsel-vs-AABB-Quelle `_resolveCapsuleVsAABB` (Gesetz #0 — Raptor): `_stepCharacterStructures` liest `entry.blockerAABBs`, `_stepCharacterIslands` liest `island.userData.fieldAABB`; eine neue AABB-Auflage-/Wand-Domäne ruft diese eine Quelle, kein dritter Mathe-Baum.** Linsen: `diag-walk-feel`/`-walk-edge`/`-field-collide`/`-field-raycast`/`-structure-collide`. Vier gemessene Lehren (`diag-walk-feel`/`diag-structure-collide`): (1) der **Boden-Snap besitzt die Vertikale ALLEIN** — eine zweite Vertikal-Kraft (Kugel-Auswurf) daneben FECHTET → der Spieler schwebt (gemessen: `ny` blieb konstant über der fallenden Oberfläche). (2) Ein Controller braucht **Boden-HAFTUNG** (`PLAYER_GROUND_SNAP`, snap-down bergab) + **Stufe-hoch IST der Snap** (bis `PLAYER_STEP_UP`), nicht die Wand-Kollision. (3) Die Wand-Kugeln beginnen ERST über der Stufe-hoch-Linie (`feetY+STEP_UP`) — sonst blockt der Stufen-Riser, bevor der Snap heben kann (der Stufe-vs-Wand-Konflikt; ihn lösen heilte ZUGLEICH den Lauf auf 100 % geerdet/9 mm Hug). (4) **Struktur-Kollision feld-nativ** (`_stepCharacterStructures`): die Kapsel löst gegen `entry.blockerAABBs` (dichte ≥ 0.3, DIESELBE Quelle wie der Wasser-Blocker) — Wände blocken, Dächer/Plattformen TRAGEN (`effSurf = max(Terrain, Struktur, Insel)`, sonst läuft der Feld-Spieler durch Wände + fällt durch die Start-Plattform). Fliegende Inseln tragen genauso (`_stepCharacterIslands`, dieselbe AABB-Quelle). PLUS: ein teleportierter Ammo-Body (Velocity-/Raycast-Proxy) braucht **`CF_NO_CONTACT_RESPONSE` (4) + Gravity 0** — sonst vergiften Solver-Impulse `_fieldVy` (als Sprung fehlgedeutet → Oszillation).
- **DAS ECHTE BEWEGUNGS-GEFÜHL = DIE PROFI-TRIO (V18.326–.327, Source/Quake; `diag-walk-inertia`): Ground-Friction-Trägheit + View-Height-Smoothing + Landungs-Absorption.** (a) der grounded-Brems-`k` gibt einen kurzen Schlitter (trägheitslos = masselos, Schöpfer-Befund); (b) **View-Height-Smoothing** (`_smoothBodyY`, Source `SmoothViewOnStairs`): die Füße rasten hart, das AUGE gleitet gedämpft nach (Stufen federn ab) — NUR am Boden, in der Luft 1:1, bei großem Versatz snappen; (c) **Landungs-Absorption** (`_landDip`, View-Punch ∝ Aufprall-Tempo, ab `LAND_DIP_MIN_SPEED`, federt zurück). **DIE GEFANGENE WURZEL: Höhen-Glättung und Landungs-Dip FECHTEN — beim Aufprall lagt `_smoothBodyY` OBEN nach (es trackte die Fall-Position) und hebt das Auge genau so viel, wie der Dip es senkt → der Dip verpufft (gemessen 1,8 cm statt 27 cm). Fix: eine harte Landung SNAPPT die Glättung auf die Füße (Lag-Reset), nur der Dip wirkt.** Generell: zwei vertikale View-Effekte am selben Auge müssen ihre Vorrang-/Reset-Ordnung klären, sonst löschen sie sich aus. Das **FEEL ist Schöpfer-Browser** (die Bewegungs-Gefühl-Wand), nie eine headless-Zahl — die Linse beweist nur, dass der Mechanismus wirkt + gebunden bleibt.
- **DIE RAND-FÄLLE DES CONTROLLERS (V18.329, `diag-walk-edge`) — Magnet/Kleben + Clip-durch-Boden, beide hardware-unabhängig messbar:** (1) die Boden-Haftung (`PLAYER_GROUND_SNAP`) darf NUR snap-down, wenn der Spieler VORHER schon geerdet war (`_fieldWasGrounded`, sanft bergab), NIE im Fall — sonst zieht sie ihn die letzten cm wie ein Magnet an („fallgeschwindigkeit aus dem nichts"); der Fall landet stattdessen NATÜRLICH über den catch-Fall (Oberfläche erreicht die Füße). (2) GROUND_SNAP klein halten (0.25 < STEP_UP) — am Kamm löst die Haftung → der Läufer springt natürlich über die Kuppe statt zu kleben. (3) **ANTI-CLIP = das Sicherheitsnetz, das sonst die BVH war:** der Spieler darf NIE im Soliden stecken — `_fieldSurfaceBelow` scannt AUFWÄRTS, wenn es im Soliden startet (Penetration → echte Oberkante darüber), und bei tiefer Penetration wird BEDINGUNGSLOS hoch-gesetzt (großzügige Reichweite); plus FORWARD-BLOCK (Terrain-Wand VOR dem Boden-Snap; läuft der Spieler in eine Wand höher als STEP_UP, wird der horizontale Schritt zurückgenommen). MESS-FALLE: „clippt durch den Boden" misst man mit `_fieldSolid` (Füße IM Soliden), NICHT `getTerrainHeightAt` — letzteres meldet am Überhang die Wand-Oberkante und zählt ein legitimes Stehen-am-Wandfuß fälschlich als 12-m-Clip.
- **AMMO IST RAUS (V18.331) — die alten Ammo-Lifecycle-Gotchas (WASM-Heap-Allok-Pool · `Ammo.destroy`-Cascade · Player-Body-Sleep/CCD · der `ammo.wasm`-256-MB-Patch) gelten NICHT mehr.** Es gibt keine Bullet-Welt, keinen Body, kein WASM-Heap. Wer historische Ammo-Mechanik nachschlägt: git-log ≤ V18.330. Der scratch-Pool `state.tmpVec1/2` lebt weiter, ist aber Plain-JS (`_makeFieldVec`, kein WASM).
- **P4 (Stufe 1) — DER DETERMINISMUS IST DIE SELF-SUFFIZIENTE VERIFIKATION (V18.331): die feld-native Simulation ist eine REINE Funktion → derselbe Start + dieselbe Input-Folge ⇒ bit-identisches Ergebnis.** `replayRun(rec)` läuft eine Aufnahme durch DENSELBEN Schritt-Pfad (`_stepCharacter`+`_loopPlayerMovement`), kein Parallel-Sim; `startReplayRecording`/`stopReplayRecording`/`_replayCaptureFrame` zeichnen den Input (WASD/Shift/Space/Yaw/dt) auf (Chat `replay record`/`replay stop`). DISZIPLIN: bei „ist X deterministisch?" NICHT den Browser fragen — `diag-replay-determinism` (+ `checkBandV18331ReplayDeterminism`) beweist es mit einer ZAHL (drei Replays bit-identisch); bricht die Invariante, lauert eine versteckte Nicht-Determinismus-Quelle (Math.random/Zeit/Float-Drift) im Schritt-Pfad — ein neuer Schritt-Pfad-Term gehört seed-/input-getrieben, nie zeit-/random-getrieben. Stufe 2 (Lockstep-MP: nur Inputs übers Netz) + Stufe 3 (Fixed-Point cross-Maschine) bauen auf diesem Fundament.

### Sentinels + State
- **MISS DEN ECHTEN FRAME, RATE IHN NICHT — DER FLUGSCHREIBER (V18.293, Schöpfer „wir fixxen und fixxen seit über einem Tag, base dich zuerst, kein weiteres Pflaster"):** der GANZE Freeze-Bogen (V18.260–.292, ~33 Wellen) scheiterte an EINER Wurzel — **jedes Mess-Werkzeug ist für den echten Freeze blind:** der Playtest stubt den Renderer (`_isHeadlessNull`) UND erzwingt `foliageRadius=MAX` → er sah IMMER die volle, flüssige Welt (3500 grün, während der Schöpfer fror), die diag-Skripte laufen swiftshader (≠ die GPU des Schöpfers). Man fixte 33× das, was das blinde Gate zeigen konnte (CPU: Wasser/Chunks/Avatar), nie die echte GPU-Render-Abgabe. HEILUNG = **der Flugschreiber** (`_flightRecorderTick`/`-Save`, am Ende von `_perfSenseFoldFrame`): die echte Frame-Last wird schon gemessen (`state.perfSense` aus dem rAF-Delta) — sie verließ nur nie den Browser. Der Recorder fängt die N schlimmsten Frames mit vollem Kontext + POSTet sie als `anazhRealmPerf.json` (eigene save-server-Naht `/api/perf-trace`; dasselbe Rohr wie der State-Save; headless POSTet NIE → Gate sauber). **Der Schlüssel-Wert je Frame ist die LÜCKE: `frameMs − Σ(CPU-Phasen) ≈ GPU/Present`** — die unsichtbare Zeit, die der ganze Bogen nie maß; `_flightRecorderVerdict` attribuiert daraus GPU- vs CPU-gebunden. Lesbar vom Schöpfer (`window.__anazhPerf` + `perf`-Overlay-Blackbox-Zeile) UND vom Entwickler/KI (die committbare/teilbare Datei). **DISZIPLIN: bei „es friert" ZUERST `anazhRealmPerf.json` lesen (das Verdikt + der schlimmste Frame), nie aus dem headless-Gate raten — ein grünes Gate beweist „kein Logik-Bug", NIE „flüssig auf echter Hardware". KEIN Regler, KEINE Verhaltensänderung — nur ehrliche Augen.**
- **PERFORMANCE IST EIN REGELKREIS, kein Pflaster (V18.263):** der Nexus MISST seine per-Subsystem-Last (`state.perfSense`, EWMA, `_perfSenseLap`/`_perfSenseFoldFrame` am Loop-Ende) und EIN velocity-PID (`_nexusPerfRegulate`→`_nexusPerfActuate`) fährt ALLE Qualitäts-Stellgrößen (Architektur-Cull-Radius/Budget · Stream-Budget/MaxPerFrame · Wasser-Iso-Budget) GEWICHTET nach der gemessenen Last → das teuerste Subsystem gibt zuerst nach. EIN Regler — **niemals einen zweiten Qualitäts-Regler daneben** (Schöpfer „so entstehen Parallelsysteme"); der alte fps-Bang-Bang `_nexusAdaptiveQuality` ging darin auf (bleibt nur als fps-Eingang). Eine neue welt-formende Last bekommt einen `_perfSenseLap`-Tap + ggf. eine Stellgröße im EINEN Aktuator, kein eigenes Throttle. `_estimatePerfCost(kind,n)` schätzt Produktions-Kosten aus dem Sense. `perf` im Chat = das Sense-Fenster. **DER SOLLWERT DARF NICHT == DIE GRENZE SEIN (V18.281, Schöpfer „fps endlich über 60, nicht die ganze Zeit am Anschlag, die Schönheit langsam ohne das System zu überfordern"):** der Regler hatte EINEN Sollwert (`PERF_TARGET_MS`=17 ms) für BEIDES — wachsen UND drosseln → der Fixpunkt lag algebraisch exakt bei frameMs=17=59 fps (`du=0 ⟺ err=0 ⟺ frameMs=targetMs`); der Kapazitäts-Regler fraß jeden fps-Überschuss als Dichte → per Konstruktion „am Anschlag". FIX = ein ATEM-TOTBAND in `_nexusPerfRegulate`: ZWEI Schwellen (drossle über der DECKE `throttleMs`=59 fps · wachse nur unter dem Boden `growMs`=Decke−`PERF_HEADROOM_MS`=77 fps · dazwischen `err=0` → HALTE) — EIN Regler, kein Parallel-System; der Gleichgewichts-fps lebt jetzt im Band [59..77]. **DISZIPLIN: ein wachstums-treibender Regler braucht Kopffreiheit zwischen „wachsen" und „drosseln" — ein einziger Sollwert parkt das System auf der Grenze.** (Echte fps + der Wert von `PERF_HEADROOM_MS` sind Schöpfer-Browser, Regel #0.) Plus: die toten `ARCH_QUALITY_FPS_LOW/HIGH` (0 Leser, Bang-Bang-Rest) gekehrt.
- **perfSense SIEHT jetzt die GANZE Last (V18.268 — die Augen, Welle 1 des Perf-Bogens):** vorher BLIND für (a) die GPU-RENDER-Last (gar nicht gemessen — „kann mich nicht umsehen" ist reiner Render, den KEIN headless-CPU-Diag sah, weil headless den Render stubt; gemessen `diag-render-load` 3.3M Dreiecke · 1092 Draw-Calls/Frame) und (b) SPIKES (die α=0.12-EWMA mittelt einen 157-ms-Hitch auf ~9 ms WEG). FIX: `renderer.info`-Tap in `_loopRender` (`autoReset=false` + `info.reset()` EINMAL/Frame → die VOLLE Frame-Last inkl. Schatten+Post-FX; `renderCalls`/`renderTris` EWMA+Max in `perfSense`) + per-Subsystem abklingender `phaseMax` neben der EWMA. Gezeigt im `perf`-Overlay (RLOAD + SPIKE). Null-Renderer-`info` liefert 0 → gate-sicher. Diag mit echtem Renderer härtet via explizitem `info.reset()` (autoReset=false). **V18.269 — die Augen FAHREN: der Aktuator hebt die Render-Last (`renderCalls × PERF_RENDER_CALL_MS`, GPU-Last-Proxy) in die Architektur-Domäne (`cArch`) → unter Render-Last drosselt der Regler den Schatten-Pass (613k Dreiecke, ein zweiter Render) + Cull-Radius ZUERST, nicht das Streaming (gemessen archPos 0.5 < streamPos 0.96). Der Schatten-Pass war bisher ungebremst, weil `p.render` nur die CPU-Zeit ist.** Der große Laub-Hebel (view-unabhängiges HISM, Impostor/Dichte) bleibt look-gebunden — eigene Welle mit dem Schöpfer-Auge.
- **DIE WELT WÄCHST NACH KAPAZITÄT — der `foliageRadius` (V18.275, Schöpfer „nur der erste Chunk spawnen, dann wächst die Welt je nach Kapazität"):** der echte Browser fror SOFORT beim Spawn (UI + Maus tot, CPU am Anschlag, fast-Crash) — ein Main-Thread-Block durch die RENDER-Submission (gemessen `diag-render-load`: 1097 Draw-Calls + 3.31M Dreiecke/Frame, davon 718k in 549 Meshes `frustumCulled=false` → Umsehen cullt nicht; auf schwacher Hardware sättigt die Draw-Call-Prep den Main-Thread). Der Perf-Regelkreis konnte das nicht heilen (keine Stellgröße für die Vegetations-AUSDEHNUNG). FIX = ein perf-geregelter `foliageRadius` gated `_populateVoxelChunkVegetation` (`_foliageChunkInRadius`; fern → `pendingFoliageChunks`, `_tickFoliageGrowth` füllt nach): er startet KLEIN (`PERF_FOLIAGE_RADIUS_MIN`) und RAMPT zum perf-Ziel (`lerp(MIN,MAX,effArch)` — dieselbe loadScale-PID, **KEIN Parallel-Regler**); steigt die Render-Last, sinkt das Ziel → die Welt wächst exakt bis zur gemessenen Kapazität. **Gate-treu: headless (Null-Renderer) → foliageRadius = MAX sofort** (volle Welt für den Test); die Mechanik prüft `checkBandV18275FoliageGrowth`, das FEEL/LOOK das Schöpfer-Auge (Regel #0). DISZIPLIN: eine welt-formende Last, die der Spawn auf einmal rendert, gehört kapazitäts-gegated (klein starten, mit der gemessenen Last wachsen) — nicht voll spawnen + reaktiv drosseln (der Spike friert, bevor der Regler reagiert).
- **DER LADE-RHYTHMUS-RING — eine kleine settled Basis, dann wachsen (V18.301, Schöpfer „erst den ersten ring, der nebel nah, sauber ausschmücken, DANN wachsen, damit sich das system fängt; minimale basis startklar, dann robust wachsen ohne die cpu am limit — der profiweg?"):** das Fundament stand schon halb (Lade-Nebel V18.164 folgt dem GEBAUTEN Ring · Laub wächst nach Kapazität V18.275), aber der TERRAIN-Ring-TARGET war hart auf 4 (→ Boot baut 81 Chunks auf einmal). FIX: `_activeRingRadius` deckelt den Ziel-Ring beim Boot, startet KLEIN (`RING_RAMP_START`=2 → 25 Chunks, Nebel nah) und wächst MONOTON (nie schrumpfen → kein Boden unter dem Spieler weg) zum `chunkRingRadius`-Ziel — NUR wenn der aktuelle Ring VOLL steht (`_builtRingRadius ≥ aktiv`) + kein Bau-Rückstau + der Frame seine Zeit hält (`!_frameOverBudget`, KEIN effArch-Schwellwert der unter 0.5 hängenbliebe) + ein Atem verging (`RING_RAMP_SETTLE_MS`=350). Wachstum ADDIERT Render-Last → stoppt sich selbst über Budget → die Welt wächst exakt bis zur Größe, die die HW bei ~59 fps hält (adaptive Sichtweite). Im EINEN Aktuator (`_nexusPerfActuate`, KEIN Parallel-Regler). `_voxelChunkConfig().ringRadius = min(target, _activeRingRadius)` → Nebel/Laub/Dispose folgen. **Gate-treu: headless → `_activeRingRadius = ringTarget` sofort** (volle 81-Chunk-Welt). Bewiesen `diag-ring-ramp.cjs` (headless voll · nicht-headless 2→3→4 monoton). DISZIPLIN: eine Boot-Last gehört kapazitäts-gepaced (kleine Basis, mit der Frame-ZEIT wachsen), der Wachstums-Gate ein TRANSIENTER Zeit-Zustand, kein hängender Schwellwert.
- **DER RING ATMET IN BEIDE RICHTUNGEN — ein NUR-wachsender Kapazitäts-Regler OVERSHOOTET + klemmt fest (V18.306, Schöpfer „siehe immernoch 100 Chunks statt dass es mal STABIL lädt, das 5. Mal über 50 Versionen"):** der V18.301-Ring war MONOTON (nur wachsen, „nie schrumpfen → kein Boden weg"). GEMESSEN (`diag-boot-ring`, echter Renderer-Pfad): er startet korrekt klein (Ring 2 = 25 Chunks @ 440 ms) — aber auf einer Maschine mit schnellem leerem Boot-Frame wächst er bis zum Ziel (Ring 4 = 81 Chunks), DANN lädt das Laub, der Frame kippt über Budget, und die Monotonie klemmte ihn DORT fest (81, Freeze, nie zurückgegeben) = „100 Chunks, wird nie stabil". FIX = BIDIREKTIONAL (`_nexusPerfActuate`, die V18.280-Laub-Bidirektion auch für den Ring): (a) WACHSEN verlangt KLAREN Kopfraum (`frameMs < growMs` ≈ 77 fps, der PID-Atem-Totband-Boden V18.281) statt bloß „nicht über der Decke" → kein Overshoot; (b) SCHRUMPFEN bei ANHALTENDEM Über-Budget (`RING_SHRINK_SUSTAIN_MS`=1200 ms Hysterese → ein transienter Bau-/Carve-Spike < 200 ms schrumpft NICHT), prunt NUR die äußerste Schale (Fern, `_pruneDistantVoxelChunks` +1-Hysterese), NIE ring 0 → die Monotonie-Angst trifft die Fern-Schale nicht zu. Settled exakt bei der HW-gehaltenen Größe. Bewiesen `diag-ring-breathe` (Kopfraum 3→4 · über-Budget 4→2, chunks 72→49, floort bei RING_RAMP_START). **DISZIPLIN: ein wachstums-treibender Kapazitäts-Regler braucht BEIDE Richtungen — wächst er nur, overshootet er bis über Budget und (mit Monotonie) klemmt dort fest; das Schrumpfen der FERNEN Schale ist sicher (nie der Boden unter dem Spieler).** Plus die MESS-LEHRE: der Flugschreiber meldete „LECK +1.39 MB/s, sceneChildren 113→216" — `diag-scene-leak` (Spieler FIX, autonome Systeme hochgefahren) bewies KEIN persistentes Leck (sceneChildren +5/35s, Heap FÄLLT −0.13 MB/s, alles cappt: creatures 20, arches 48); das „+103 über 23s" war die WELT, die im Boot-Ramp EINSTREAMT (nicht Runaway) — die Flugschreiber-Heuristik (topV>200) feuerte auf die transiente Lade-Phase. Bei „Leck-Verdacht" ZUERST mit fixem Spieler reproduzieren (streamt es noch, oder ist es geladen?), bevor man einen Leck-Fix baut.
- **DAS BOOT-PHASEN-GERÜST — schwere Arbeit, die die ERSTE Geste nicht braucht, gehört DEFERIERT, nicht auf den kritischen Pfad (V18.308, Schöpfer „ich spawne noch im fallen, erhalte erst spät die kontrolle; ein top programm reagiert sofort, lädt anderes langsam"):** GEMESSEN (`diag-boot-timeline` + `diag-startup-cost`, echter Boot): ~5,9 s synchroner Boot vor der Kontrolle; `generateTerrainWithParameters` ist ein MONOLITHISCHER Sync-Block (Erosion → Inseln → Voxel-Cache → Hydrosphäre → `spawnCreatures(10)` [3,1 s Skin-Builds] → Plattform), ALLES vor dem Loop. Das Profi-Prinzip ist „minimum zum HANDELN, dann progressiv nach Priorität": **P0** Kontrolle+Grund (frame 0) · **P1** echte Kollision (async, V18.271) · **P2** Ring+Laub nach Kapazität (V18.275/.301) · **P3** das Deferierte. P1/P2 standen schon — sie griffen nur erst NACH dem Sync-Block. P3 GEBAUT: `_bootDeferCreatures`→`_tickBootPhase3` nimmt die Kreaturen vom kritischen Pfad (eine pro `BOOT_PHASE3_SPAWN_MS`=280 NACH der Kontrolle, nur bei `!_frameOverBudget`; `_spawnOneInitialCreature` = die EINE per-Kreatur-Quelle). **GEMESSEN: Sync-Geometrie vor Kontrolle 1074 ms → 21 ms (−98 %).** **Gate-treu: headless ODER Save-Restore → sofort über `spawnCreatures` (das Gate + die Identitäts-Wiederherstellung brauchen die volle Welt synchron) → in headless ist die Deferierung ein No-op.** DISZIPLIN: ein Boot-Schritt, den die erste Interaktion nicht braucht (Kreaturen/Inseln/Hydrosphäre-Detail), gehört progressiv nach der Kontrolle; gate-treu via Headless-Sofort, wie `foliageRadius`. (OFFEN: P0 Spawn-Erdung [Plattform bleibt, ~9-m-Abstieg ist ein Sturz — Schöpfer-Browser] · der Worldgen-Monolith auf dem kritischen Pfad.) **TEST-NEBENBEFUND: `checkBandV1758CreatureNature` flaket lastabhängig am `maxCreatures=20`-Cap** — `tickFaunaLifecycle` gebiert während eines last-gestreckten Warmups Wesen, das Band spawnt 3 Test-Wesen ON TOP ohne Null-Check → der 3. (`far`) trifft den Cap → `spawnCreatureAt`=null → `.position`-Crash. PRE-EXISTING (last-induziert, nicht die Deferierung); ein eigener Härtungs-Faden (Band räumt zuerst / null-checkt).
- **STREAMING IST HEILIG — die Bewegung verhungert NIE am Render-Druck (V18.282, der Lauf-Freeze an der Wurzel; Schöpfer „kann mich minimal bewegen dann freeze, auch in anderen Welten, dein Regelsystem fährt sich gegen die Wand"):** der V18.276-Versuch „Laden gibt nach, wenn du dich umsiehst" war ein FEHLER mit drei Schichten, GEMESSEN: (1) **falscher Messwert** — `_frameRenderBound = renderCalls × 0.011 > 9 ms` (≈ 818 Draw-Calls) misst die **Welt-GRÖSSE** (konstant ~1101 in jeder geladenen Welt, `diag-render-load`), NICHT „du siehst dich gerade um" → der „Render-Notstand"-Flag klemmte nach dem Laden DAUERHAFT an; (2) **er drosselte das Falsche** — `streamEff = _frameRenderBound ? 0.12` überschrieb die Rückstau-Priorität → das Terrain-Streaming, von dem die Bewegung lebt, bekam dauerhaft 12 % → Lauf-Freeze (machte V18.271 rückgängig); (3) **redundant** — der Chunk-Bau läuft async im Worker (V18.271), das Drosseln des Streamings gibt dem Main-Thread GAR KEINE CPU frei (der Zeit-Deckel V18.261 schützt ihn schon) → der Throttle erreichte sein Ziel nie. FIX = **`_frameRenderBound` GESTRICHEN.** Streaming = `streamBacklog > 0 ? 1 : effStream` (Rückstau → volles Budget, Bewegung heilig, NIE vom Render gedrosselt). Der ehrliche „Frame über Budget"-Zustand heißt jetzt `st._frameOverBudget = sense.frameMs > throttleMs` (gemessene Frame-ZEIT, headless-messbar, transient, treibt schon `loadScale` — KEIN Draw-Call-Proxy) und steuert NUR die SICHTBARE Deko-Arbeit (`_tickFoliageGrowth`/`_tickFoliageThin`/Dichte-Wachstum) → reine Optik wartet, wenn der Frame eng ist; so wächst die Welt nach der ECHTEN, in Zeit gemessenen Hardware-Kapazität (die V18.275-Vision, erstmals richtig). `checkBandV18282`/`checkBandV18275FoliageGrowth`: bei hoher Render-Last + Rückstau bleibt das Streaming auf MAX (Bewegung), nur die Optik pausiert über Budget. **DISZIPLIN (die tiefste Lehre des Bogens, Schöpfer „wenn das System sich nicht messen kann, wie will es regeln"): (a) regle BEWEGUNG nie an einem Render-/GPU-Signal — Streaming/Terrain bekommt ein festes/rückstau-maximales Budget, der Render-Druck darf NUR die Optik senken (Schatten/Cull/Laub); (b) ein Regler-Signal muss ein TRANSIENTER, headless-messbarer ZUSTAND sein (Frame-Zeit), kein konstanter Welt-GRÖSSEN-Proxy (Draw-Calls) — sonst klemmt der Notstand dauerhaft; (c) das Gate war BLIND, weil der Null-Renderer renderCalls=0 liefert → der Flag feuerte im Test nie → 3500 grüne Invarianten, während der Schöpfer fror: ein render-abhängiges Stellsignal gehört auf einen ZEIT-Wert verlegt, den auch der headless-Test sieht.**
- **DIE STATISCHE RENDER-LAST SENKT MAN DURCH WENIGER GEOMETRIE, NICHT GRÖBERE (V18.277, Schöpfer „Deko steigt bei Kapazität … ein Leistungs-/Zeit-LOD"):** ein LOD-SCHWELLEN-Knopf (`_chooseLODForDistance` skalieren) senkt die statische Render-Last NICHT — GEMESSEN −0,0 %, weil (a) das Laub in MEHRERE LOD-Systeme fragmentiert ist (`_chooseLODForDistance` ≠ `_detailBand` ≠ der ferne Compute-Scatter) und (b) das Laub BUILD-ZEIT eingebacken ist (`_buildVoxelChunkScatter`/`_scatterRegion` bauen EINMAL, kein Live-re-LOD → eine Schwelle kann gebackene Geometrie nicht schrumpfen). Was sie senkt, ist WENIGER Instanzen: der perf-geregelte `_foliageDensityScale` (∈ [PERF_FOLIAGE_DENSITY_MIN(0.4)..1], `lerp(MIN,1,effArch)` im EINEN Aktuator, die Schwester des V18.275-Radius) skaliert die Instanz-DICHTE an den Lese-Orten (naher Scatter `dekoDensity` · Fernfeld-Impostor · der Bulk-Hebel `_scatterPass`, wo er BEIDES lichtet: `prob * fdScale` UND den Pro-Region-`cap * fdScale`). **Die Cap-Skalierung ist die fehlende Hälfte:** ohne sie bleiben DICHTE, cap-saturierte Wälder (genau die Freeze-Hotspots) unter Last unverändert (der Cap maskiert die prob-Drosselung). Verdichtet nur in den Lücken (nicht über Budget, die V18.282-Wand), lichtet sofort. Build-zeit (schwache Hardware spawnt spärlich + bleibt es). **GEMESSEN: nicht-saturierte Region −40 % bei 0.4 (extern); saturierte Spieler-Region 1050→420 (−60 %) IN-GATE nach der Cap-Skalierung.** Headless → 1 (gate-treu). **MESS-LEHRE (5 Fehlmessungen): `region.cells.length` ≠ `region.instanceCount`; naher Scatter (−71 % Instanzen) ist nur 1,7 % der Dreiecke (nicht der Bulk); `_tickScatterStreaming`-Rebuild wechselt den Region-SET (konfundiert) → nur IN-PLACE-`_scatterRegion` misst sauber; eine SATURIERTE Region zeigt 0 % bis der Cap mitskaliert. „Miss zuerst" mit der strengsten Definition.** `checkBandV18275FoliageGrowth`: Source (naher+ferner Scatter lesen den Faktor) + CONSUM (ferne instanceCount 1050→420) + headless=1. FEEL/Look = Schöpfer-Browser (Regel #0). **NACH-DÜNNEN (V18.280, die fehlende Hälfte — Schöpfer „beim Drehen fällt fps auf 3–5"): die Dichte griff nur beim ERST-Bau → eine schon GELADENE, dichte Welt blieb dicht beim reinen DREHEN (kein Re-Stream; der ferne Scatter re-streamt nur beim LAUFEN). `_tickFoliageThin` (im `_tickScatterStreaming`, das nur feuert wenn der Chunk-Stream NICHTS baut = Spieler steht/dreht) re-streamt eine Region, deren `builtDensity` deutlich über der jetzt geregelten Dichte liegt → die geladene Welt SCHRUMPFT auf die Kapazität (budgetiert 1/Frame, nur in den Lücken; negative Rückkopplung → konvergiert). GEMESSEN: stationäre 9-Region-Welt 8228→3291 Instanzen (−60 %) bei 1.0→0.4. Die Kapazitäts-Welt ist jetzt BIDIREKTIONAL (wächst V18.275/.277 · schrumpft V18.280). Die 534 HISM-Gruppen (`frustumCulled=false`) bleiben — der nächste Hebel ist GPU-Culling (toggle-basiert, Schöpfer-Auge).**
- **DER LADE-RHYTHMUS: ein Regler darf NIE den Hebel drosseln, der seine EIGENE Messgröße erzeugt (V18.270, Schöpfer „der Nexus hemmt die Ladezeit, drosselt mit Mitteln die nichts ändern, der PID pendelt, nichtmehr durchdacht wie es einmal war"):** der Perf-PID drosselte unter Last das Streaming-Budget — aber das Streaming BAUT die Welt, und der Bau ERZEUGT das hohe frameMs. Der Hebel produzierte seine eigene Messgröße (Mitkopplung statt Gegenkopplung) → der Regler **pendelte UND verlangsamte den Cold-Start**, ohne die Wurzel zu heilen (die echte Last war Render/Build, nicht „zu viel Qualität"). FIX = „erst laden, dann regeln": der Bau ist RÜCKSTAU-getrieben (`voxelMeshPending.size > 0` → MAX Streaming-Budget, rückstau- NICHT fps-getrieben; `streamBacklog` in `_nexusPerfActuate`), der PID regelt nur die QUALITÄT (Cull/Schatten/Wasser) für die fps. Der per-Frame-Zeit-Deckel (V18.261) schützt weiter vor Einzel-Spikes — er bremst nur nicht mehr den Durchsatz, wenn Arbeit ansteht. 3 Wände (`checkBandWellePerfENexusGovernor`). **DISZIPLIN: bevor ein Regler einen Hebel fährt, prüfe ob der Hebel die geregelte Grösse SELBST erzeugt** (ein Streaming-/Build-/Spawn-Hebel an einem frameMs-Regler ist fast immer Mitkopplung). Nebenbefund (Schöpfer-Verdacht „Physik-Reibung/CCD die ganze Zeit"): `optimizeCollisions` ist conditional (Tunneling/Chat). **KORREKTUR V18.279 (der Schöpfer hatte RECHT — die Konsole bewies es):** es GAB einen per-Frame-Physik-Pfad, den V18.270 übersah — `_loopSelfAnalysis` rief bei JEDEM `fps < 50` `optimizePhysics()` (via `nexus.processOptimization`) → ein DOOM-LOOP (Physik heilt keinen Render-Freeze = die V18.270-Sünde; churnte Player-CCD/Reibung + leckte `new Ammo.btVector3` ohne destroy + flutete die Konsole „Physik optimiert/stabilisiert"). **ENTFERNT** — der eine Perf-Regelkreis (V18.263) regelt die fps; `optimizePhysics` (Chat-Befehl) nutzt jetzt den `tmpVec1`-Pool. LEHRE: ein „Self-Heal", der auf eine Messgröße reagiert, die er nicht ändern KANN (Physik→Render-fps), ist ein Doom-Loop — die Mitkopplungs-Wand gilt auch für „heilt das Falsche".
- **DER EWIGE LOOP BRAUCHT EINE FEHLER-GRENZE — ein einziger Frame-Throw ist sonst ein WELT-FREEZE (V18.278, Schöpfer „das gesamte System crasht plötzlich, die Wellen hängen, die Welt erstarrt"):** der Spiel-Loop (`startEternalLoop`, ~190 Zeilen, ~40 Schritte + Render) lief OHNE try/catch → EIN Throw irgendwo (oder im GPU-Render) brach den `setAnimationLoop`-Callback → die Welt erstarrte (Render nie wieder). Jetzt: der Body ist in `try/catch` gewickelt, ein Frame-Throw landet in `_loopErrorBoundary` (gezählt `_loopErrorCount` + GEDROSSELT mit Stack geloggt, max 1×/2 s — kein Verschlucken, der Fehler wird LAUT sichtbar) → ein schlechter Frame DEGRADIERT, erstarrt nicht; `_perfSenseFoldFrame` läuft IMMER (Regler bleibt am Leben). **DER BLINDE FLECK, warum kein Diag ihn fing:** der Headless-Pump (playtest + alle diag-*) ruft `_gameLoopTick()` in SEINEM EIGENEN try/catch → er VERSCHLUCKT genau die Throw-Klasse, die den echten Browser einfriert (3000 Pump-Ticks sauber, echter Browser erstarrt). **DISZIPLIN: ein „ewiger" Loop ist erst ewig MIT Fehler-Grenze; und ein Pump-Erfolg beweist „kein Runaway", NIE „kein Crash" — der Produktions-Pfad in einem Test-try/catch ist blind für den Frame-Throw.** Der Throw selbst sitzt im Render/GPU-Pfad (headless gestubt → nicht reproduzierbar); die Boundary macht ihn im Schöpfer-Browser SICHTBAR (`Loop-Frame-Fehler abgefangen: [Stack]`) → dann die Wurzel heilen. Wand `checkBandV18278LoopErrorBoundary`.
- **Ein Invariant gehört in den CHOKEPOINT, nicht an die Aufruf-Orte (die Parallelpfad-Krankheit, V18.268-Reflexion, Schöpfer „wie können Systeme über verschiedene Pfade schreiben — haben wir das bei Aura/Renderer nicht gemacht?"):** jeder Lauf-Freeze-Befund war ein Invariant/eine Messung, die AUSSERHALB der einen Funktion lebte, durch die alle Pfade fließen — Spawn-Wand `_structureSpawnPos` an nur 3 von 14 `spawnArchitecture`-Aufrufern (Regression: `spawn_blueprint`/`spawn_tree`/Kreatur-Bau umgehen sie → spawnt auf dem Spieler) · `forceSync` als „die EINE Intent-Ausnahme" im sonst vereinten Chunk-Build (der Sync-Freeze) · die Render-Last gar nicht in `perfSense`. **Die Genese jeder Regression: ein Fix am SYMPTOM/Aufruf-Ort statt an der WURZEL/Chokepoint** — dann wächst ein neuer Pfad, der den Fix nicht erbt (Kopplung-ohne-Kohäsion eine Ebene tiefer). Die geniale Heilung (wie Aura `auraAt`/`deposit` = eine Lese-/Schreib-Quelle + der Renderer-Seam): den Invarianten IN den Chokepoint heben → kein Pfad KANN ihn umgehen, + ein struktureller Test am Chokepoint (nicht an N Aufrufern).
- „Noch nie passiert"-Stempel = `-Infinity`, NIE `0` (`now - 0 < cooldown` feuert fälschlich beim ersten Aufruf); Muster `map.has(k) ? map.get(k) : -Infinity`.
- `buildStateSnapshot`/`loadState` kopieren mit FESTEM Feld-Satz — ein neues persistentes Feld in BEIDEN (V8.59-Klasse: `portalMeta` fehlte → Portal verlor Ausrichtung); ein Welt-Katalog/`soul`-Feld zusätzlich im `signaling-server.js`-Handler.
- `state.p2p`/`state.vibePass` sind Laufzeit/global-localStorage — NIE im Welt-Snapshot (ein geteilter Schlüssel leckt Identität).
- Tag-Clamp [0,1] VOR den Stat-Formeln (`computePlayerStats`/`computeCreatureStats`; Tags >1 leben für Welt-Effekte weiter). Gekoppelte Felder über EINEN Setter (`_applyPlayerSpeed` = die eine Quelle für sprintSpeed=2×speed).

### DSL / Multi-User
- Eine stehende Welt-Regel ist ein nicht-verfallendes `when` (`rule`-Op REGISTRIERT, `_tickWorldRules` evaluiert) — vier Disziplinen: Re-Entrancy (Array-Länge am Tick-Anfang fangen), Performance (frozen `WORLD_RULES` static-field kein per-Frame-Getter, budgetPerFrame, allokationsfreie TTL-Compaction), Determinismus (`_worldRuleSeed` FNV-1a, kein `Math.random`; Regeln schreiben NUR die reaktive Schicht), Runaway (Cap 64 + Dedup + `_evictWorldRule`). Läuft durch denselben `dslRun` (Budget/Whitelist/CSP heilig).
- `NON_BROADCASTABLE_OPS`: `player_*`/`set_mode`/Inventar/Kreatur-Tasks/`set_time_of_day` gehen NIE über P2P (ein AST-Treffer überspringt den GANZEN Broadcast). Welt-Geometrie-Ops (`terrain_*`/`voxel_*`/`spawn_*`/`player_soul`) NICHT im `dslComposeAtomic`-Pool. Eingehende Remote-DSL durch dieselbe `dslRun`-Sandbox (Loop-Schutz dreifach). Ein neuer WS-Typ braucht einen EXPLIZITEN `signaling-server.js`-Handler (kein Fallthrough); `processChatCommand` lowercased nur `parts`.

### Test / Playtest
- **Headless-grün ≠ vision-aligned** — der FLUSS/die UI-Präsentation braucht das Schöpfer-Browser-Auge FRÜH (eine Mach-Logik kann korrekt sein + dennoch einen Fluss duplizieren/parallelisieren); fragen „vollendet das einen Pfad oder dupliziert es ihn?".
- **`window.AnazhRealm` ist UNDEFINED im Headless — die KLASSE ist `window.anazhRealm.constructor`** (V18.259, kostete 4 Tests): ein Test, der einen Static (`SPECIES_TAG_REFERENCE`/`WERTUNG`/`NON_BROADCASTABLE_OPS`) über `window.AnazhRealm.X` liest, bekommt `undefined` → `|| {}`/`|| null` → die Prüfung läuft auf leer („diffs undefined") und FÄLLT STILL. Muster: `(window.AnazhRealm || r.constructor).X` ODER direkt `window.anazhRealm.constructor.X`.
- **Eine spawnbare SPEZIES = stabile IDENTITÄT (Name·Tags) + variable GESTALT** (V18.259): schneidest du den statischen Bauplan einer named species (`baum_eiche`…), MUSS die Identität bleiben — der NAME ist load-bearing für ~6 Subsysteme (Crafting·Archetyp·Instancing·R3·HISM·Worldgen-Detektion), auch wenn die Geometrie hässlich war. Re-anker als grammatik-gewachsenen Built-in mit `_grownSpecies` (→ `computeCompoundTags` mischt die `SPECIES_TAG_VARIATION` ein). Tests, die auf die ALTE Gestalt kalibriert waren (fixe Part-Zahl `=7` · gemergte vs per-part Leaves · `0xc0392b`-Avatar-Rot · `genVersion>=4`-Schranke), ziehen der gewachsenen Wahrheit nach — KEIN Pflaster, die gemessene neue Realität.
- `setTimeout` in `page.evaluate()` yieldet headless nicht an rAF → evaluate beenden, AUSSEN warten, in 2. evaluate ablesen; Loop-Features synchron über `_gameLoopTick()` treiben. Headless-rAF ~1 Hz → Warmup COUNT-/PLATEAU-basiert pumpen (nicht Wall-Clock — last-empfindlich), `maxPoolSize=1` als Leak-Beweis.
- **„Last-Flake" ist die bequeme Diagnose — MISS zuerst:** ein Test, der Warmup-Zustand liest (statTags/Boosts/Position/Groundedness) ist meist KONFUNDIERT, kein CPU-Opfer → einen Probe (`diag-soul-jump`-Muster) bauen, dann auf INTENT heilen (Boosts leeren, deterministisch). Der `voxelChunks`-COUNT war last-empfindlich → count-/plateau-basierter Warmup half, aber V18.271 (Spieler-Chunk async) brachte die Wurzel zurück (s. nächste Zeile).
- **DER WARMUP BAUT WORKER-NULL/SYNC — nie an Async-Worker-Timing hängen (V18.273):** beide Gate-Tiers (`playtest.cjs` + `playtest-fast.cjs`) hängen `r.state.voxelWorker = null` für die Aufwärm-Schleife AUS → der Streaming-Tick baut jeden Ring-Chunk SYNC im Tick (`_acquireVoxelChunkBuild`-Stufe-3, bit-identisch via Determinismus-Wand), restore danach. WURZEL: V18.271 nahm dem Spieler-Chunk den `forceSync`-Anker (RICHTIG für den Produktions-Lauf-Freeze) — dadurch hing die Warmup-Welt an der ASYNC-Worker-Lieferung, die unter CPU-Last (CI, mehrere Runner) AUSGEHUNGERT wird → `voxelChunks=0` + 9 Folge-Fehler (derselbe Commit rot↔grün je nach Last — GEMESSEN: 2× rot unter Last, grün solo). Sync = load-unabhängig (kein Cross-Thread-Hunger). Die ASYNC-Produktions-Wahrheit prüft `checkBandV18271AsyncSoftFloor` separat — der Warmup braucht nur eine WARME, deterministische Welt. (Die ~60 `diag-*`-Skripte teilen das Muster, sind aber MANUELLE Werkzeuge, nicht CI-gated → nicht betroffen.)
- **DIE BOOT-READINESS WARTET GEDULDIG — `_gameLoopTick`, nicht eine 5-s-Magic-Number (V18.276, der LETZTE Last-Flake im Boot):** vor der Warmup-Schleife wartet der Gate auf das wahre „Welt bereit"-Signal `_gameLoopTick` (exponiert von `startEternalLoop` am Boot-Ende) — NICHT `rendererReady` (= nur „GPU-Device fertig", feuert beim Null-Renderer SOFORT, lange vor dem Loop). Der alte 5-s-Deckel (`Math.min(5000, durationMs)`) traf auf einem gedrosselten Container ZU FRÜH (GEMESSEN: `bootElapsed=5066ms hasTick=false terrainGen=false` — Worldgen lief noch) → der ehrliche Fallback griff → der Warmup-Pump lief NIE → `voxelChunks=0` + 9 Folge-Fehler, Page-Errors=0, terrainEverGenerated=true (DERSELBE Signatur wie der V18.273-Flake, aber eine Stufe FRÜHER — Boot-Wait statt Chunk-Build; auch SOLO rot, nicht nur unter Parallel-Last). FIX (beide Tiers): `BOOT_READY_CAP_MS = 60000` — ein reiner Sicherheits-Deckel gegen einen echten Hänger; die Schleife exitet, sobald `_gameLoopTick` da ist (normal Sekunden) → kostet den Schnellfall nichts, macht den Boot aber load-unabhängig (mit 60 s baut der Pump 81 Chunks). **DISZIPLIN (die Lehre): ein „bekannter Flake" ist erst die Diagnose, wenn die ZAHL ihn bestätigt — hier log der „solo nochmal"-Reflex; eine temporäre Warmup-Instrumentierung (`built`/`ticks`/`firstErr`/`bootElapsed`, danach sauber entfernt) zeigte die wahre Wurzel in EINEM Lauf. Page-Errors=0 schliesst einen per-Tick-Throw NICHT aus — der Warmup schluckt Tick-Fehler still (`firstErr` machte sie sichtbar).**
- **Der per-Frame-GPU-Render (`renderer.render`, swiftshader ~2.5s/Frame) war der dominante Playtest-Kosten** → chirurgisch stubben (NUR `renderer.render`, NICHT `_loopRender` — dessen Uniform-Updates sind load-bearing); Warmup PLATEAU-basiert. 214s→~93s. Bands messen (`diag-warmup-speed` A/B/C), nicht raten.
- Strukturelle Invarianten (`fn.toString()`-Greps) wandern bei einem Refactor mit (V9.56-i, `grep 'NAME.toString()' scripts/playtest.cjs` vor dem Schnitt); ein Test bei einer neuen Kopplung wandert/heilt an die Wurzel, wird NICHT aufgeweicht.
- **Absenz-Greps gegen `.toString()` lesen über `window.__codeOf(fn|src)` (EINE Quelle im Playtest, strippt Kommentare)** — sonst stolpert ein `!/X/.test(methodeSrc)`-Check über den erklärenden Kommentar, der den ENTFERNTEN Code zitiert. GEMESSEN: das volle Gate stand deterministisch ROT (2 Invarianten), weil der V18.267-`instanceColor`-Heilungs-Kommentar in `_scatterMaterial`/`_grassInstanceMat` den `attribute("instanceColor")`-Read WÖRTLICH zitierte, den derselbe Test als ABWESEND prüft („der Code darf das Wort tragen, der CODE darf es nicht"). Die 5 vormals verstreuten Inline-Strips sind auf `window.__codeOf` verdichtet; ein neuer Absenz-Grep fängt die Quelle mit `window.__codeOf(r._method)` statt `.toString()`.
- **Der Playtest TIMET sich selbst** (`timed`-Wrapper um JEDEN Band-Call, EINE Quelle viele Leser; die thematische Dispatch-Gruppierung bleibt) → der Schluss-Bericht listet die 20 teuersten Bänder. Bei „der Playtest hängt" ZUERST den Timing-Bericht lesen: GEMESSEN Pareto (Top 12 Bänder ≈ 55% der ~11-min-Band-Zeit; `checkBandVoxelP3AndInventory` allein 57s) — ein langes STILLES Band (schwere Welt-Mutation: Voxel-Chunk-Builds/BVH/Soul/Avatar, ~5s/`page.evaluate`) SIEHT aus wie ein Hang, ist aber keiner. (Löst die externe `diag-gate-cost` ab.)
- **DER GATE-LAUF KANN NICHT MEHR STILL STERBEN ODER EWIG HÄNGEN (V18.309, das Gesetz-#0-Beispiel):** derselbe `timed`-Chokepoint trägt jetzt die Fehler-Grenze (kein neuer Pfad, EIN Ort wie `auraAt`). Drei strukturelle Linsen lösen die zwei blinden Flecken, an denen ICH scheiterte (toter Lauf sah grün aus · Hänger schwieg 50 min): (1) `puppeteer.launch({ protocolTimeout: 240000 })` macht einen toten/hängenden `page.evaluate` zu einer FANGBAREN Rejection (>> das teuerste legitime Band ~57s, aber ENDLICH); (2) eine Per-Band-Try/Catch in `timed` verbucht einen Band-Throw als Invariante-Verletzung (Lauf läuft weiter → ALLE Fehler sichtbar, nicht nur der erste) und setzt bei tote-Seite-Signatur (`Execution context|Target closed|detached|Protocol error|timed out|Connection closed`) `gateAborted` → alle Folgebänder werden still übersprungen (kein Folge-Fehler-Flut); (3) ein 3-Wege-Schluss-Verdikt: `gateAborted` → `⛔ GATE UNVOLLSTÄNDIG bei Band X` + Exit≠0 (egal ob STRICT — ein unvollständiger Lauf ist NIE grün). **DISZIPLIN: lies das VERDIKT (die `✅ Alle Invarianten OK`/`⛔`/`❌`-Zeile), NIE den ✅-Zähler — ein gecrashter Lauf hat viele ✅ und KEIN grünes Verdikt.** Der Container kumuliert swiftshader-Last → der echte Renderer-Prozess stirbt im Schwanz (gemessen ~3300 von ~5340); das volle grüne Gate gehört auf die CI (frischer Prozess pro Lauf). Happy-Path bewiesen unverändert (3 Szenarien standalone: clean→grün/exit0 · Logik-Fehler→verbucht+weiter · tote-Seite→Abbruch+exit1).
- **Das Mechanik-Gate läuft GPU-FREI (Null-Renderer, Default):** kein Band liest je ein Pixel, die Rasterung war ohnehin gestubbt — der echte WebGPU-Kontext war reine swiftshader-Crash-Quelle (unter kumulativer Last stirbt der Renderer-Prozess → „detached Frame" → alle Folge-`page.evaluate` scheitern, SIEHT aus wie ein Hang). `window.__anazhHeadlessNullRenderer` → `_makeHeadlessRenderer` (No-op-Hülle OHNE GPU; deckt die schmale Renderer-API; `init()` resolvt). **Der Null-`render` macht die billige CPU-Buchhaltung (`scene.updateMatrixWorld`) — sonst verfehlt der echte Raycast/Grabe-Hieb das Terrain (MECHANIK).** PMREM-Env (LOOK) fällt auf die schlichte DataTexture zurück. Der LOOK lebt getrennt in `diag-settled-view` (echtes WebGPU, settled, Augenhöhe) — „MECHANIK braucht eine ZAHL, LOOK braucht ein BILD". Opt-out `PLAYTEST_REAL_RENDERER=1` (echter Renderer + End-Screenshot). GEMESSEN: 684s→442s, unzerstörbar (~5340 Invarianten grün GPU-frei).
- **Welt-bereit = `_gameLoopTick` ist eine Funktion (Boot durch `startEternalLoop`), NICHT `rendererReady`** (= nur „GPU-Device fertig", via `renderer.init().then()`): beim echten Renderer fallen beide zeitlich zusammen (GPU-init langsam), beim Null-Renderer resolvt `init()` SOFORT → `rendererReady` feuert VOR `startEternalLoop` → ein Test, der auf `rendererReady` allein wartet, liest eine halb-gebootete Welt (`_gameLoopTick` fehlt, 0 Chunks). Auf den Loop warten ist renderer-unabhängig korrekt.
- **Ein render-pfad-abhängiger Mechanik-Check heilt an der WURZEL, nicht per Skip:** der ECHTE Ammo-Raycast/Grabe-Hieb (W6.G P4) braucht (a) die Spieler-Chunk-Kollisions-BVH gebaut + (b) den Spieler auf gesetzter Oberfläche — beides liefert ein kurzer `_gameLoopTick`-Pump VOR dem Raycast (Schwerkraft settelt + `_ensurePlayerChunkBVH` im Loop baut). Heile auf INTENT (die Grabe-MECHANIK), nicht „Last-Flake".
- **Verifiziere KONSUM, nicht Existenz (Passagier-Trugschluss):** jede neue Achse/Uniform/Hook braucht eine Invariante, die einen ECHTEN LESER nachweist (Source-Probe + behavioral), NIE nur „das Feld ist da"; ein `uniform()` ohne `.value =`-Zuweisung ist ein stummer Haken.
- **`audit:strict`-Whitelist mit dem Code SYNCHRON halten — sonst verliert die Wache ihr Signal (V18.272):** ein neues lazy-init State-Feld (`if (!state.X) state.X = new Map()`) ODER ein undefined-sicheres Flag/Sentinel/Scratch, das NICHT in `init()` steht, MUSS in die `audit-strict.cjs`-Whitelist (mit Begründung) — sonst flaggt der State-Field-Audit es als „nicht in init()". GEMESSEN: die Wasser-CA- (V18.84–.94) + Perf- (V18.260–.271) + Scatter/Worldgen-Bögen ließen ~32 Felder abdriften → 72 ❌, eine dauerhaft-rote Wache, in der der EINE echte read-before-init ertränke. Vor dem Whitelisten JEDES Feld am LESE-Ort prüfen (guarded/undefined-sicher?), nicht blind eintragen (`_skyEnvFailed` wird via `st`-Alias gesetzt — `this.state.X =`-Grep allein lügt). audit:strict steht NICHT im CI-Gate (nur `check` + `playtest`) → die Drift wächst unbemerkt; im pre-push-Audit mitlaufen lassen. **audit:strict hat seit V18.274 DREI Schichten** (State-Field · CSV-Var · Soft-Default) — der vierte „Public-Method-Smoke" ist GESCHNITTEN (s. nächste Zeile).
- **`audit:strict` prüft INIT-PRÄSENZ, nicht KONSUM — ein Feld kann grün UND tot sein (V18.285):** die State-Wache prüft nur, ob ein Feld in `init()` STEHT, nie ob es noch INFORMATION trägt → ein Regler, der ins Leere schreibt (`maxLoadedChunks`), ein nie-gefüllter Container, ein null-Feld mit nur falsy-Fallback-Lesern (`minHeight || -50`) bleiben grün. `scripts/diag-sediment.cjs` schliesst die Lücke (kommentar-/string-bereinigt — die `groundHeightField`-Falle: seine „Leser" waren Kommentare; klassifiziert jede `state.X`-Ref nach contentWrite/realRead/fallbackRead + flaggt 0-Aufrufer-Methoden). DISZIPLIN: das Werkzeug ist ein Kandidaten-FLAGGER, kein Orakel — vor JEDEM Schnitt gegen `scripts/` verifizieren (der Playtest liest `r.state.X` — ein `this.state`-only-Grep übersieht es, so wurde `_cameraDesiredY` fälschlich „tot" geflaggt, obwohl der Pitch-Test ihn liest); GEMESSEN: 7 der 18 V18.285-Kandidaten waren KEINE Passagiere (6 Migrations-Wächter, deren Wert das Gate asserted: `groundHeightField===null`/`minHeight===0`/`terrainMaterial===null`/`architectureCullingTickHz===2.0` · 1 lebender Test-Hook), erst die Verifikation fing es.
- **Ein Test, dessen PRÄMISSE unsound ist, ist ein Passagier — SCHNEIDEN, nicht endlos patchen (V18.274):** der alte audit:strict-„Public-Method-Smoke" (jede non-`_`-Methode blind ohne Args aufrufen) war unheilbar flacky, WEIL die Prämisse für diesen Codebase unsound ist: ~20 Methoden (Worldgen/Avatar/Skybox/Worker) EXEKUTIEREN beim Blind-Call massive async-Arbeit → Event-Loop-Hunger + Page-Overload (`Target closed`). Acht Heil-Varianten (protocolTimeout · Null-Renderer · Toleranz · Isolation · await · Yield · Zeitbudget · Heavy-Skip) scheiterten ALLE an der Wurzel; der Test fand zudem NUR False-Positives (args-brauchende Methoden), 0 echte Bugs. **GESCHNITTEN** statt gepatcht → der Audit ist 13 s clean. DISZIPLIN: misst ein „Test" das Falsche an der Wurzel (unsound, nur False-Positives, nicht CI-gated), ist Patchen das Wuchern — schneide den Passagier, die echte Wahrheit trägt das richtige Gate (hier: der Playtest übt die Methoden in echten Szenarien). Re-add ihn NICHT.
- Eine Welt-Kopplungs-Probe misst den ROHEN berechneten Wert (`_dayNightComputeTint`), nicht den LIVE applizierten Uniform (von Blends/Welt-Zustand verdünnt → flaky); deterministisch machen, nicht Schwelle senken.
- Cap-vs-Worldgen-Variance: eine state-Liste isolieren (Snapshot/ersetzen/restoren) vor einer cap'enden Funktion; Maximal-Wert messen. Substanz-Spawns (Bäume/Felsen) NICHT terrain-gaten (verhungern im Warmup) — Dichte steuert die Kosten; nur reine Deko verträgt das `if(!chunksBuilt)`-Gate.
- REMOTE-CONTAINER: `ps`/`pgrep` über Bash-Aufrufe unzuverlässig (PID-Namespaces) + node-stdout block-gepuffert → auf LOG-MARKER + Artefakt-mtimes warten (until-grep), nie Prozess-Listen. Diag-Härtung: eigener `DIAG_PORT` (4317) + inkrementelles JSON-Artefakt + Warm-Welt-Cache. `pkill -f playtest` separat (killt sonst die Shell).

### Rendering · TSL / WebGPU
- **TERRAIN-ROUGHNESS ist ein SUBSTANZ-Hebel, kein eigener Pfad (V18.335):** das Terrain-Material (`_getVoxelChunkMaterial`) holt Albedo UND Rauheit aus `_substanceCharacter` (via `_terrainGeologyAlbedo`, das den `roughNode` über einen OUT-Param `roughOut` zurückreicht — KEIN neuer Rückgabe-Typ, sonst brechen die Ω-OPSIS-S1-Tests/`ALBEDO_SENTINEL`); uniforme Rauheit = ein toter Plastik-Sheen unterm Sky-IBL (auf dunklem Vulkan-Boden am krassesten). Eine neue Boden-Oberflächen-Eigenschaft (Glanz/Feuchte/Moos-Mattheit) gehört in den EINEN Kern als Param (`mossW` matt · `wetDrive` glänzt — beide lawful aus dem damp/green-Feld), NICHT als zweiter Terrain-Material-Zweig. Werke/Rinde bleiben unberührt (ihr `mossW`≈0, `wetDrive`=null; die Roughness-Tests prüfen den SKALAR `userData.pbrRoughness`, nicht den Node).
- **TERRAIN-MIKRO-RELIEF ist ein BUMP auf der GEFLATTETEN Normale, nur im Terrain-Zweig (V18.336):** der transformative Hebel für den flachen Low-Poly-Boden (`TERRAIN_BUMP` + der Bump-Block im `vertexColors && !useFlexAttr`-Zweig von `_buildPbrNodeMaterial`) — ein zwei-oktaviger `mx_noise_float`-Höhengradient kippt die geflattete Normale (`(-dh/dx,0,-dh/dz)`), LOD-gegated (nah voll, fern glatt → kein Aliasing). **NORMAL-LESER-AUDIT ist Pflicht vor jeder Normal-Änderung:** der EINZIGE Leser dieser Normale ist die PBR-Lichtung — die Substanz-Response liest die GEOMETRIE-`normalWorld` separat, das `feld`-Profil hat `micro:0`/`rim:0` (AO/Rim feuern für Terrain nicht), die Schatten-MAP rendert aus der Geometrie → kein Konflikt. Vegetation behält ihre echte Normale (der Zweig schließt `useFlexAttr` aus). `strength`/`strength2` sind die Balance-Knöpfe; nur statische Nodes ans einmal-gebaute Material → kein GPU-Churn.
- **Um das TERRAIN zu SEHEN: ein LEICHTER Shot an NACHGEWIESEN-Land über Wasser (V18.335/.336)** — der volle `diag-settled-view` stirbt am swiftshader-Kumulativ-Last-Tod dieses Containers (ProtocolError-Timeout im Schwanz, NICHT der Code). Der Profi-Weg: kleine Welt (~20 Chunks, Stream bei `sz≥20` stoppen) · kleineres Viewport · `protocolTimeout: 600000` · 1–2 Frames. **DER STANDORT IST DIE HALBE WAHRHEIT (zwei Schöpfer-Korrekturen, beide meine Fehldeutung):** (1) den Spieler von der SPAWN-`start_plattform` (5 m über Grund) auf echtes Terrain teleportieren — sonst zeigt der Steil-nach-unten-Blick die flache PLATTFORM, nicht den Boden; (2) `getTerrainHeightAt` liefert den SOLIDEN Boden (= Seeboden) → ein blinder Teleport landet UNTER WASSER (der Wasser-Shader verfälscht das Urteil) → einen flachen LAND-Spot SCANNEN (`y − _waterLevelAt ≥ 4`, niedriger slope, moderate Höhe = kein Hazy-Vista) + `eyeAboveWater`/`slope` LOGGEN. Der Boden-Richter ist der Steil-nach-unten-Blick (Augenhöhe verbirgt Relief/Roughness hinter Grazing-Fresnel + LOD-Fade); ein Substanz-A/B braucht den IDENTISCHEN deterministischen Spot (Toggle via Konstante).
- **VEGETATIONS-LAUB = eine Karte, die ein aus ECHTER Blatt-Geometrie GEBACKENES Büschel zeigt (LAAS-Methode, `_ensureFoliageClusterAtlas`)** — NIE eine flache Farb-Karte/runde Maske; eine prozedural gebackene Textur aus generierter Geometrie ist §3.7-konform. Bei „X sieht fake aus, LAAS hat Tiefe" die ECHTE Quelle holen + die METHODE anwenden, nicht die eigene Heuristik tunen.
- **DER SKIN-GUSS IST GECACHT + RES-GETUNT — nie wieder synchron-blockierend bauen (V18.283):** `_buildCreatureSkinGeometry` ist die teuerste Op im Spiel (128³-Isosurface × ~72 Teile + Taubin = **29 s headless** für den Avatar). Sie ist DETERMINISTISCH → memoisiert (`_skinGeomCache`, Signatur `JSON.stringify(parts)+opts` → Klon, LRU-Cap 16): Wiederbau 28421→18,9 ms (verifiziert). Die Avatar-Auflösung ist EIN Tunable (`AnazhRealm.AVATAR_SKIN_RES`=96, `g.res` übersteuert pro Bau); 96 ist look-verifiziert ≈ 128 im engen Close-up (64 verwäscht die Anatomie). **DISZIPLIN: ein neuer Aufrufer von `_buildCreatureSkinGeometry` MUSS denselben parts+opts geben (sonst Cache-Miss = 29 s); eine res-Erhöhung kostet N³; der Erst-Bau pro Signatur bleibt synchron (~½–1 s real bei res 96) → für zero-hitch async-inkrementell (Coroutine/Worker), NIE den Bau ungecacht/synchron in einen per-Frame- oder Spawn-Pfad hängen.** Gehört zur EINEN Bug-Klasse des Freeze-Bogens: schwere Arbeit synchron auf dem Main-Thread.
- **DER SKIN-GUSS IST RÄUMLICH AKZELERIERT — byte-exakt beschleunigt, kein Stutzen (V18.319):** der Isosurface-Bau (`bake-core`) war brute O(G³·Knochen) — res-96-Avatar ~14 s (270 Teile × ~912k Gitterpunkte ≈ 65 M SDF-Auswertungen, die allermeisten fern der Oberfläche = pure Verschwendung). Jetzt trägt ein grobes „bone grid" (OpenVDB/narrow-band-Stil, `NC≈N/3`, gedeckelt) pro Zelle NUR die lokal beitragenden Knochen ein; `field()` iteriert die Zell-Liste statt aller Knochen → **Avatar 4,4×, BYTE-IDENTISCH** (GEMESSEN `diag-bake-bench`: accel == brute, maxDiff EXAKT 0). Byte-exakt, weil ein ausgelassener Knochen für JEDEN Punkt der Zelle beweisbar `smin-h=1` (smin=d, Beitrag 0) bzw. Displace-`tt=0` erfüllt — garantiert durch den Rand-Margin `Tsmin=(Lsmin+1)·Zelldiagonale + kMax + (DISPLACE?dispCap:0)`. **DREI Disziplinen (jede gemessen-erzwungen): (1) bei DISPLACE liegt die Iso bei `d∈[0,dispCap]` (nach außen geschoben), NICHT `d≈0` → der Margin MUSS `dispCap` addieren (sonst 1e-7-Versatz am Avatar). (2) Zu feine Zellen (N/2) lassen den absoluten Margin kollabieren → echter 1e-3-Versatz (die Klippe) → `NC=N/3` ist der gemessene Schnellpunkt mit Puffer. (3) `field()` NACH dem Gitter-Bau definieren (stabiles `__cg`) + leere Zellen = geteiltes EMPTY-Array → der nicht-akzelerierte Kreatur-Pfad (gate `bones≥64`) bleibt monomorph/regressionsfrei.** Der Fehlermodus ist GRAZIÖS (sub-Mikron-Versatz, nie Loch/Crash). Lens: `diag-bake-bench` (selbst-tragend via `opts.__noAccel`), `diag-skin-dump` (Fixture); Knöpfe `opts.accelL/accelNC` für künftiges Tuning. DISZIPLIN: ein neuer Bake-Aufrufer mit ≥64 Knochen akzeleriert automatisch — eine Margin-Änderung IMMER gegen `diag-bake-bench` (maxDiff muss 0 bleiben).
- **Körper-Blob-Wurzel = fehlende Muskel-DYNAMIK, NICHT die Glättung** (gemessen `diag-koerper-tiefpass`, 17.06.): die geteilte Metaball-Pipeline (`_buildCreatureSkinGeometry`: smin-k + 6× Taubin) bewahrt das Makro-Relief ~100 % (Reh-Rücken-Welle/Humanoid-Taille stehen pre+post Taubin) — ein blindes Senken von Taubin/k erodiert die dünnen Glieder OHNE den Blob zu heilen. Der „Rakete/Schlauch"-Look kommt vom Muskel-Massing (Hinterhand/Schulter/Hals ∝ Gang-Drehmoment) + der Bein-Asymmetrie, die `_creatureSkeleton.buildLeg` heute fehlen (alle vier Beine teilen EINEN `legR`, `frontR==rearR`). Vor einem Glättungs-„Fix" die Wand fragen (`opts.taubinPasses/taubinLambda/taubinMu/kMax`, Defaults = Live-Verhalten). **LANDMARK-SCHÄRFE (BEDINGUNG ii):** ein `part.kScale<1` blendet den smin tight (Knochen-Kante) — ABER zu tight (k unter ~0.04) RISST die Haut: das Fresnel-Warm-Rim der Haut (`rimAmt`) leckt rot in den Spalt → „Wund"-Look (GEMESSEN). Floor 0.04 + Muskel-Massen + die GRAT-AO (`+ridge`, der Sicht-Multiplikator) statt scharfer Cracks. Das Skinning des Avatar-Rigs ist SPATIAL (nächste Bone-Segmente), kein Part-Index → neue Muskel-Parts in `_humanoidSkeleton` sind rig-sicher.
- **Begehbare/hohle Struktur = EMERGENT aus PER-PART-Kollision** (`_buildArchitectureCollision` ein Box-Shape pro gerendertem Teil, NIE Voll-AABB); eine Tür-LÜCKE = kein Part → man geht hindurch. Vor „begehbar" die Kollisions-Wahrheit MESSEN. Walm-Dach: Basis ≈ √2·Spannweite, 45°-Drehung.
- **Eine FORM-Vergrößerung kann eine FORM-Affordance (`balancing`) auslösen OHNE die Tags zu ändern** — für ein „kein-Affordance"-Kontroll-Compound einen garantiert-leeren nehmen (`stein_block`), nicht eine größen-driftende Struktur.
- **Ein reference-first DETAIL gehört in EINE custom Geometrie (parametrisch), NIE in N Deko-Parts** (`flutedColumn`/`bladeProfile`); ein neuer Shape koppelt an 3 Stellen (`_makePartGeometry`-switch · `validateBlueprintParts`-Whitelist+Feld-Bewahrung [Built-ins bypassen] · die Form-Leser `SPATIAL_POINTED_SHAPES`). Ω-Φ3-b-Wand-Treue: eine breite tragende Wand ist keine freie Euler-Säule (knickt nicht).
- **Wenn eine GERECHNETE Größe sichtbar werden soll = die Vereinigung SEIN→ANBLICK** (Ω-W3 Werkstatt-Spiegel): Verdikt bei Bau-/Edit-Zeit cachen (NIE pro Frame), der Leser liest eine ECHTE Quelle (`_loadPath.floatingParts`), KONSUM via Source-Probe (flake-frei), MECHANIK = Zahl + LOOK = Bild (augen-bound).
- **WIND-SWAY = EINE Quelle `_windSwayOffset` (V18.310, Gesetz #0 — Raptor):** die Feld-Wind-Mathematik (`phase = uWindTime·1.7 + worldX·0.28 + worldZ·0.21`, die wandernde `gust`-Welle, `offsetZ`) lebt in EINER Funktion; das Gras (`_grassInstanceMat`) + die weiche Streu-Vegetation (`_applyScatterMotion`) LESEN sie, der per-System-Charakter ist Parameter (`ampX` Gras 1.5 / Streu 1.2 · `windScale` Wipfel-Dämpfen). Eine neue wind-bewegte Vegetation liest dieselbe Quelle, kein dritter Mathe-Baum (sonst driftet er wie 1.5↔1.2). Die Tree-Crown-Sway (`_applyVegetationResponse`, `phase·1.1`, crown-flex²+Hochfrequenz-Flutter, KEIN gust) ist ein ANDERES physikalisches Modell — bewusst getrennt (kein Verschmelzen verschiedener Dinge). Lens: `checkBandLambda…` source-probt `gustWave` (beide rufen `_windSwayOffset`) + `windCollapsed` (gust NUR in der einen Quelle). Wind-LOOK = bewegter Render → Schöpfer-Browser-Wort.
- **PBR-Metalle render SCHWARZ ohne `scene.environment`** → prozedurale Equirekt-`DataTexture` als Env (`_ensureSkyEnvironment`, WebGPU-native; `PMREMGenerator.fromScene` ist WebGPU-INKOMPATIBEL in r184); feature-detected + try/catch, gedrosselt.
- **`scene.environment` IDENTITÄTS-STABIL + RATE-GEDECKELT halten — ein pro-Frame wechselndes Env ist ein WebGPU-Pipeline-Recompile-CASCADE (V18.322, der periodische Idle-Freeze):** `_ensureSkyEnvironment` läuft per Frame (über `_applyDayNightToScene`); die Tag-Nacht-Uhr driftet die Himmelsfarbe auch IM STEHEN → bei jeder Drift >0.04 allokierte der alte Code via `fromEquirectangular(equirekt)` ein NEUES PMREM-Target = ein NEUES `scene.environment`-Texture-Objekt → WebGPU rekompilierte SYNCHRON die Pipelines ALLER env-samplenden PBR-Materialien (Multi-Sekunden-Stall, „2 s Bild / 10 s Freeze"). FIX: in das BESTEHENDE Target rendern (`fromEquirectangular(equirekt, st._skyEnvRT)` → `rt || _allocateTarget()`, also Wiederverwendung) → stabile Texture-Identität, `scene.environment` nur EINMAL gesetzt; PLUS ein Wall-Clock-Mindestabstand (`_skyEnvLastRegenMs`, `SKY_ENV_REGEN_MIN_MS` = 6 s seit V18.324) gegen die Drift-Rate. **Der Null-Renderer (headless) ÜBERSPRINGT den PMREM-Pfad (`_isHeadlessNull`-early-return) → das Gate sah den Freeze NIE** (33 Wellen-blinder-Fleck); die Linse ist der ECHTE Renderer + ein Objekt-`===`-Check (`diag-skyenv-identity`, hardware-unabhängig), die Drift-Rate misst `diag-skyenv-freq`. DISZIPLIN: jede pro-Frame neu allokierte GPU-Render-Ressource (Env/RT/Material) gehört identitäts-stabil + rate-gedeckelt — sonst zahlt jeder Frame einen Pipeline-Recompile.
- **DIE STEHENDE LINSE FÄNGT DIE GANZE KLASSE — Steady-State-Pipeline-Churn macht den CI-Build ROT (V18.324, Gesetz #0 — die strukturelle Heilung der V18.322-Blindheit):** der scene.environment-Bug überlebte 50+ Versionen, WEIL der Null-Renderer-Gate ihn STRUKTURELL nicht sehen konnte (er stubt den GPU-Pfad), und das Projekt seine bekannte GPU-Blindheit mit Wachsamkeit statt einer Linse deckte (die unter dem 33-Wellen-Firefight brach). `npm run gpu-lens` (`scripts/diag-idle-gpu-churn.cjs`, in der CI nach dem Playtest) fährt den ECHTEN Renderer (swiftshader → WebGL2-Fallback) und zählt `createRenderPipeline`/`linkProgram`: nach Warmup MUSS eine Env-Regenerierung **0** Pipelines neu kompilieren (alt ~270/Regen → ❌, gefixt 0 → ✅) + reines Idle ≤8. Der `warmupCompiles>0`-Guard verhindert blind-grün (der Zähler MUSS greifen). DISZIPLIN: (a) ein neuer per-Frame/periodischer GPU-Op (Env · Material-Variante · RT) gehört gegen `gpu-lens` geprüft (0 Recompiles im Steady-State); (b) eine BEKANNTE Blindheit des Gates (Null-Renderer stubt den GPU-Pfad) ist eine stehende Schuld → schließe sie mit einer stehenden Linse auf dem ECHTEN Renderer, NICHT mit „nächstes Mal dran denken"; (c) überlebt dasselbe Symptom N Fixes, ist das Problem die LINSE, nicht der nächste Fix — wechsle von fixen zu linse-bauen-außerhalb-des-blinden-Gates.
- **Eine ROLE_SIGNATURES-Änderung ändert Spektrum/Fit/Chip, NICHT die Live-Rolle:** Built-ins domain-fest (`computeBlueprintRole`→`computeBlueprintDomain`), user via `_computeFormRole`→`FORM_ROLE_SIGNATURES` (andere Tabelle); ein A/B läuft den ECHTEN Klassifikator, nicht `_blueprintRoleSpectrum[0]`. `_rollableFraction` = ROLL-Geometrie (horizontaler Zylinder), nicht Rundheit.
- **Ω-PHYSIS ist reine Berechnung → headless VOLL verifizierbar** (gegen Handrechnung, `diag-physis`); SSF = Kanten-Abstand / SCHWERPUNKT-Höhe (nicht „CoM zentriert"); SEIN headless-beweisbar, ANBLICK augen-bound (nicht verwechseln).
- **Die Trapeze-Wurzel ist die FACETTEN-Lichtung** (directional Sonne × hochfrequente Mesh-Normalen → bimodales `dot(N,L)`), NIE die Albedo (`diag-facets`) → heile via Shading-Normale-zur-Up (`normalNode = mix(normalWorld, up, terrainFlatten)`, render-only, KEIN eps/Schatten-Konflikt); die Kavitäts-AO (`fwidth`) MUSS DIESELBE geflattete Normale lesen (sonst view-abhängiger Dunkel-Term); jeder Normal-Leser (AO/Schatten-Bias/Hemisphere) ist zu auditieren.
- **Ein „eps/Glättung half nichts" MISST man gegen die ROUGHNESS-WELLENLÄNGE** (eps ≈ λ/2); wer den Normalen-eps weitet, zieht den Band-Skip-Margin mit (sonst Gipfel-Normalen kippen, `diag-normals`).
- **Himmelskörper KAMERA-RELATIV** (`position = camera + Offset`), NIE Welt-Ursprung-Orbit (sonst rasen sie neben dem fernen Spieler + Wasser-Glitzer divergiert von `sunDir`); `_followCelestialBodies` per Frame; Tests lesen `userData.skyOffset.y`.
- **Die ATMOSPHÄRE ist EINE geteilte Funktion, alle opaken Ebenen rufen sie IDENTISCH POST-lighting** (`_applyAerialOutput`/`_applySubstanceResponse`); Aerial-Perspektive POST-lighting (auf `TSL.output`), `scene.fog` trägt die DISTANZ, der Höhen-Melt EYE-RELATIV (`positionWorld.y − cameraPosition.y`, nicht absolute Welt-Höhe); Mikro-Tiefe/AO output-seitig (`output.xyz.mul(shade)`), NIE colorNode (überschreibt `material.color`).
- Stable-Shadow-Texel-Snap in LIGHT-SPACE (⟂ sunDir), nicht Welt-X/Z (`diag-shadow-snap`); `shadow.normalBias` koppelt an die Normale (eps-Änderung ändert Schatten-Qualität).
- **Deko-Detail MUSS mit der GRÖSSE skalieren — miss die Render-Last per Art** (`diag-render-load` `hismByPrefix`, V18.266): ein winziger `kiesel` (0.5 m) war mit 3 noiserock-Parts × 80 Dreiecke (240) der GRÖSSTE view-unabhängige HISM-Posten (589k > jede Baum-Art) — Deko kostete mehr als der Held. `noiserock` det skaliert jetzt mit `_maxDim` (`<0.7 m → det 0`, sonst 1; explizites `noiseDetail` = Override). Bei „freezt" ZUERST `diag-render-load` nach Art aufschlüsseln — der grösste Posten ist oft über-tesselliertes Klein-Deko, nicht der Held-Content.
- **DREHEN HÄNGT = GPU-RENDER, NICHT JS — global-gekeyte Laub-Gruppen cullen NIE (V18.300):** Drehen ist reine GPU (kein Streaming/JS) → hängt es, zeichnet die GPU die ganze Welt egal wohin man schaut. Die Streu-Laub-Gruppen (`_archInstanceGroupFor`) waren GLOBAL pro (Art#Leaf) → Instanzen welt-verteilt → `frustumCulled=false` (Group-BBox nutzlos) → Umsehen cullt NIE = der „kann mich kaum drehen"-Freeze (universell, in jeder Welt). FIX: die STREU-Gruppen pro 256-m-Region keyen (`name#leaf@regX,regZ`, `_archInstanceGroupFor(...,regionKey)`, Flag `state.useRegionFoliageCull` Default an) → jede Gruppe trägt EINE Region → instanz-bewusste Bounding-Sphere LOKAL → `frustumCulled=true` lässt die Engine die Regionen hinter dem Blick wegcullen. Placed Architektur (`_archInstanceAdd`, kein regionKey) bleibt global/unverändert. Region-privat → `_disposeScatterRegion` entsorgt die Gruppe GANZ (`_disposeArchInstanceGroup`), KEIN per-Slot-Null-Skalieren (das die Sphere zum Ursprung aufbliese); `region.regional` = Bau-Schnappschuss (Laufzeit-Toggle sauber). **GEMESSEN (`diag-turn-cull.cjs`): Umsehen cullt Ø 60 % der Laub-Last (vorher 0 %).** Three.js cullt InstancedMesh per INSTANZ-bewusster `boundingSphere` (nicht der Geometrie-Sphere) — darum wirkt das Region-Keying. DISZIPLIN: bei „freezt beim DREHEN" ZUERST die GPU-Render-Last (headless unsichtbar — Render gestubt!), eine `frustumCulled=false`-InstancedMesh ist eine permanente Voll-Render-Last.
- **DIE DOMINANTE RENDER-LAST WAR DAS GRAS — UND SIE STAND AUSSERHALB DES REGLERS (V18.307, Schöpfer „studiere, nicht headless, stelle die Synergie wieder her"):** zwei Lehren in einer. (a) **Eine Render-Kategorie kann den Schuldigen VERSTECKEN:** `diag-render-load` zeigte „Scatter 2.14M / 56k inst" — aber `diag-scatter-species` (der per-System/per-Art-Aufbruch) bewies, dass `voxelChunkScatter` (die kleine Streu) **0** Instanzen trug; die 2,12 M Tris waren das **GRAS** (`_buildVoxelChunkGrass`, 53 k Halm-Büschel × 40 Tris = 5 Blätter × SEG 4 × 2) = **83 % der Szene**. Vor dem Optimieren die Last auf die ECHTE Quelle aufbrechen, nicht auf die Sammel-Kategorie (V18.266-Disziplin „miss pro Art", eine Stufe tiefer). (b) **Ein Kapazitäts-Regler ist nur so gut wie sein Hebel ans GRÖSSTE Subsystem:** der Halm-`count` las NIE `_foliageDensityScale` → das Gras (die 83 %) stand AUSSERHALB des einen PID → der Regler drosselte die regelbaren Reste (Streu → drab, Ring → klein), während der eigentliche Last-Träger ungebremst die GPU pinnte = „drab UND langsam zugleich" (der gemessene Synergie-Verlust). FIX: SEG 4→2 (Geometrie −50 %, look-erhaltend, Schöpfer-Browser) + das Gras liest `_foliageDensityScale` (Headless → 1, gate-treu). **DISZIPLIN: JEDES kapazitäts-skalierte Subsystem MUSS den Regler-Faktor lesen — ein dominanter Last-Träger ausserhalb der Regelschleife zwingt den Regler, alles ANDERE zu überdrosseln (drab), ohne die fps zu heilen (langsam).** Offen: Gras in `_tickFoliageThin` (stehende Welt, nicht nur Bau-Zeit).
- **Der Schatten-Pass ist ein ZWEITER Voll-Render** (gemessen 2.32M Dreiecke, JEDEN Frame beim Laufen — der V18.264-Cache hilft nur im Stand). **Schatten-DISTANZ (V18.265):** ferne Foliage (LOD1/LOD2, `grown_*_lod[12]`) wirft KEINEN Schatten (`_archGroupCastsShadow`, EINE Quelle, der LOD-Tick trägt's mit; Bauten + nahe LOD0 werfen weiter) → −44 % Schatten-Dreiecke, look-sicher (fern+fog). Ein `castShadow`-Flag MUSS durch `_archInstanceGroupGrow` mitgeführt werden (sonst kippt ein wachsender Fern-Baum zurück auf true).
- **EIN CACHE AN EINEM KONTINUIERLICH DRIFTENDEN SIGNAL IST TOT — die langsame Drift gehört auf den STALENESS-Pfad, der Bewegungs-Trigger nur auf den DISKRETEN Sprung (V18.305):** der V18.264-Schatten-Cache (`_loopShadowUpdate`, autoUpdate=false → nur neu rendern bei Spieler-/Sonnen-Bewegung) griff im STAND NIE — die `sunMoved`-Schwelle war `1e-5`, aber die Sonnen-Position (`focus + sunDir·200`, lightDist=200) driftet kontinuierlich mit der Tag-Nacht-Uhr (**GEMESSEN `diag-shadow-sun`: Δ²≈1.15e-3/Frame → 119/119 Frames über der Schwelle**) → `sunMoved` feuerte JEDEN Frame → der 518k-Dreieck-Schatten-Pass (ein zweiter Voll-Render) lief dauernd statt gecacht. FIX: Schwelle `1e-5` → `4.0`. Die kontinuierliche Drift akkumuliert über das `hardStale=30`-Fenster nur Δ²≈1.0 (< 4.0 → Cache greift, Staleness governt); `sunMoved` fängt jetzt NUR den diskreten Sprung (manueller Tageszeit-Wechsel, Δ²≥10³ → sofort aktuell); die schnelle Dämmerung (Δ² bis 9.5e-2/Frame) trippt alle ~7 Frame (adaptiv mehr Updates bei schnell wanderndem Schatten). **BEWIESEN (`diag-shadow-sun`, über `_loopShadowUpdate`-needsUpdate-Flips): Schatten-Renders im Stand 100 % → 3 %** (4/120, grösste Lücke 30) → ~97 % übersprungen. DISZIPLIN: bei einem Bewegungs-getriggerten Cache prüfen, ob das Trigger-Signal langsam DRIFTET (Sonne/Zeit/Wind) — dann defeatet die Drift den Cache; die Drift gehört auf die Staleness, der Trigger nur auf den Sprung.
- **`instanceColor` wird von Three.js AUTOMATISCH multipliziert** (`setupDiffuseColor`: `if(t.instanceColor){s=vInstanceColor.mul(s)}`) sobald `mesh.instanceColor` gesetzt ist — ein manuelles `albedo.mul(attribute("instanceColor"))` ist REDUNDANT (Doppel-Multiply) UND die Quelle des `instanceColor not found`-Fehlers (geteilte Geometrie trägt das Per-Mesh-Attribut nicht; COMPILE-Warning, EINMAL pro Pipeline — NICHT der Freeze). **GEHEILT V18.267:** die manuellen Reads in `_grassInstanceMat` + `_scatterMaterial` ENTFERNT → der Tint kommt über den nativen InstanceNode-Pfad (DERSELBE wie das Laub, EINE Konvention); `useInstanceTint`-Flag + `setColorAt` reichen, kein manuelles Attribut. Wer pro-Instanz-Tint braucht: NUR das `useInstanceTint`-Flag setzen, NIE `attribute("instanceColor")` lesen (Tests greppen jetzt auf die ABWESENHEIT des manuellen Reads).
- **Post-FX = `RenderPipeline`, NICHT `PostProcessing` (r184, V18.267):** r184 benannte `PostProcessing` → `RenderPipeline` um; der alte Name ist ein DEPRECATED Alias, der schon beim `typeof`-ZUGRIFF eine Konsolen-Warnung feuert + intern ShaderMaterial-basiert ist (→ „ShaderMaterial is not compatible"). Der Bootstrap (`vendor/three-bootstrap.js`) MUSS `RenderPipeline` ZUERST in `THREE` kopieren (NodeMaterial-basiert), den alten Namen nur als Fallback. **Der Bootstrap-`<script>` in `index.html` braucht den `?v=`-Cache-Buster** (er ist eine separat geladene versionierte Datei — ohne Buster serviert der Browser den stale Bootstrap, die „Fix verifiziert, Schöpfer sieht nichts"-Falle; bei jedem Bootstrap-Edit den `?v=` mitziehen).
- **Seit r164 rendern NodeMaterials NUR auf `WebGPURenderer`** — jeder sekundäre/Offscreen-Renderer (Bauplan-Preview) MUSS ein `WebGPURenderer` sein (+ `stencil=false` + async `init()` + `rendererReady`-Gate); ein roher `WebGLRenderer` rendert NodeMaterials NICHT (leer, kein Crash → zuerst den Renderer-Typ prüfen). Headless ist pixel-blind (WebGPU→WebGL2-Fallback beweist Konstruktion, nicht den LOOK).
- TSL-Idiome: uniform-Knoten `uniform(v)` haben `.value`-Setter, leben NICHT in `material.uniforms` (in `state.XUniforms` sammeln; Tests greppen `createX.toString()`); `discard` → `material.alphaTest`; `vertexColors:true` wirkt NICHT → `attribute("color","vec3")` explizit (auf WebGPU STRIKT — fehlt das Attribut = Crash, immer setzen); `positionNode`-Displacement → Lighting/Fog brauchen manuelle displaced World-Pos (`modelWorldMatrix.mul`); `cond(test,a,b)` statt `if`; custom Normale via `modelNormalMatrix.mul`. (Material-Pfad: **PBR `MeshStandardNodeMaterial` ist die EINE Wahrheit** — Toon/gradientMap ist gestrichen, V18.236.)
- WebGPU-Shadow-Pass braucht `scene.overrideMaterial` (minimal, leitet `positionNode` durch) + `castShadow`-Filter (`setRenderObjectFunction`); DepthTexture pure-Depth + `compareFunction=LessCompare` + `texture(t,uv).compare(refZ)` (Hardware-PCF); Custom-Lighting im colorNode (`state.XUniforms` + `_dayNightApply*`-Sync). `material.color.setRGB` schreibt LINEAR bei ColorManagement=true → Farb-Mathe über `THREE.Color` (`diag-avatar-color`).
- **Vendor-Bug durch UPGRADE heilen, nicht workaround'en, wenn die Heilung im Upgrade artikuliert ist;** nach dem Upgrade den Workaround GANZ streichen (Verifikation: das native Symbol instantiieren + Properties prüfen VOR dem Schnitt); WGSL-Parse-Errors triggern KEINEN Hot-Swap → fehlerhaftes Feature dauerhaft entfernen (konstante Alternative), nicht auf Fallback hoffen. Renderer-Hot-Swap: Canvas-Element austauschen (Context-Type ist permanent) + Loop migrieren (`setAnimationLoop`). Race nach 3+ Wellen → Mesh-Memory-Trade (nicht disposen) ist die Profi-Antwort; fremder-Code-Augen-Disziplin (`grep` die Crash-Größe).

### UI / Theme
- **„Auf X-Niveau" = den ZWECK des Raums verstehen + die ANORDNUNG wählen, die IHM dient** — NIE die Topologie eines Referenz-Raums abpausen (Werkstatt=erschaffen / Ich=verwalten / Hof=dirigieren / Bibliothek=entdecken+bewerten); das Muster ANWENDEN, nicht Pixel kopieren; nicht jeder Raum hat einen content-STAR (ein UTILITY-Raum bekommt Ordnung nach Häufigkeit + progressive disclosure); ein „Duplikat"-Befund MESSEN (was tut der Control WIRKLICH) → die ganze DOMÄNE heimziehen, nicht nur den Knopf.
- **EIN Control ist erst „fertig", wenn sein LOOP WIRKT** (eine Suche, die nichts treibt, ist die Nullnummer) — den MECHANISMUS des Referenz-Raums kopieren (search filtert/dimmt/handelt), den Loop SELBST fahren, die Invariante prüft KONSUM nicht Existenz, EINE Quelle pro Absicht.
- **ICH BIN NICHT PIXEL-BLIND — bei JEDER UI-Welle die GEOMETRIE MESSEN** (`getBoundingClientRect` vs Viewport an mehreren Größen, `scrollHeight>clientHeight`) + den Screenshot Zone-für-Zone gegen den Referenz-Raum LESEN + den Fit als Invariante encoden. Ein zentriertes Overlay ohne `max-height`+`box-sizing:border-box`+flex läuft aus dem Bild; Räume sind TALL columns (`min-height:0;overflow-y:auto` je Spalte), kein Boden-Streifen.
- Die Detail-Hebel als GETEILTE Bausteine: `--ink-on-dark`/`-soft` (lesbarer Text auf dunklem Grund) · `.help-pop` nach `document.body` PORTALIERT (CSS clippt nicht weg, wenn ein Vorfahr `transform` trägt) · explizites `grid` statt `column-count` (driftet) · primäre Aktion `position:sticky`. Ein Legacy-Inline-Style überschreibt STILL jede CSS-Regel (`el.getAttribute('style')` zuerst); Rects NACH der Transition messen.
- „Ich baue X, sehe es nie" ist oft ein UI-ORT-Bug (in ein eingeklapptes/`hidden`-Panel gerendert) — Container DEFAULT-sichtbar? schaut der Schöpfer DORT hin? (Browser-DOM-Dump trennt „existiert im Code" von „sichtbar"). Neue semantische CSS-Var als ALIAS auf die numerische (`var(--parch-1)`), in `:root` UND `[data-theme=nacht]` (Custom-Props resolven am Use-Site).

### Save-Server / CSP / Build / WASD / Multi-Welt
- Save-Server-POST nur localhost (CDN → stiller Skip, localStorage); `anazhRealmState.json` NICHT git-getrackt. CSP strict (`script-src 'self'`, kein eval); dokumentierte Konzessionen (Three `unsafe-inline` styles, `worker-src blob:`, LLM-Hosts; `wasm-unsafe-eval` war Ammo-only und kann nach einem WASM-Konsumenten-Audit gedroppt werden — V18.331).
- **JEDE separat geladene versionierte Datei braucht den `?v=`-Cache-Buster, nicht nur die Haupt-JS** (`new Worker("voxel-worker.js?v="+VERSION)` + `importScripts` erben den Cache) — Diagnose-Signatur „Fix verifiziert-korrekt ABER Schöpfer sieht keine Änderung" → ZUERST Caching/Pfad verdächtigen. `grep 'new Worker\|importScripts\|<script\|<link\|fetch('` für alle Ressourcen.
- WASD: `state.right` ist bei yaw=0 `+X`, aber player-RECHTS ist `−X` → A=`+right`, D=`−right` (nicht „intuitiv" invertieren, bricht beim asymmetrischen Drache).
- Multi-Welt: 3 localStorage-Keys (`anazhRealmWorlds` Index · `anazhRealmState_<id>` · `anazhRealmActiveWorld`); Welt-Wechsel reload-basiert (`{reload:false}` für Tests); Sub-Welt-Eigenschaften (`trust`/`serverMode`/`multiplayer`) reisen feldweise durch die Vendor-/Mesh-Kette (eine vergessen = Leck). `generateNewWorld` 30-s-Cooldown.
## Workflows

- **Der DEV-LOOP (Durchsatz):** `npm run playtest:fast` (~20 s, ~13 Kern-Gesundheits-Checks: Crash · Welt baut · Avatar/Kreatur/Bauplan baut · Resonanz/Physik sinnvoll) während des Iterierens; der VOLLE `npm run playtest` (~5340 Invarianten über 241 Bänder, ~9-10 min im Container) bleibt das MERGE-Gate. Nicht jede Änderung durch den 10-min-Gate zwingen — der Fast-Tier fängt ~80 % der Regressionen sofort.
- **Lokaler Audit (vor Merge):** `npm run check && npm run format:check && npm run lint && npm run playtest`
- **Stamm-Navigation:** `npm run atlas` druckt die LIVE-Karte der 26 Stamm-Zonen (Start/Zeilen/Methoden); `node scripts/diag-atlas.cjs --find <regex>` verortet eine Methode in ihrer Zone — VOR jeder Grep-Archäologie zuerst die Karte (W6, R-032). `--check` (Registry ↔ Marker-Drift) läuft im `npm run check`-Gate.
- **Web-Sessions:** ein SessionStart-Hook (`.claude/hooks/session-start.sh`, registriert in `.claude/settings.json`) ruft `npm install` — Lint/Format/Audit/Playtest sind in Claude-Code-on-the-web-Sessions sofort lauffähig (frischer Clone hat sonst kein `node_modules`).
- **Slash-Befehl:** `/audit` führt die Prüfung in jeder Claude-Session aus (`.claude/commands/audit.md`).
- **CI:** zwei parallele Jobs (`check` für statische Checks + `playtest` für Runtime-Invarianten) bei jedem Push.
- **Git:** kleine thematische Commits, `git push -u origin <branch>` mit exponential backoff bei Netzfehlern.

## Konventionen

- Keine emojis im Code/Commits außer auf expliziten Wunsch.
- Commits klein und thematisch.
- Keine Backwards-Compat-Layer für veralteten Code – sauber löschen.
- Pull Requests nur auf expliziten Wunsch des Users.
- **Vision treu bleiben**: jeder Vorschlag sollte die Heilige Lektion respektieren (keine Re-Komplexifizierung). Bei Zweifel: `docs/state-of-realm.md` §2 nachlesen.
- **Schöpfer-Rückmeldung: handeln, nicht horten (das W7-Erbe, ohne den Ledger)**: eine Rückmeldung ist ein SIGNAL für eine Wurzel — bau die Heilung, dann lass die DAUERHAFTE Lehre in die `## Wichtige Gotchas` und die GESCHICHTE (welche Welle, welcher Befund) in `docs/archiv/handover.md` fließen. **Kein „GEBAUT ✓" ohne Nachbau im NUTZER-ZUSTAND** (frieden · leeres Inventar · Konsole zu · 1366px) — headless-grün ist nicht der Nutzer. Ein noch unbestätigter Look gehört auf die nächste Schöpfer-Browser-Runde, ein offener Entscheid in `docs/roadmap.md` §4. **KEIN stehender Rückmeldungs-Korpus als eigenes Doc** — das war Blur; die zwei Heimaten (Gotchas + handover) tragen es schärfer (die `archiv/rueckmeldung.md`/`archiv/aktiv.md` sind der gefaltete Snapshot).
- **Stamm-Größengrenze (V9.44-Lehre)**: neue Logik in eine neue benannte Methode geben, nicht an einen bestehenden Kontrollfluss-Giganten anhängen. Der Stamm-Pflege-Bogen fand die Schmerzgrenze bei ~200 Zeilen — wächst eine Kontrollfluss-Methode darüber, ist das ein Schnitt-Signal (kleinere Methoden DERSELBEN Klasse), kein Modul-Signal. Sonst re-fettet der Stamm, und vertagte Struktur-Schuld wird teurer (V9.40-Lehre).
- **Line-Range-Delete eines „Clusters" → ALLE Methoden-Defs im Range auflisten, nicht nur die Pattern-Treffer (V17.20-Lehre, gemessen — der Schnitt brach die GANZE Welt)**: wer ein totes Subsystem schneidet, das per Pattern-Grep (z.B. `voxelGpu`) kartiert wurde, aber per **Line-Range** (`sed 'A,Bd'`) löscht, MUSS vorher `awk 'NR>=A && NR<=B && /^    (async )?[a-zA-Z_]/ {print NR": "$0}' file` laufen — ein per-Pattern-„Cluster" kann eine NICHT-passende Methode ENTHALTEN, die der Grep nie sieht. Konkret V17.20: der GPU-Density-Block (`sed 13567,14314d`) enthielt **`_configureRenderer`** (Nicht-GPU, konfiguriert den Haupt-Renderer) MITTEN zwischen `_voxelGpuDispose` und `_voxelGpuEnsureDensityPipeline` → mit-gelöscht, vom `voxelGpu`-Grep nie geflaggt → Page-Error `_configureRenderer is not a function` → `terrainEverGenerated=false`, die ganze Welt tot. `node --check` fängt das NICHT (Syntax bleibt valide). **Disziplin: (1) `cp file /tmp/backup` VOR jeder großen sed-Chirurgie (der Rettungsanker); (2) `awk` alle Methoden-Defs im Range listen + jede gegen das Schnitt-Kriterium prüfen; (3) nach dem Schnitt `node scripts/diag-page-error.cjs` (fängt Runtime-`is not a function` an der Wurzel, bevor der Playtest in Folge-Fehlern ertrinkt) DANN Playtest.** Plus: bei „wir schneiden totes X" ZUERST fragen (V17.20-Schöpfer-Frage) — wurde X durch etwas Höheres ERSETZT (→ tot, Hülle) oder ist es bewusst aufbewahrte Infra (→ Koma, nicht schneiden ohne Entscheidung)? Pro Kandidat 0-Aufrufer-Grep + „was ersetzte es?" beantworten. **V18-Vertiefung (GEMESSEN — der Schöpfer stoppte einen „alle 18 toten Methoden raus"-Plan mit „schneidest du etwas, das nie tiefer/synergetischer ersetzt wurde?"): von 18 „toten" Methoden waren nur ~9 echtes Sediment — die andere Hälfte war SAAT (bewusst ruhende Vision-Fäden, jeder mit der Begründung im EIGENEN Kommentar: `_fieldWohlErlebt` „sonst Runaway, gemessen", `_archInstanceUpdate` „für künftige bewegliche Instanzen", `_drainPools` Leak-Schutz). Ein blinder „tot = raus"-Schnitt hätte das dritte Verb der Vision (WERTEN) beschädigt. DISZIPLIN: (a) der „nie tiefer/synergetischer ersetzt → BEHALTEN"-Test ist die Wand — schneide NUR, was VERIFIZIERT durch etwas Tieferes ersetzt wurde (movementWorker ← U3/aiDiv · `_waterCellAt` ← `_playerWaterContext`-inline · createWorld ← Voxel); (b) TEST-/DIAG-Verankerung ist ein Signal: eine „tote" Methode mit Playtest-Treffern ist oft Saat (der Test bewahrt sie) ODER ein Feature, das nur der Test am Leben hält (falsche Sicherheit) → Fall-für-Fall, NIE blind; (c) der awk-„nächste Methode @ N"-Range schließt den KOPF-KOMMENTAR der Folge-Methode ein → ein `sed A,Nd` nimmt Zeilen zu viel (GEMESSEN: der `_getVoxelWorker`-Kommentar mit-geschnitten) — die End-Grenze ist die Methoden-`}`, NICHT die nächste-Def-minus-1; (d) Sediment-Chaos ≠ Modul-Chaos: die Stamm-Disziplin verhindert das Modul-Wirrwarr, aber abgelöste Ringe bleiben liegen — sie wegzukehren ist Pflege, das Saat-Holz zu schonen ist Weisheit.**
- **Harmonie statt Revert — bricht ein Test bei einer neuen Kopplung, heile die HARMONIE, nimm die Kopplung NICHT weg (V17.23-Schöpfer-Lehre, die tiefste der Session)**: wenn zwei Kräfte dieselbe Sache formen (Emotion + Feld → Wetter; DSL-Farbe + Tag-Nacht → Himmel) und ein Test bricht, ist die Wurzel fast immer fehlende Harmonie — die eine Kraft ÜBERSCHREIBT die andere, statt zu VERSCHMELZEN. Der Revert (Kopplung wegnehmen) behandelt nur das Symptom + bleibt oberflächlich (verliert die Tiefe). Die Heilung ist eine **nachgebende Hierarchie**: die intentionalere Kraft (der explizite Spieler-Wille) FÜHRT, die ambiente (das Feld) WEICHT zurück, wo der Wille stark ist (×`(1−emoSignal)`, `emoSignal=|joy−sorrow|`), und FÜLLT, wo er schweigt — so überschreiben sie sich NIE, die Invariante hält per KONSTRUKTION (nicht per Wegnahme), und in der Ruhe scheint das Feld durch. **Plus: nichts SNAPPT — alles FADET.** Zwei Kräfte, die sich pro Frame gegenseitig überschreiben (V17.23: `skybox_color` snappte `nebulaColor`, das `_dayNightApplySkybox` jeden Frame überschrieb), heilt man mit einem BLEND + Fade (eine `skyTint`-Tönung, die der Tag-Nacht-Himmel `lerp`t, smooth gerampt + langsam decayt), nicht mit „wer-zuletzt-schreibt". **Die Disziplin-Frage bei JEDER neuen Welt-Kraft: harmonisch oder abrupt? überschreibt sie die andere, oder verschmilzt sie (parallel, mit Vorrang-Hierarchie)? snappt sie, oder fadet sie?** Ein Test, der bei einer Kopplung bricht, ist eine Einladung, die Harmonie zu finden — kein Befehl, die Kopplung zu töten.
- **Test-First-Mentalität**: nach jeder substanziellen Änderung Playtest-Gate, nicht nur Code-Analyse. Drei Selbst-induzierte Regressionen in dieser Session entstanden durch zu späte Browser-Tests.
- **Doku-Disziplin — drei Zeit-Ebenen, drei Heimaten, jede Datei EINE Aufgabe**: `CLAUDE.md` = was JETZT gilt (auto-geladen, schlank) · `docs/archiv/handover.md` = die Chronik (volle Wellen-Historie, jüngste oben) + wie du startest · `docs/roadmap.md` = der Plan vorwärts · `docs/state-of-realm.md` = die Vision. Pro Welle → **ein git-Commit** (die Geschichte) + **ein Chronik-Eintrag oben in `handover.md`**. Gilt eine Lehre DAUERHAFT (eine Bug-Klasse, ein technischer Stolperdraht) → **eine kuratierte Zeile in der `## Wichtige Gotchas`-Sektion dieser Datei**. **Der erzählerische Wellen-Bericht lebt NIE in dieser auto-geladenen Datei** — sonst wächst sie zur Halde (die Heilige Lektion, eine Schicht höher). Fertige Designs + Audit-Snapshots wandern nach `docs/archiv/` (verschieben, nicht löschen). **Doc-Sync ist eine Grep-Aufgabe** (W15-Lehre, V8.72): vor jedem Commit `grep -rn "<alte-Version>\|<alte-Invariantenzahl>" docs/*.md CLAUDE.md README.md` laufen und jeden Treffer heilen.

## Doc-Map

**Die EINE kanonische Doc-Landkarte lebt in `docs/README.md`** (alle Dokumente nach Zeit-Ebene geordnet: Einstiege · lebendige Anker · aktive Pläne · Referenz · Archiv). Hier nur die load-bearing **„ZUERST lesen"-Trigger** — welche Doc VOR welcher Arbeit:

**Zwei Begriffe, sonst Verwirrung:** (1) **DER AKTIVE BOGEN** ist IMMER genau EINER — heute `docs/wahrerguss.md` + seine Sub-Bögen (`docs/lebendiger-koerper-plan.md`, `docs/koerper-neuanlage-plan.md`); jede ANDERE Datei, die „aktiv/AKTIVER Bogen" in ihrem Kopf trägt, ist ein stehengebliebener Header (gilt nicht). (2) **`docs/archiv/` = die BIBLIOTHEK, nicht der Friedhof** — sie trägt DREI Regale: *Referenz* (NORMATIV/code-tragend, z. B. `wahrerbauplan.md` = der Physik-Richter) · *schlafende Pläne* (vollendet/pausiert, „ZUERST lesen" wenn ihr Thema dran ist) · *Chronik* (die Vergangenheit). „Liegt in archiv" heißt NICHT „tot" — die meisten ZUERST-lesen-Trigger unten zeigen bewusst dorthin.

- **„was als Nächstes" / DER AKTIVE BOGEN / die offenen Wellen** → **`docs/wahrerguss.md` (DER WAHRE GUSS — der Master-Plan zum atemberaubenden Bauplan):** vier GETEILTE Systeme, durch die JEDER Bauplan fließt (Detail·Schönheit·Vielfalt·Leben = Auslesewerte): A SUBSTANZ (prozed. PBR) · B KÖRPER (Skelett+Haut+Bewegung) · C GRAMMATIK-TIEFE (rekursiv) · D DETAIL-PASS. Genom reitet oben, Ω-PHYSIS garantiert, das Tor erzwingt Fidelität, das Auge richtet. **AKTIVER SUB-BOGEN → `docs/lebendiger-koerper-plan.md` (DER LEBENDIGE KÖRPER):** Form × AAA-Bewegung × Werkstatt-Anpassung vereint — die Maschinerie steht (Seele=Bauplan · `forgeAvatar` · `_animateCompoundMotion`-Kern · Resonanz), die Arbeit sind die Qualitäts-/Erlebnis-Schichten (Glied-Grammatik · Motion-Konvergenz+IK/Blend/Spring à la Overgrowth · Körper-Editor · Substanz). Flankiert von `docs/archiv/wahrerbauplan.md` (Ω-PHYSIS, Richter im Code, NORMATIV).
- **Bauplan / Physik / Stabilität / Schwerpunkt / Werkstatt / Crafting-Tiefe / Kreatur-Körper / „ein Ding verhält sich falsch"** → `docs/archiv/wahrerbauplan.md` (Ω-PHYSIS — rechne die Wahrheit [Schwerpunkt·Stützpolygon·Trägheit·Hebel·Lastpfad], lies die Eigenschaft daraus; das unsichtbare Kugel-Problem; Säule I→IV).
- **Anblick / Render / Material-Oberfläche / Boden / Gras / Bewuchs / Atmosphäre / Vegetation-Look / „die Welt sieht X aus"** → `docs/wahrerguss.md` System A (SUBSTANZ — der prozedurale Material-Pass aus Krümmung×AO×Gradient×Tags×Feld) + die volle Pfeiler-Tiefe Ω-O1..O16 (Boden·Gras·Kiesel·Pfade·Rinde·Fels·Blatt·Atmosphäre) in `docs/archiv/wahreranblick.md`. Die fünf Wände: SEHEN vor behaupten.
- **Genom / Form-Vielfalt / Grammatik / Bäume / Vegetation / Fels / Kristall / Glut / Gerät / Bauwerk-Variation / Kreatur-Form / „aus einem Seed wächst die Palette" / Platzierung-folgt-Feld** → `docs/wahrerguss.md` System C (GRAMMATIK-TIEFE — rekursiv + das geteilte Vokabular) + System B (KÖRPER). Die volle §4-Achsen-Landkarte + T1–T6-Genom-Detail + der §10-Vertikal-Bogen in `docs/archiv/wahrerwuchs.md`.
- **Werkstatt-UX / Rollen-Register / Fahrzeug-Vollendung / Befunde des Schöpfer-Audits 12.06.2026 / §8.6 Stand-der-Technik-Matrix / v1.0-DoD-Liste (Erste-5-Minuten-Geste · FPS-Sockel) / M9-Sprosse Schlüssel-Verwahrung** → `docs/archiv/meister-plan.md` (ARCHIVIERT — §8-Marsch W-A..W-H RUND + M1–M8 RUND + §8.8 Φ-Bogen RUND via V18.188–V18.191; nur §3 Aufstiegs-Leiter Sprossen 4-7 + S-Dialog-Fäden offen — die gemerkten Fäden in `docs/roadmap.md` §4).
- **Welten-Netz / öffentliche Welten / Portale-als-Links / Regions-Blasen / Naht-Handoff / Sichtbarkeit / Hausrecht / Mittragen / Compute-Spende / Portal-Hallen / „Tausende unter einem Himmel"** → `docs/archiv/archipel-plan.md` (DER ARCHIPEL-BOGEN Φ — VOLLENDET V18.188–V18.191, 7/7 Wellen GEBAUT; Fundamente nachgemessen §6; Anti-Scope-Wände §3 verbindlich. M9-Sprossen 1+2+3 stehen via Φ).
- **Worldgen-Qualität / Feuchte / Vegetation-Verteilung / Klump-Ökologie / Flora-Arten / genVersion / Makro-Anker / Baum-Varianten / Worldgen-Determinismus** → `docs/archiv/genese-plan.md` (ARCHIVIERT — Γ-KERN V18.166 + Γ4-BOGEN V18.179–.181 via Synthese-Sub-3h+3i ALLE GEBAUT [Feuchte-Feld · Kronen-Lesarten · Stream-Gesetz · Boden-atmet · Welt-Komposition · Berg-Tiefe · Slope/Rock-Foundation]; Reste klein [Γ7 · Lesart 5 · Γ3 · Γ-M · Γ8 · Totholz] in `docs/roadmap.md` §4).
- **Gesamtbild / Master-Reihenfolge / Zwillinge / Stand der Technik / Rekursion / P2P-Vollendung / Nexus-Ökonomie / Kreatur-Tiefe / Motion-Animation** → `docs/archiv/gigant-plan.md` (DER GIGANT — die neun Säulen, gemessen, mit Ankern; §5-Pfad GESCHLOSSEN — der Meister-Plan ist der Nachfolger).
- **Bauplan-Serialisierung / Import / Export / Signatur-Kanon / p2p-Protokoll-Schema / Snapshot-Schema / Versions-Felder / fremde Artefakte / Material-Magnituden** → **ZUERST `docs/taille-spec.md` (NORMATIV, Ω1 — die zwei Sätze + fünf Gesetze: Draht-Formen, must-ignore/must-preserve, Empfänger-Gesetz, EINE Versions-Semantik, Ledger-Gesetz, Namensraum)**, dann `docs/archiv/taille-plan.md` (DER EWIGKEITS-BOGEN Ω — der Bogen + die Risse + die Wellen; Fischer-verifiziert — die Gott-MAUER-Korrektur an Riss 2 steht im Kopf des Plans). **Die goldenen Dateien `spec/golden/v1/` sind EINGEFROREN — NIE regenerieren** (`scripts/diag-taille.cjs` prüft Konformanz, mintet nur wenn der Ordner fehlt).
- **UI / Menü / Fenster / Knopf / Drawer / Tab / Inventar / Werkstatt-Layout / Omnibox / Hof / Bibliothek / Einstellungen / HUD / Erreichbarkeit** → `docs/archiv/ui-putz-plan.md` (der UI-Bogen, RUND — Referenz: die Vision §1–2, die 10 Denkmuster + der 9-Schritt-Prozess, die Werkstatt als Referenz) **+ `docs/archiv/menu-feld-plan.md`** (die gemessene Feld-für-Feld-Inventur: wohin jeder klickbare Knopf gehört + warum).
- **Hof / Kreaturen / Dirigieren / Wesen-Zeilen / Gesetze (Welt-Regeln) / Kreatur-Spawn-UI** → `docs/archiv/hof-plan.md` (DER TIEFE HOF-PLAN auf Werkstatt-Niveau: der Werkstatt-Pfad gespiegelt · der Gamezyklus Knopf-für-Knopf · die Vision Dirigent+Orchester · die Wesen-Spec-Card aus den reichen Vektoren · die Bühne · Sektionen · die Partitur · die Wellen Hof-D–G).
- **„Ich" / Inventar / Charakter / Spieler-Stats / Emotionen / Rezeptbuch / eigene Seele (Avatar) / Ausrüstung** → `docs/archiv/ich-plan.md` (DER TIEFE ICH-PLAN auf Werkstatt-/Hof-Niveau: das Selbst-Porträt — die Selbst-Bühne · das geteilte Selbst-Spec-Sheet [WERTE führen, equip-gefaltet] · die reiche 6-Achsen-EMOTION + Mood als Herz-Signatur · die REISE [Journal] · Habe/Hotbar/Rezeptbuch · die Wellen Ich-A0–E + die KONSUM-Grundierung §G).
- **Bibliothek / Welt-Teilen / Welt-Karten / Andocken / Signieren / Provenienz (Stammbaum·Tagebuch) / Welt-Browser** → `docs/archiv/bibliothek-plan.md` (DER TIEFE BIBLIOTHEK-PLAN: karten-zentral — das Welt-Karten-Raster der Star · `_worldProfile` als Spec-Card-Vektor [Trust·Provenienz·DSL·Signatur·Stammbaum] · Picker→Fokus wie der Hof · die Formulare eingeklappt · „Diese Welt" zieht aus Einstellungen).
- **Einstellungen / Regler / Speicher / Tastenbelegung / Begleiter+LLM / Multi-User / Render-Tuning (Atmosphäre·Wasser·Schatten)** → `docs/archiv/einstellungen-plan.md` (DER TIEFE EINSTELLUNGEN-PLAN: ein UTILITY-Raum — KEIN Star, sondern Ordnung nach Häufigkeit + progressive disclosure; die 6 Gruppen, die ~20 Dev-Regler im eingeklappten „Feinschliff", das Welt-teilen-Duplikat → Bibliothek).
- **lebendiger Welt / Emotion / Nexus / DSL / Kreaturen** → `docs/das-lebendige-feld.md` (DER WAHRE NORDEN: das Feld, das alle lesen · schreiben · WERTEN).
- **Kampf / Waffen / Werkzeug / Rüstung / Trank / Avatar / Abbauen / Werkstatt / Schmieden / Crafting / Bauplan-Teilen** → `docs/archiv/kampf-plan.md` (Referenz · Kampf/Crafting — §11 Schöpfungs-Fluss + Resonanz-Vereinheitlichung GEBAUT; der aktive Bogen ist wahrerguss).
- **LOD / Streaming / Sicht-Ring / Wasser-Ferne / Schatten-CSM / Deko-Distanz / Draw-Calls / Kreatur-Perf** → `docs/archiv/lod-kaskade-plan.md` (DIE VEREINTE DETAIL-KASKADE — eine kamera-relative Distanz, viele Gesichter: Farbe·Geometrie·Licht·Leben·Draw-Calls; der Pfad U1–U6; die Distanz-Schwester der Aura).
- **Sicherheit / Sandbox / Portal-Kanal / Vibe-Pass / Signatur / Herkunft / Rückruf / Welt-Vertrauen / Souveräne Aktion / Immunsystem / Robustheit / Flut-Dämpfung** → `docs/archiv/robustheit-plan.md` (DER ROBUSTHEITS-BOGEN — Freiheit ohne Zusammenbruch: drei Ringe [Lokalität · Dämpfung · der eine Schlüssel] + ein lebendes Immunsystem [Herkunftskette · Rückruf · Antikörper-Archiv]; die Treppe R0–R6, gigant-plan §2-G8 + §5 Phase G).
- **W18 / Portal-Ko-Präsenz / in fremden Welten leben** → `docs/archiv/world-portal-w18-plan.md` · **Welle 12+ / Fremd-Engine** → `docs/archiv/world-portal.md`.
- **Terrain-Mesh / Chunk-Naht / LOD-Stitching / Wasser-Dynamik / kantiges Terrain / Höhlen-Eingänge / Canyons** → `docs/archiv/terrain-koharenz-plan.md` (ARCHIVIERT — der vollendete Architektur-Bogen „DIE EINE GRENZE": die Chunk-Naht als die EINE Wurzel hinter blobig·Edit-Naht·LOD-Naht·Wasser-Naht·Wasser-fließt-nicht; IPERKA, alle Schnittstellen, die These geprüft; T0–T5: Naht-Kohärenz → kantiger Mesher [Dual Contouring] → Wasser-CA → Canyons).
- **Wasser-RENDER-Schichten im Detail (das statische `L`-Modell, die 6 Schichten, die V18-Historie)** → `docs/archiv/wasser-render-architektur-plan.md` (DIE EINE KONSOLIDIERTE WAHRHEIT V18.31: der volle Bogen end-to-end, das statische Höhenfeld vs. Fluid-Dynamik, das Chaos der 30 Wellen, ALLE offenen Punkte, die 3 Schöpfer-Optionen — ersetzt die zwei alten Pläne) · HISTORIE: die Geometrie-Wahrheit „Fläche auf `L`" in `docs/archiv/wasser-finale-form-plan.md` (V18.6, subsumiert) · das ältere Modell + der V13-Bogen in `docs/archiv/hydrosphere.md`. · **Material/Form/Tag/Compound/Resonanz/Rolle/Katalysator** → `docs/archiv/crafting-konzept.md` + `docs/archiv/aktivierungsmatrix.md` (`FORM_TAG_ACTIVATION`) + `docs/archiv/resonanz-system.md` (DIE systematische Vereinheitlichung — ein Produkt-Vektor, viele Leser; der Pfad U1–U6, VOLLENDET V17.80–.85 + archiviert).
- **Stand/Vision/Plan**: `docs/archiv/handover.md` (Chronik + Start) · `docs/state-of-realm.md` (Vision) · `docs/roadmap.md` (Plan vorwärts, „OFFENE FÄDEN") · `docs/archiv/` = die Bibliothek (Referenz + schlafende Pläne + Chronik — NICHT „tot"), Index in `docs/archiv/README.md`.

## Skripte + Infrastruktur

| Datei                          | Was                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `scripts/playtest.cjs`         | Headless-Playtest mit **~5340 `check()`-Invarianten über 241 Bänder** als CI/MERGE-Gate (gemessen 06/2026; der Konsolen-Zähler driftet ±2-3 je Lauf — „Alle Invarianten OK" ist die Wahrheit, nicht die Zahl)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `scripts/playtest-fast.cjs`    | **DER SCHNELLE DEV-TIER (~20 s, `npm run playtest:fast`)** — bootet die Welt render-gestubbt + kurzer Plateau-Warmup + ~13 Kern-Gesundheits-Checks (Crash · Welt/Terrain baut · Avatar-Rig/Kreatur/Bauplan baut · Resonanz-Rollen · Stats). Fängt im Loop ~80 % der Regressionen; KEIN Eingriff in den Monolithen (null Gate-Risiko). De-Entropie 17.06.2026.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `scripts/smoke-multiuser.cjs`  | End-to-End-Test des signaling-servers: spawnt ihn, öffnet zwei WS-Clients, prüft welcome/peer-join/pos/dsl/soul/aura/vibe/rtc-Relay/peer-leave-Flow (`npm run smoke:multiuser`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `scripts/smoke-webrtc.cjs`     | W7+W16+W17 — echter Zwei-Browser-WebRTC-Test: zwei Puppeteer-Seiten treten einem Raum bei, bauen einen RTCDataChannel auf, Positions-/Bau-/Kreatur-/Stimm-Traffic fliesst peer-to-peer; W16 — A vendort eine Welt, B holt ihr Bündel peer-to-peer über das Mesh; W17 B-Relay — beide betreten dasselbe Multiplayer-Portal, A's Sub-Welt-`ws-send` erscheint bei B als `ws-recv`; W17 C — A betritt ein Multiplayer-Portal, B bekommt die `portal-invite` + folgt ihr ins selbe Portal; W17 B-JS-Compute — A wird Compute-Host, B Gast, B's Verkehr fliesst peer-to-peer durch A's Server-Kontext (die laufende Summe 12=7+5 beweist die autoritative Rechnung); W17 B-JS-Compute Phase 2 — A verlässt das Mesh, B übernimmt als Compute-Host mit frischem Server-Kontext (`npm run smoke:webrtc`) |
| `scripts/smoke-translated.cjs` | KI-Übersetzer Phase 2 — Browser-Beweis des generischen Translated-World-Renderers: öffnet `worlds/translated/`, schickt eine Beispiel-Szene per enter-Handshake, prüft den fehlerfreien Aufbau + screenshottet (`npm run smoke:translated`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `scripts/smoke-sandbox.cjs`    | V8.70 — Browser-Beweis des Untrusted-Welt-Tors: spannt ein echtes `sandbox="allow-scripts"`-iframe auf, lädt die Schwarm-Welt null-origin hinein, prüft dass die fremde Engine läuft UND beweisbar isoliert ist (`npm run smoke:sandbox`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `scripts/smoke-vendor.cjs`     | V8.71/V8.72 — W15: End-to-End-Beweis des Auto-Vendor-Pfads. Teil A POSTet echte Bündel an `/api/vendor-world` (Schreiben + Sicherheits-Wand), Teil B lädt die frisch vendorte Welt in ein null-origin-iframe + prüft, dass sie läuft + isoliert ist, Teil C startet ein lokales Fake-GitHub + lässt den save-server ein Repo holen (W15 P2 GitHub-Fetch — offline, deterministisch) (`npm run smoke:vendor`)                                                                                                                                                                                                                                                                                                                                                                                      |
| `scripts/smoke-shim.cjs`       | W17 Transport-Shim: Browser-Beweis. Der save-server injiziert den Shim bei `?anazh-shim=1` (+ NICHT ohne Marker); eine Test-Welt im echten `sandbox="allow-scripts"`-iframe öffnet einen Shim-`WebSocket`, sendet einen Ping — AnazhRealms `_portalNetReceive` macht daraus einen `subworld-net`-Mesh-Broadcast (W17 Phase B-Relay; den Zwei-Browser-Durchlauf prüft `smoke-webrtc.cjs`) (`npm run smoke:shim`)                                                                                                                                                                                                                                                                                                                                                                                   |
| `signaling-server.js`          | Multi-User-WebSocket-Broker (RFC-6455 von Hand, zero deps); seit W7 P1 auch WebRTC-Rendezvous (rtc-offer/answer/ice). `npm run signaling` startet auf Port 4313                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `start.bat`                    | Windows-Starter: bootet save-server.js + signaling-server.js + öffnet Browser. Mac/Linux: `npm run leuchtturm` (beide Server, EIN Befehl — V18.171/R-035) oder einzeln `npm start` + `npm run signaling`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `scripts/leuchtturm.cjs`       | DER LEUCHTTURM (R-035): beide zero-dep-Server mit einem Befehl — die beweisbare Souveränität; das Broker-Protokoll lebt als Andock-Vertrag in `docs/taille-spec.md` §7 (+ EN-Spiegel), Drift-bewacht im Golden-Band                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `.claude/commands/audit.md`    | `/audit`-Slash-Befehl Definition                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `.github/workflows/check.yml`  | CI-Definition (zwei Jobs)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
